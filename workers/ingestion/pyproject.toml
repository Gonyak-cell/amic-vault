[project]
name = "amic-vault-ingestion"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115,<1",
  "uvicorn[standard]>=0.34,<1"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
