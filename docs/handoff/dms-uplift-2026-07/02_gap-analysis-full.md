Warning: truncated output (original token count: 78929)
Total output lines: 1008

# 갭 분석 전문 — 최초 컨셉 대비 구현 미비점 130건

Date: 2026-07-02 (분석 기준 워킹트리: codex/matter-identity-staging-execute-gate)

이 문서는 최초 컨셉 문서 2건(reference/ 참조)과 리포지토리 실코드를 10개 차원에서 대조한 전수 분석이다.
73개 분석 에이전트가 코드를 직접 탐사했고, critical/high 판정 65건 중 62건은 별도 검증 에이전트가 반박을 시도해
CONFIRMED(주장 정확) / OVERSTATED(부분 구현 존재, 교정문 포함) 판정을 부여했다(무판정 3건은 각 차원의 검증 상한(8건/차원)에 걸린 것으로, 미검증일 뿐 기각이 아니다 — 착수 시 코드로 직접 확인).

읽는 법:
- 각 차원의 `구현요약`은 "이미 잘 구현된 것"의 증거다 — 재구현하지 말고 재사용하라.
- 각 finding은 (severity/gap_type) 제목, 요구 출처, 현재 상태, 파일:줄 증거, 검증판정, 개선안으로 구성된다.
- gap_type: MISSING(부재) / STUB(껍데기) / SHALLOW(데모 수준) / NOT_OPERATIONAL(로직 있으나 운영 불가) / QUALITY(품질 결함)
- 이 문서의 개선안은 참고용 배경이며, 규범 작업계획은 03_workplan-TUW-snapshot.md(원본: docs/execution/TUW_INTERNAL_DMS_UPLIFT_H1_H3.md)다.
- 주의: 분석 이후 워킹트리가 진전되어 일부 finding은 이미 해소되었다(예: 버전 라벨 0119, 한국어 n-gram 0109, 이메일 본문 검색 0108, Entra 검증기/Graph 트랜스포트). 착수 전 현재 코드로 재확인하라.


====================================================================================================
[matter-core] maturity=OPERATIONAL

구현요약:
Matter 코어는 실동작 수준으로 구현되어 있다. (1) 스키마: db/migrations/0013_create_matters.sql이 tenant RLS 강제, client/user FK, matter_code 유니크, 상태 CHECK(proposed→disposed 8단계), metadata 민감키 차단 CHECK를 갖춘 실제 테이블을 만들고, 0018(matter_members), 0016/0017(ethical_walls), 0034(matters.ai_policy_id), 0060(legal_holds/retention_policies/records_archives)이 이를 확장한다. (2) API: apps/api/src/modules/matter/matter.service.ts는 인메모리가 아닌 Postgres 기반으로 생성/조회/수정/상태전이/legal-hold를 구현하며, 모든 변이는 AuditService 트랜잭션+감사로그로 감싸고, permission.service의 멤버십·Ethical Wall 평가를 fail-closed로 통과해야 한다(ETHICAL_WALL_BLOCKED 분기 matter.service.ts:642-650). 상태 전이는 packages/domain/src/matter/matter-transitions.ts의 상태기계가 검증하고 R12(폐기) 전이는 차단된다. 생성 시 lead owner 멤버십 자동 부여(matter-member.service.ts:277-289)와 로컬 AI 파일정리 정책 자동 연결(matter.service.ts:450-470, 0080 마이그레이션)이 실제로 수행된다. (3) Client/Party: apps/api/src/modules/client/client.service.ts(고객 confidentiality_level 포함), party 모듈(0020-0022, matter 단위 당사자·restricted 마커) 모두 실제 DB 로직이다. matters.client_id FK로 고객-사건 관계가 스키마에 존재한다. (4) UI: /matters 목록과 /matters/[matterId] 상세(apps/web/src/app/(app)/matters/[matterId]/page.tsx)가 실제 API에 연결되어 Matter 관리상태 패널, 감사 타임라인, 문서함, 이메일 타임라인, AI 정리 준비 대시보드를 표시하고, /matters/[matterId]/team은 멤버 추가/역할변경/제거를 API로 완결한다. /matters는 hidden-route가 아니라 production visible 라우트다(apps/web/src/lib/features.ts:38-43). (5) 테스트: tests/integration/matter-core.spec.ts, matter-lifecycle.spec.ts는 목이 아닌 실제 NestJS 앱+Postgres(RLS 포함)를 기동해 cross-tenant 차단, 상태전이, 감사 메타데이터를 검증한다. 다만 §5.2 필수 필드 중 Conflicts Status·비밀등급·Retention Policy·Related Matters가 Matter 객체에 없고, 생성 파이프라인(이해상충 검사·권한 템플릿·보존정책 자동 적용), Matter Dashboard의 쟁점/리스크/기한/외부활동/AI세션 표시, 종료 시 Closing Binder/지식은행 자동 정리는 미구현이다.


--- (critical/MISSING) 이해상충(Conflicts) 검사 및 Conflicts Status 필드 전면 부재
요구출처: 사양서 §5.2 'Conflicts Status: 이해상충 검토 상태' 필수 필드, §6.1 'Matter 생성: 고객 선택, 이해상충 검사, ...'
현재상태: matters 테이블·MatterDto·생성 파이프라인 어디에도 conflicts 개념이 없다. 리포지토리 전체에서 'conflicts_status/conflictCheck/이해상충' 검색 결과는 ethical-wall 사유 enum 문자열 'conflict_check' 테스트 2건뿐이다. matter.service.create()는 클라이언트 존재·담당자 존재 검증 후 즉시 INSERT하며 상충 검사 게이트가 없다.
증거: /Users/jws/Projects/amic-vault/apps/api/src/modules/matter/matter.service.ts:165-210 (create()에 conflicts 게이트 없음); /Users/jws/Projects/amic-vault/db/migrations/0013_create_matters.sql (conflicts_status 컬럼 부재); /Users/jws/Projects/amic-vault/packages/shared/src/matter/matter.dto.ts:76-95 (MatterDto에 conflictsStatus 없음); /Users/jws/Projects/amic-vault/apps/api/src/modules/ethical-wall/ethical-wall.service.spec.ts:20 (유일한 'conflict_check' 흔적은 wall 사유 문자열)
검증판정: CONFIRMED — 모든 주장 근거를 원문으로 검증했고, 반박 증거를 찾기 위한 확장 수색에서도 구현 흔적을 발견하지 못했다.

(1) 인용 정확성 검증:
- /Users/jws/Projects/amic-vault/apps/api/src/modules/matter/matter.service.ts:165-210 — create()는 actor 역할 검증(canCreateMatterRole) → assertClientUsable → assertLeadLawyerUsable → 트랜잭션 내 insertMatter + addLeadOwner + MATTER_CREATED 감사로그 순서로만 진행한다. 이해상충 검사 게이트는 존재하지 않는다. 인용 정확.
- /Users/jws/Projects/amic-vault/db/migrations/0013_create_matters.sql — matters 테이블 DDL에 conflicts_status 컬럼 없음. matters를 ALTER하는 후속 마이그레이션은 0019(status check), 0031(legal_hold), 0034(ai_policy) 3건뿐이며 어느 것도 conflicts 컬럼을 추가하지 않는다. 인용 정확.
- /Users/jws/Projects/amic-vault/packages/shared/src/matter/matter.dto.ts:76-95 — MatterDto 필드는 matterId~up
개선안: (1) matters에 conflicts_status(not_started/in_review/cleared/blocked) 컬럼과 conflict_checks 테이블(검색 대상: clients.name, parties.name, related_client_id, 과거 matter 당사자) 마이그레이션 추가. (2) ConflictCheckService를 party/client 인덱스 기반 이름 정규화 매칭(초기: pg_trgm 유사도, 이후: OpenSearch)으로 구현하고 matter.service.create()를 'proposed 상태 생성→conflict check 완료 전 open 전이 차단'으로 상태기계(matter-transitions.ts)에 게이트 추가. (3) 검사 결과·해소 근거를 audit_events에 기록하고 blocked 시 ethical wall 자동 제안. (4) /matters/[matterId]에 Conflicts 패널과 승인 워크플로 UI 추가. iManage 급에서는 intake 단계 conflicts clearance가 법적 전제조건이므로 최우선 순위.

--- (high/SHALLOW) Matter 생성 자동화 파이프라인이 담당자 지정+AI정책만 적용 (권한 템플릿·보존정책 미적용)
요구출처: 사양서 §6.1 'Matter 생성: 고객 선택, 이해상충 검사, 담당자 지정, 권한 템플릿, 보존정책, AI 정책 자동 적용'
현재상태: create() 트랜잭션은 lead owner 멤버십 1건 삽입(addLeadOwner)과 기본 로컬 AI 정책 연결(findDefaultLocalAiPolicyId→matters.ai_policy_id)만 수행한다. 권한 템플릿(사건유형별 팀/그룹 프리셋) 개념이 permission 모듈에 없고('template' 검색 0건), retention_policies(0060)는 존재하나 matters에 retention_policy_id 컬럼이 없어 생성 시 보존정책이 연결되지 않는다.
증거: /Users/jws/Projects/amic-vault/apps/api/src/modules/matter/matter.service.ts:174-207 (생성 트랜잭션: 멤버 1건+AI정책만); /Users/jws/Projects/amic-vault/apps/api/src/modules/matter/matter-member.service.ts:277-289 (addLeadOwner 단일 owner 삽입); /Users/jws/Projects/amic-vault/db/migrations/0060_records_governance.sql:116-153 (retention_policies는 있으나 matter 연결 없음); /Users/jws/Projects/amic-vault/apps/api/src/modules/permission (권한 템플릿 코드 부재)
검증판정: CONFIRMED — 모든 인용 근거가 정확함을 확인. (1) matter.service.ts create() 트랜잭션(174-207행)은 findDefaultLocalAiPolicyId→insertMatter(ai_policy_id 포함)→addLeadOwner→감사로그만 수행하며, insertMatter(410행~)의 INSERT 컬럼에 retention/template 관련 항목 없음. (2) addLeadOwner(matter-member.service.ts:277-289)는 owner/edit 멤버십 1건만 삽입. (3) retention_policy_id는 전체 마이그레이션 중 0060의 retention_policies 테이블 자체에만 존재하고, matters를 변경하는 마이그레이션(0013/0019/0031/0034)에 보존정책 컬럼 추가 없음. API에서도 records.service.ts의 정책 CRUD(생성/조회)만 있고 matter 연결 코드 전무. (4) permission 모듈 내 'template' 검색 0건 확인. 반박 증거 수색 결과: 0088 마이그레이션에 enterprise_dms_matter_templates(사건유형별 템플릿)가 존재하나 이는 document_sets_json만 담는 문서세트 템플릿으로 팀/그룹/권한 프리셋이 아니며, applyDmsMatterTemplate(enterprise.service.ts:10
개선안: (1) matter_templates 테이블(matter_type별 기본 멤버 역할 세트, 기본 confidentiality, 기본 retention_policy_id, 기본 ai_policy_id) 신설. (2) matters에 retention_policy_id FK 추가하고 create() 트랜잭션에서 템플릿 해석→멤버 벌크 삽입→보존정책·AI정책 연결을 원자적으로 수행. (3) 템플릿 관리 API+관리 UI(/admin)를 추가하고 적용 결과를 MATTER_CREATED 감사 메타데이터에 template_ref로 기록. (4) 기존 default AI 정책 조회 로직(DEFAULT_LOCAL_AI_FILE_ORG_POLICY_NAME name-매칭)을 템플릿 기반으로 일반화.

--- (high/MISSING) Matter 종료 자동화(체결본·Closing Binder·지식은행 후보 정리) 미구현
요구출처: 사양서 §3.1 'Matter 종료 시 체결본, 최종 의견서, Closing Binder, 지식은행 후보를 자동 정리', §6.1 'Matter 종료: 체결본·최종본·Closing Binder·지식은행 후보 자동 정리'
현재상태: closing→closed 상태 전이와 closed_at 타임스탬프 자동화만 존재한다(updateMatterStatus). 'binder/Binder' 검색 결과 0건, 지식은행 후보 추출 코드 0건. records 모듈의 archiveDocument는 문서 단위 수동 아카이브이며 matter close 이벤트와 연결되지 않는다.
증거: /Users/jws/Projects/amic-vault/apps/api/src/modules/matter/matter.service.ts:254-292 (updateStatus: 전이+감사만); /Users/jws/Projects/amic-vault/packages/domain/src/matter/matter-transitions.ts:16-22 (closing→closed 전이 정의); /Users/jws/Projects/amic-vault/apps/api/src/modules/records/records.service.ts:712 (archiveDocument는 수동·문서단위)
검증판정: CONFIRMED — 인용 근거 3건 모두 정확함. (1) matter.service.ts updateStatus(254-292)는 전이 검증 + 상태 UPDATE + 감사 로그만 수행하며, 유일한 자동화는 507-509행의 SQL(closed 전이 시 COALESCE(closed_at, now()))이다. 후속 훅·이벤트·잡 큐 없음. (2) matter-transitions.ts:16-22는 closing→closed를 전이 테이블에 정의할 뿐이다. (3) records.service.ts:712 archiveDocument는 문서 단위 수동 아카이브(documents.status='archived' + records_archives insert)이며 records 모듈 어디에서도 matter closed_at을 참조하지 않는다. 반박 증거 수색 결과: 'binder'는 docs/package/03_Pyramid_PBS_DBS_Structure.md:102에 'Closing Binder | Candidate / Index / Export | 체결본 패키지'라는 *계획된* 캐퍼빌리티로만 존재하고(tools/evalset의 'closing-binder'는 합성 평가케이스 라벨), 구현 코드 0건 — 스펙에 명시된 기능이 미구현임을 오히려 강화한다. 지식은행/knowledge bank 후보 추출 코드 0건(knowledge_manager 역할과 검색용 kn
개선안: (1) MatterClosingService 신설: closing 진입 시 체크리스트 생성(체결본 지정 여부, 최종 official version 존재, 미해결 legal hold, 미처리 외부공유 링크), closed 전이를 체크리스트 통과로 게이트. (2) closing_binders 테이블+빌더: 체결본(documents.status='executed' 계열)·최종본·핵심 이메일을 매니페스트(해시 포함)로 묶어 records_archives에 연계. (3) 지식은행 후보 파이프라인: workers/ingestion에 close 이벤트 구독 잡을 추가해 재사용 승인 후보(조항·템플릿) 목록을 생성하고 검토 큐(/work)에 노출. (4) 전 과정 audit_events 기록 및 matter 상세 페이지에 Closing 탭 추가.

--- (high/SHALLOW) Matter Dashboard에 미해결쟁점·리스크·기한·외부활동·AI세션 표시 부재
요구출처: 사양서 §6.1 'Matter Dashboard: 최근 활동, 핵심 문서, 미해결 쟁점, 리스크, 기한, 외부활동, AI 세션 표시' 및 한 화면 워크스페이스 요구
현재상태: /matters/[matterId]는 최근활동(감사 타임라인), 문서 목록, 이메일 타임라인, AI 파일정리 준비 상태는 실제 API로 표시하지만 쟁점·리스크·기한·외부활동·AI세션 패널이 없다. ai_sessions 테이블은 matter_id 인덱스까지 존재하나(0050) matter 화면에 미노출. 쟁점/기한 데이터는 litigation/dd vault(0056/0057 internal_deadline, due_date)에만 있고 해당 라우트는 '운영 노출 차단'으로 봉인되어 있다. 대시보드 모듈(dashboard.service.ts)은 펌 전체 대시보드이며 per-matter 집계 엔드포인트가 없다.
증거: /Users/jws/Projects/amic-vault/apps/web/src/app/(app)/matters/[matterId]/page.tsx:104-141 (표시 패널 구성); /Users/jws/Projects/amic-vault/db/migrations/0050_create_ai_sessions.sql:57 (ai_sessions matter 인덱스 존재, UI 미연결); /Users/jws/Projects/amic-vault/apps/web/src/app/(app)/hidden-routes.test.tsx:18-56 (litigation/dd/contracts 라우트 봉인); /Users/jws/Projects/amic-vault/apps/api/src/modules/dashboard/dashboard.service.ts:150-164 (펌 단위 overview만 존재)
검증판정: CONFIRMED — 모든 인용 근거가 정확함을 직접 확인했다. (1) apps/web/src/app/(app)/matters/[matterId]/page.tsx(104-141행)는 거버넌스 컨텍스트, 감사 타임라인, 문서 목록(MatterFileSection), AI 파일정리 준비(AiPrepMatterDashboard), 이메일 타임라인, 워크플로 운영 패널만 렌더링하며 쟁점·리스크·기한·외부활동·AI세션 패널은 없다. (2) 0050 마이그레이션의 idx_ai_sessions_tenant_matter_created 인덱스는 존재하지만 API는 GET /ai/sessions/:sessionId 단건 조회뿐이고 matter별 목록 엔드포인트가 없으며, apps/web 전체에서 AI 세션을 소비하는 UI 코드가 전무하다. (3) hidden-routes.test.tsx와 features.ts가 /dd·/litigation·/contracts를 production: 'hidden'으로 봉인함을 확인했다. (4) dashboard.service.ts의 getOverview(actorUserId)는 펌 단위 집계뿐이고 per-matter 집계 엔드포인트는 어느 모듈에도 없다(matterId 스코프 라우트는 parties/members/documents/audit-events/ai-prep/email-timeline뿐). 반박 시도: API에는 GET /dd/i
개선안: (1) GET /matters/:id/dashboard 집계 엔드포인트 신설: 최근활동·핵심문서(체결본/최종본 우선순위 랭킹)·미해결쟁점·리스크·기한·외부공유활동·AI세션을 단일 응답으로 병렬 집계(permission filter 재사용). (2) matter_issues/matter_key_dates 테이블을 코어로 승격(현재 litigation/dd 전용 스키마에서 일반화)하고 봉인된 라우트 대신 matter 상세 탭으로 노출. (3) ai_sessions 최근 N건을 AI 거버넌스 요약(모델 티어·정책 결정 포함)과 함께 matter 화면에 표시. (4) external 모듈(0058)의 공유 활동을 matter별 타임라인으로 연결.

--- (medium/MISSING) §5.2 필수 필드 중 비밀등급·Retention Policy·Related Matters·Lead Partner/Associate 구분이 Matter 객체에 없음
요구출처: 사양서 §5.2 Matter 필수 필드 (Confidentiality Level, Retention Policy, Related Matters, Lead Partner/Lead Associate)
현재상태: confidentiality_level은 clients(0012)와 documents에는 있으나 matters에는 없다. retention_policy 연결 컬럼 없음. related_matters는 테이블·컬럼·DTO 어디에도 없다('related_matter' 검색 0건). 담당자는 단일 lead_lawyer_id뿐이고 matter_members 역할도 owner/member/limited_reviewer로 파트너/어소 구분이 없다. Ethical Wall Status는 요청 시점 동적 평가로 기능은 충족하나 Matter 필드/DTO로 노출되지 않아 목록·대시보드에서 상태 확인이 불가하다.
증거: /Users/jws/Projects/amic-vault/db/migrations/0013_create_matters.sql (해당 컬럼 부재); /Users/jws/Projects/amic-vault/packages/shared/src/matter/matter.dto.ts:76-95 (DTO 필드 부재); /Users/jws/Projects/amic-vault/packages/shared/src/matter/matter-member.dto.ts:4 (roles: owner/member/limited_reviewer); /Users/jws/Projects/amic-vault/apps/api/src/modules/matter/matter.entity.ts:3-20 (엔티티 필드 부재)
개선안: (1) 마이그레이션으로 matters에 confidentiality_level, retention_policy_id FK, lead_partner_id/lead_associate_id(또는 matter_members.matter_role에 partner/associate 세분화) 추가. (2) related_matters 조인 테이블(tenant_id, matter_id, related_matter_id, relation_type: 선행사건/병행사건/후속사건)과 API·UI 추가 — 이는 후속 Matter Graph 차원의 기반 데이터가 된다. (3) MatterDto에 ethicalWallStatus(active_walls_count 기반 파생 필드)를 추가해 목록/대시보드에서 즉시 확인 가능하게. (4) matter confidentiality를 permission-query.builder의 ABAC 속성으로 연결해 문서 기본 등급 상속 규칙 구현.

--- (high/NOT_OPERATIONAL) Matter 생성 UI 부재 + 외부 Matter 관리 시스템 연동이 env 플래그 기반 로컬 프로젝션에 머무름
요구출처: 사양서 §3.1 'Vault의 최상위 객체는 Matter', §6.1 Matter 생성 플로우 (사용자 관점 생성 경험)
현재상태: POST /matters API는 실동작하지만 웹에는 생성 폼이 없고 '새 Matter' 버튼이 /integrations/matter-app 상태 페이지로만 연결된다. matter-app 런타임은 MATTER_APP_SOURCE_MODE 등 env 플래그로 모드 라벨을 계산하고 lookup은 로컬 matters 테이블만 조회한다 — 'matter_app_api' 모드에서도 외부 HTTP 호출 코드가 없다(파일 내 fetch 0건). 실제 canonical matter 반영은 tools/migration/lawos-canonical-matter-reflection.mjs가 개발자 로컬 파일 경로(/Users/jws/Documents/... 하드코딩)를 읽어 오프라인 배치로 수행한다.
증거: /Users/jws/Projects/amic-vault/apps/web/src/app/(app)/matters/page.tsx:131-136 ('새 Matter'→/integrations/matter-app 링크); /Users/jws/Projects/amic-vault/apps/api/src/modules/integrations/matter-app/matter-app-runtime.service.ts:181-232 (env 플래그 status, 외부 API 호출 없음); /Users/jws/Projects/amic-vault/tools/migration/lawos-canonical-matter-reflection.mjs:10-16 (로컬 절대경로 하드코딩 소스); /Users/jws/Projects/amic-vault/apps/web/src/app/(app)/integrations/matter-app/page.tsx:19-27 (NEXT_PUBLIC env 기반 상태 표시)
검증판정: CONFIRMED — 모든 인용이 정확함을 직접 확인했다. (1) matters/page.tsx:131-136의 '새 Matter' 버튼은 /integrations/matter-app 읽기전용 상태 페이지로의 Link일 뿐이며, 웹 전체에 Matter 생성 폼·createMatter API 클라이언트 함수가 존재하지 않는다(grep 0건). POST /matters는 matter.service.ts:165에서 권한검사·트랜잭션·감사로그를 갖춘 실동작 API로 확인되어 주장의 '실동작하지만 UI 부재' 서술과 일치한다. (2) matter-app-runtime.service.ts의 status()는 MATTER_APP_SOURCE_MODE 등 env 플래그만으로 모드를 계산하고, matter_app_api 모드에서도 matterAppApiConfigured()가 env 값 존재 여부만 검사하며, lookup()은 모드와 무관하게 로컬 matters 테이블 SQL만 실행한다 — 파일 내 fetch 0건 확인. (3) lawos-canonical-matter-reflection.mjs:10-16은 개발자 로컬 절대경로를 기본값으로 하드코딩하고 특정 source revision(2026-07-01 인벤토리)과 고정 카운트(99/148)에 핀된 오프라인 배치다. 반박 시도: tools/migration의 matter-app-canonical-upsert-sync.
개선안: (1) 단기: 웹에 Matter 생성 폼(클라이언트 선택→유형→담당자→템플릿 미리보기)을 추가해 기존 POST /matters를 운영 경로로 노출하거나, LawOS를 단일 진실원천으로 유지할 경우 (2) matter_app_api 모드에 실제 HTTP 클라이언트(서명된 계약 스키마, 재시도·서킷브레이커, staleness 검증)를 구현하고 오프라인 reflection 러너를 웹훅/이벤트 프로젝션 소비자(workers/ingestion 잡)로 전환. (3) 하드코딩된 로컬 경로 의존을 제거하고 아티팩트 전달을 서명된 번들+버전 계약으로 대체. (4) 상태 페이지를 실제 연동 헬스체크(마지막 sync 시각, drift 카운트)로 승격.

--- (medium/NOT_OPERATIONAL) Client 객체 관리 UI 부재 및 Matter 화면의 고객 표시 누락
요구출처: 사양서 §3.1 '고객...을 Matter에 연결', §5 데이터 모델의 Client 최상위 객체와 고객-사건 관계
현재상태: Client API(CRUD, confidentiality_level, 감사)는 실구현이고 matters.client_id FK로 관계도 스키마에 존재하지만, 웹에 /clients 라우트가 없고 api-client.ts에 client 호출 함수도 없다. Matter 상세 화면은 clientId만 보유하고 고객명을 표시하지 않는다(matter-app lookup의 client_name join은 Matter code 픽커 전용).
증거: /Users/jws/Projects/amic-vault/apps/api/src/modules/client/client.service.ts:100-125 (실구현 API); /Users/jws/Projects/amic-vault/apps/web/src/app/(app) 디렉터리에 clients 라우트 부재; /Users/jws/Projects/amic-vault/apps/web/src/lib/api-client.ts (clients 엔드포인트 호출 0건); /Users/jws/Projects/amic-vault/apps/web/src/app/(app)/matters/[matterId]/page.tsx:108-125 (고객 표시 없음)
개선안: (1) MatterDto에 clientDisplayName(권한 필터 통과 시)을 추가하고 matter 상세·목록에 고객 표시. (2) /clients 관리 화면(목록·상세·해당 고객의 matter 목록·confidentiality 표시)을 추가해 고객→사건 내비게이션 축 완성. (3) 이후 conflicts 엔진과 고객 검색 인덱스를 공유하도록 clients에 alias/구명칭 필드 확장.

--- (medium/NOT_OPERATIONAL) Party(당사자) 모듈이 API 전용으로 웹 UI에 미노출
요구출처: 사양서 §3.1 '고객, 상대방, 이해관계자...를 Matter에 연결'
현재상태: party 모듈은 matter 단위 당사자 CRUD, 당사자 유형/역할 taxonomy(0021), restricted 마커(0022), 종료 matter 변이 차단까지 실구현이고 tests/integration/party.spec.ts로 검증되지만, 웹 어디에서도 호출되지 않는다(api-client.ts에 party 함수 0건).
증거: /Users/jws/Projects/amic-vault/apps/api/src/modules/party/party.service.ts:36-58 (실구현); /Users/jws/Projects/amic-vault/db/migrations/0020_create_parties.sql, 0021, 0022; /Users/jws/Projects/amic-vault/tests/integration/party.spec.ts (DB 통합 테스트 존재); /Users/jws/Projects/amic-vault/apps/web/src/lib/api-client.ts (party 호출 부재)
개선안: (1) matter 상세에 당사자 패널(역할·유형·restricted 배지·관련 고객 링크) 추가. (2) 당사자 데이터를 conflicts 검사 엔진의 1차 매칭 소스로 연결. (3) restricted 당사자는 safeLabel 규칙과 동일한 노출 최소화 정책 적용.

--- (low/SHALLOW) Ethical Wall이 동적 평가로는 견고하나 Matter 수준 상태 가시성이 없음
요구출처: 사양서 §5.2 'Ethical Wall Status: 윤리장벽 설정 여부' (Matter 필드로서의 상태)
현재상태: ethical_walls는 matter FK 기반 실제 테이블(0016/0017)이고 permission 평가·matter.service의 ETHICAL_WALL_BLOCKED fail-closed 분기, /walls 관리 UI, tests/integration/ethical-wall.spec.ts까지 잘 구현되어 있다. 다만 Matter 목록/상세/DTO 어디에도 '이 Matter에 wall이 설정되어 있는지' 상태 필드가 없어, 관리자가 /walls 화면으로 이동해야만 확인 가능하다.
증거: /Users/jws/Projects/amic-vault/db/migrations/0016_create_ethical_walls.sql:6-18; /Users/jws/Projects/amic-vault/apps/api/src/modules/matter/matter.service.ts:642-650 (fail-closed 분기); /Users/jws/Projects/amic-vault/packages/shared/src/matter/matter.dto.ts:76-95 (wall status 필드 부재); /Users/jws/Projects/amic-vault/apps/web/src/app/(app)/matters/[matterId]/page.tsx:92-96 (/walls로 링크만 제공)
개선안: (1) matter 조회 쿼리에 active wall EXISTS 파생 컬럼을 추가해 MatterDto.ethicalWallActive로 노출(권한 있는 사용자에게만). (2) matter 상세 관리상태 패널과 목록 보안 컬럼에 wall 배지 표시. (3) wall 생성/해제 시 matter 감사 타임라인에 이미 기록되는 이벤트를 상태 배지 툴팁과 연결.

--- (low/QUALITY) Matter 서비스 단위테스트가 순수 함수 가드만 검증 (핵심 검증은 통합테스트에 의존)
요구출처: 깊이 검증 체크포인트 (6) 테스트가 실동작 검증인가
현재상태: apps/api/src/modules/matter/matter.service.spec.ts는 역할 가드 함수 3개만 검사하는 26줄 스모크다. 대신 tests/integration/matter-core.spec.ts와 matter-lifecycle.spec.ts가 실제 앱+Postgres로 생성/수정/전이/RLS/cross-tenant를 검증하므로 실질 커버리지는 통합 계층에 존재한다. 이는 갭이라기보다 구조적 선택이나, 생성 파이프라인 확장(템플릿·conflicts) 시 서비스 계층 회귀 안전망이 얇다.
증거: /Users/jws/Projects/amic-vault/apps/api/src/modules/matter/matter.service.spec.ts:8-26 (가드 함수만 테스트); /Users/jws/Projects/amic-vault/tests/integration/matter-core.spec.ts:136-262 (실 DB 통합 검증); /Users/jws/Projects/amic-vault/tests/integration/matter-lifecycle.spec.ts:154-210 (전이·차단 검증)
개선안: conflicts/템플릿/closing 자동화가 추가되는 시점에 트랜잭션 오케스트레이션(부분 실패 롤백, 감사 누락 방지)을 검증하는 서비스 계층 테스트(테스트 컨테이너 또는 쿼리 클라이언트 페이크)를 보강하고, matter 생성 파이프라인의 각 자동 적용 단계를 개별 검증하는 통합 케이스를 추가.

====================================================================================================
[document-vault] maturity=OPERATIONAL

구현요약:
핵심 Document Vault는 실사용 가능한 수준으로 구현되어 있다. (1) 업로드 파이프라인: 디스크 기반 multipart 수신, 확장자 화이트리스트+매직바이트 MIME 스니핑(apps/api/src/modules/document/validators/mime-type.validator.ts), SHA-256 해시(integrity/sha256.util.ts), 자체 구현 SigV4 S3/MinIO 어댑터로 스트리밍 저장(apps/api/src/modules/storage/s3-storage.adapter.ts), 트랜잭션 실패 시 스토리지 보상 삭제, 해시 기반 중복 후보 탐지와 사용자 결정(new_document/new_version/cancel) 강제(apps/api/src/modules/document/document-upload.service.ts:479-553), 감사이벤트에 해시 기록. (2) 버전관리: 불변 document_versions(version_no, current/superseded, supersedes 체인, 부분 유니크 인덱스; db/migrations/0029) + 미커밋 브랜치의 편집 라이프사이클(체크아웃 락, 내부 subversion, 리뷰어 게이트, 공식 버전 승격; db/migrations/0092, document-editing.service.ts 84KB, tests/integration/document-access/document-editing-lifecycle.spec.ts). (3) 상태관리: 사양의 초안/검토중/고객송부/상대방송부/최종본/체결본을 포함한 11개 canonical 상태 CHECK 제약(db/migrations/0028) + legal hold/soft delete/restore(document-lifecycle.service.ts). (4) 메타데이터: document_type, confidentiality_level, privilege_status, ai_allowed 컬럼(0027, 0034) 및 문서별 AI 정책 모듈(modules/ai-policy). (5) 무결성: file_objects sha256+불변 트리거(0025, 0026), 다운로드 시 실객체 해시 재검증과 DOCUMENT_INTEGRITY_ALERT(integrity/integrity-check.service.ts), append-only 감사(0006)에 업로드/열람/다운로드/버전 이벤트가 해시와 함께 남아 chain-of-custody 기반 제공. (6) 비동기 추출: pg-boss 큐(재시도·데드레터·singletonKey)로 FastAPI ingestion worker에 실 HTTP 디스패치, canonical_documents 저장, 검색 인덱스 동기화 후킹(extraction/extraction-queue.service.ts, extraction-dispatcher.ts, workers/ingestion/app/extract_router.py — pypdf/python-docx/hwpx 실파서). (7) 미리보기: PDF 원본 + DOCX→PDF LibreOffice headless 변환·아티팩트 캐시(0035, preview.service.ts, workers/ingestion/app/converters/docx_to_pdf.py), Range 지원, 권한·감사 연동, 웹 iframe 표시. (8) UI: /files는 보이는 운영 라우트(hidden-routes는 contracts/dd/litigation/launch/scale만 봉인)로 전체 문서함, 다중 파일 업로드 큐, 중복 결정 다이얼로그, /documents/[id] 액션센터(미리보기·버전목록·사유 기반 통제 다운로드·감사 타임라인·subversion 리뷰/승격)가 실제 API에 연결. (9) 테스트: 실제 Nest 앱+Postgres+스토리지를 띄우는 E2E 통합테스트(tests/integration/document-hash.spec.ts, document-access/*, extraction.spec.ts, preview.spec.ts). 반면 OCR·워터마크 실렌더링·Source(출처) 필드·10,000파일 배치·HTML/ZIP/HWP추출·버전 라벨 표준(v0.1/Final/Execution/Clean/Markup)·폴더 업로드·인쇄/복사 제한은 미구현 또는 껍데기다.


--- (critical/STUB) OCR이 상태값(ocr_pending)만 존재하고 실제 OCR 엔진이 전혀 없음
요구출처: 사양서 §6.2 'OCR, PDF/Word 텍스트 추출', §5.3 'OCR Text / Vector ID', §16.1 MVP 필수기능 'OCR', §20 '한글 HWP/HWPX, OCR 별도 개발항목'
현재상태: PDF에 텍스트 레이어가 없으면 worker가 status='ocr_pending', method='ocr_required'를 반환하고 canonical_documents에 그대로 저장될 뿐, 이 상태를 소비해 OCR을 수행하는 컴포넌트가 리포지토리 어디에도 없다. ingestion worker 의존성에 OCR 라이브러리(tesseract 등)가 전무하다(pyproject.toml: fastapi/pypdf/python-docx/uvicorn뿐). 스캔 PDF·이미지(jpg/png 업로드 허용)는 영구히 검색 불가 상태로 남는다.
증거: workers/ingestion/app/parsers/pdf.py:26-29 (본문 없으면 ocr_pending 반환 후 종료); workers/ingestion/app/parsers/types.py:22-28 (ocr_pending 상태 정의만 존재); workers/ingestion/pyproject.toml:5-11 (OCR 의존성 없음); apps/api/src/modules/document/extraction/extraction-dispatcher.ts:233-299 (ocr_pending을 저장만 하고 후속 처리 없음)
검증판정: CONFIRMED — 모든 인용 근거가 정확함을 파일 직접 열람으로 확인했다. (1) workers/ingestion/app/parsers/pdf.py:26-29는 텍스트 레이어가 없으면 ExtractionResult.ocr_pending()을 반환하고 종료하며, types.py:22-28은 status='ocr_pending', method='ocr_required', body_text='', confidence=0.0의 상태 정의뿐이다. (2) workers/ingestion/pyproject.toml 의존성은 fastapi/pypdf/python-docx/python-multipart/uvicorn뿐으로 OCR 라이브러리가 전무하다. (3) extraction-dispatcher.ts storeResult(233-299행)는 ocr_pending을 canonical_documents에 upsert만 하고, status==='ready'일 때만 검색 인덱스 동기화를 트리거하며 후속 OCR 작업 디스패치가 없다. (4) 리포지토리 전체(apps/api, apps/web, workers, packages, db/migrations, tools)를 tesseract/easyocr/paddleocr/textract/ocrmypdf/clova/upstage/form-recognizer/document-intelligence/google-vision/abbyy
개선안: 1단계: ingestion worker에 OCR 스테이지 추가 — ocr_pending 상태의 canonical_documents를 스캔하는 pg-boss 후속 큐(document.ocr)를 만들고, PaddleOCR(한국어 인식률 우수) 또는 Tesseract+kor traineddata를 별도 GPU/CPU 워커 컨테이너로 분리. 2단계: PDF를 페이지 단위로 래스터라이즈(pdfium/poppler)해 페이지별 OCR 결과와 confidence를 canonical_documents에 병합하고 extraction_method='ocr'로 기록. 3단계: OCR 완료 시 기존 SearchIndexSyncHook을 재사용해 인덱스 재동기화. 4단계: 저신뢰(<0.8) 페이지 목록을 문서 액션센터에 노출해 사람 검수 루프를 붙이면 iManage급 품질이 된다.

--- (critical/STUB) 외부공유 워터마크가 실제 렌더링 없이 watermarkApplied:true 메타데이터 선언만 함
요구출처: 사양서 §6.2 '워터마크', §8.1 '외부사용자는 ... 워터마크 ... 를 거쳐 접근', §10 VDR '워터마크'
현재상태: external_secure_links에 watermark_required 컬럼과 watermarkRef(문자열 참조)가 있고 manifest/downloadTicket API가 watermarkApplied: true를 반환하지만, PDF에 워터마크를 실제로 스탬핑하는 코드가 리포지토리 전체에 없다(pdf-lib/reportlab 등 렌더링 경로 부재). 외부 사용자가 받는 파일은 원본 그대로이며, API 응답은 사실과 다른 '적용됨' 클레임을 반환한다. 내부 미리보기/다운로드에도 워터마크 옵션이 없다.
증거: apps/api/src/modules/external/external.service.ts:659-685 (manifest가 watermarkApplied: true를 렌더링 없이 반환); apps/api/src/modules/external/external.service.ts:269-270 (watermarkRef는 'watermark:linkId:userId' 문자열 생성뿐); grep 결과: apps/api·workers 전체에 pdf-lib/drawText/스탬핑 코드 부재 (workers/ingestion/app/converters/에 docx_to_pdf.py만 존재)
검증판정: CONFIRMED — 모든 주장 근거가 정확히 검증됨. (1) external.service.ts의 manifest(654-686행)와 downloadTicket(688-743행)은 access_count 증가와 감사 로그 기록 후 watermarkApplied: true를 하드코딩으로 반환하며 파일 바이트를 전혀 처리하지 않음. (2) watermarkRefFor(269-271행)는 'watermark:linkId:userId' 문자열 생성뿐. (3) packages/shared/src/external/external-types.ts:158,171에서 watermarkApplied가 z.literal(true)로 선언되어 스키마 차원에서 false가 될 수 없는 순수 선언값임이 확인됨. (4) 리포지토리 전체(apps/api, apps/web, workers, packages, db, tools) grep 결과 PDF 스탬핑 코드 부재: reportlab은 workers/ingestion/pyproject.toml에 있으나 테스트 픽스처 PDF 생성(test_extract_router.py)에만 사용, pypdf는 텍스트 추출용 PdfReader만 사용, docx_to_pdf.py는 subprocess 기반 포맷 변환. (5) 웹 외부포털 UI(external-portal-client.tsx:62-63,85-86)는 '워터마크가 적용됩니다' 텍스트 라벨
개선안: 1단계: ingestion worker에 /watermark/pdf 엔드포인트 추가(pypdf 병합 또는 reportlab 오버레이로 수신자 이메일·시각·링크ID 대각선 스탬프 + 페이지별 각인). 2단계: 외부 다운로드 티켓 상환 시 파생 file_object(document_preview_artifacts 패턴 재사용)로 워터마크본을 생성·캐시하고 워터마크본 해시를 감사에 기록. 3단계: 내부 다운로드에도 confidentiality_level='restricted' 문서는 정책상 워터마크본만 제공하는 옵션을 permission 정책 속성으로 추가. 4단계: 뷰어 화면 워터마크(동적 오버레이)를 iframe 미리보기에 추가해 스크린캡처 억지력 확보.

--- (high/MISSING) 문서 Source(고객제공/상대방제공/내부작성/공개자료) 필드가 스키마·DTO·UI 어디에도 없음
요구출처: 사양서 §5.3 Document 필수 필드 'Source: 고객제공, 상대방제공, 내부작성, 공개자료'
현재상태: file_objects.source_system은 'upload'/'email_ingest'/'migration'으로 전송 채널만 구분하며, 문서의 법률적 출처(누가 제공했는가)를 담는 컬럼이 documents 테이블에 없다. 업로드 DTO(uploadDocumentFieldsSchema)에도 source 필드가 없고 메타데이터 편집 화면에도 없다. DD/소송에서 출처 구분은 privilege 판단과 증거 관리의 전제라서 핵심 결손이다.
증거: db/migrations/0023_create_file_objects.sql:12-13 (source_system CHECK: upload/email_ingest/migration만); packages/shared/src/dto/document/upload-document.dto.ts:12-31 (source 필드 부재); db/migrations/0027_extend_documents_metadata.sql (document_type/confidentiality/privilege만 추가, source 없음)
검증판정: CONFIRMED — 적대적 반박을 시도했으나 갭 주장을 뒤집을 증거를 찾지 못함. (1) 인용 확인: 0023의 source_system CHECK는 인용대로이며, 0035·0092에서 preview_derived/document_edit가 추가돼 현재 5개 값이지만 모두 전송·파생 채널 구분일 뿐 법률적 출처가 아님. (2) documents 테이블 전 마이그레이션(0024/0027/0028/0031/0033/0034) 확인 — source 계열 컬럼 없음, metadata_json 컬럼조차 없음. (3) uploadDocumentFieldsSchema·updateDocumentMetadataSchema 모두 source 부재이고 .strict()라 임의 필드도 불가. (4) client_provided/counterparty_provided/documentSource/provided_by/custodian 및 한국어 용어(고객제공/상대방제공/내부작성)가 apps/api·apps/web·workers·packages·db·tools 전체에서 0건. upload-preflight의 sourceMode는 matter-app 동기화 모드, 0082 DMS metadata_fields_json은 테넌트 설정 계약일 뿐 per-document 값 저장 테이블이 없어 우회 경로도 없음. (5) UI 문서 컴포넌트(upload-metadata-profile.tsx
개선안: 1단계: documents에 source 컬럼(client_provided/counterparty_provided/internal_work_product/public) + CHECK 제약 마이그레이션 추가, 기본값 internal_work_product. 2단계: 업로드 DTO·메타데이터 편집 DTO·문서 프로필 UI에 노출하고 감사 이벤트(DOCUMENT_METADATA_CHANGED)에 before/after 기록. 3단계: filename-metadata.parser와 이메일 인제스트(발신자 도메인 기반)로 source 자동 제안. 4단계: 검색 refiner와 권한 정책 속성(예: counterparty_provided는 외부공유 기본 차단)에 연결.

--- (high/NOT_OPERATIONAL) 10,000파일 배치 업로드가 미연결 상태 — BulkUploadJob은 어떤 큐·컨트롤러에서도 호출되지 않는 고아 코드
요구출처: 사양서 §13 '대량 업로드: 10,000개 파일 배치 처리', §6.2 '대량 업로드, 폴더 업로드'
현재상태: BulkUploadJob.process()는 존재하지만 이를 enqueue하는 producer/컨트롤러/큐 등록이 전무하다(document.module.ts에 provider 등록만; pg-boss에는 extraction 큐만 등록됨). DTO도 최대 100개로 제한(bulkUploadJobSchema max 100). HTTP multipart는 요청당 파일 1개로 고정(multipart.config.ts limits.files:1)이고, 웹 업로드 패널은 브라우저에서 파일별로 순차 HTTP 호출을 도는 클라이언트 루프라 10,000개 규모에서는 실패 복구·재개·속도 모두 불가하다.
증거: apps/api/src/modules/document/bulk-upload.job.ts:22-51 (순차 for 루프 처리기, 호출자 없음); apps/api/src/modules/document/document.module.ts:43 (provider 등록만 존재); packages/shared/src/dto/document/bulk-upload.dto.ts:31 (items max 100); apps/api/src/modules/document/multipart.config.ts:27 (limits.files: 1); apps/web/src/components/document/document-upload-panel.tsx:180-234 (클라이언트 측 파일별 순차 업로드 루프)
검증판정: CONFIRMED — 주장 근거 5건 모두 실측으로 정확함. (1) apps/api/src/modules/document/bulk-upload.job.ts의 process()는 순차 for 루프이며, 리포 전체 grep 결과 호출자는 자기 spec(bulk-upload.job.spec.ts:47)뿐 — 컨트롤러·큐·워커·CLI 어디에서도 호출/enqueue되지 않는 고아 코드. (2) document.module.ts:43,56에 provider 등록·export만 존재하고 이를 소비하는 모듈 없음. (3) pg-boss createQueue/send는 extraction·search-index·ai-prep 큐(+DLQ)에만 존재하고 bulkUploadQueueName('document.bulk-upload')은 어디에도 등록·발행·소비되지 않음(주장의 "extraction 큐만 등록"은 부정확하나 결론에 무영향). (4) bulk-upload.dto.ts:30 items max 100 확인(주장은 31행으로 1행 오차). (5) multipart.config.ts:27 limits.files:1 확인, document.controller.ts:138,232 모두 단일 파일 FileInterceptor, API 전체에 FilesInterceptor 없음. (6) document-upload-panel.tsx:148-224는 브라우저 측 파일별 순차 루프
개선안: 1단계: 배치 세션 모델 도입 — bulk_upload_batches/items 테이블 + 매니페스트 등록 API, 항목별 상태 머신(pending/uploaded/failed/duplicate). 2단계: S3 멀티파트 presigned URL(또는 tus 재개형 업로드)로 바이트 전송을 API 서버에서 분리하고, 업로드 완료 이벤트를 pg-boss 배치 큐로 처리(기존 extraction 큐 패턴 재사용, 동시성 워커 N개). 3단계: 웹에 배치 진행률 패널(재시도·중복 일괄 결정·CSV 리포트)과 폴더 업로드를 함께 연결. 4단계: 10,000파일 합성 배치 부하 게이트를 tools/에 추가해 처리율(SLO)을 릴리즈 검증에 포함.

--- (high/SHALLOW) 텍스트 추출 포맷 커버리지가 PDF/DOCX/HWPX 3종뿐 — xls/ppt/msg/eml/txt/hwp는 업로드는 되나 추출이 실패 처리됨
요구출처: 사양서 §6.2 'Word, PDF, Excel, PPT, MSG, EML, HTML, TXT, ZIP, HWP/HWPX 지원 구조', §6.5 'OCR 검색, 이메일 검색'
현재상태: extract_router._parse는 pdf/docx/hwpx만 처리하고 나머지는 415(UNSUPPORTED_FILE_TYPE)로 거부 → dispatcher가 WORKER_REJECTED failed로 저장하므로 xls/xlsx/ppt/pptx/msg/eml/txt/csv/doc 문서는 본문 검색·AI에서 영구 배제된다. HWP 바이너리는 매직바이트로 명시 거부(UNSUPPORTED_HWP_BINARY). MSG/EML 파서(parsers/email.py)는 존재하지만 extract 엔드포인트에 연결되어 있지 않다(테스트에서만 import).
증거: workers/ingestion/app/extract_router.py:29-37 (_parse: pdf/docx/hwpx 외 415, HWP 바이너리 즉시 실패); workers/ingestion/app/parsers/email.py (parse_eml_envelope/parse_msg_skeleton — tests/test_email_parser.py에서만 사용); apps/api/src/modules/document/extraction/extraction-dispatcher.ts:160-169 (415 응답을 WORKER_REJECTED failed로 확정); apps/api/src/modules/document/validators/file-extension.validator.ts:8-28 (업로드는 19개 확장자 허용)
검증판정: CONFIRMED — 모든 인용 근거가 실제 코드와 일치하며, 반박 증거를 찾지 못했다. (1) workers/ingestion/app/extract_router.py:29-38의 _parse는 pdf/docx/hwpx만 파서에 연결하고, OLE 매직바이트(D0CF11E0)는 즉시 UNSUPPORTED_HWP_BINARY로 실패 처리, 그 외 확장자는 415 UNSUPPORTED_FILE_TYPE을 던진다. (2) workers/ingestion/pyproject.toml 의존성은 pypdf·python-docx뿐 — xls/ppt/msg/hwp 파싱 라이브러리 자체가 없다. (3) parsers/email.py(parse_eml_envelope/parse_msg_skeleton)는 리포 전체에서 tests/test_email_parser.py만 import — extract 엔드포인트 미연결이 맞다. (4) extraction-dispatcher.ts:155-171에서 415는 transient(≥500/408/429)가 아니므로 WORKER_REJECTED failed로 canonical_documents에 확정 저장된다. (5) file-extension.validator.ts는 19개 확장자 업로드 허용 — 업로드-추출 커버리지 불일치가 실재한다. 대안 구현 수색 결과: convert_router는 docx→pdf 프리뷰 변환 전용, email-a
개선안: 1단계: txt/md/csv는 즉시 인라인 추출(인코딩 감지 포함) 추가 — 가장 저비용. 2단계: xlsx/pptx는 openpyxl/python-pptx, doc/xls/ppt는 LibreOffice headless 변환 경유(기존 docx_to_pdf 인프라 재사용) 텍스트 추출. 3단계: eml/msg는 이미 있는 email.py 파서를 extract_router에 연결하고 첨부 분리를 문서 링크로 저장. 4단계: HWP 바이너리는 hwp5txt(pyhwp) 또는 hwp→hwpx 변환 체인을 도입해 한국 로펌 필수 요건을 충족. 각 단계마다 extraction_method enum과 실파일 fixture 테스트를 추가.

--- (high/QUALITY) 1GB 파일 지원 미달 — 일반 업로드 기본 200MB 제한이고 미리보기·추출 경로가 파일 전체를 메모리에 버퍼링
요구출처: 사양서 §13 '문서 업로드: 1GB 파일 지원'
현재상태: DOCUMENT_UPLOAD_MAX_BYTES 기본값이 200MB이고 1GB는 migration 소스에만 기본 허용된다(env로 올릴 수는 있음). 더 근본적으로 preview.service와 extraction-dispatcher가 streamToBuffer로 S3 객체 전체를 메모리에 올린 뒤 처리하며(Range 요청조차 전체 버퍼 후 subarray), extraction은 파일 전체를 FormData Blob으로 워커에 전송한다. 1GB급 문서 다수 동시 처리 시 API 프로세스 OOM이 확실한 구조다.
증거: apps/api/src/modules/document/validators/file-size.validator.ts:3-4 (기본 200MB, migration만 1GB); apps/api/src/modules/preview/preview.service.ts:128-141 (streamToBuffer 후 Range subarray); apps/api/src/modules/document/extraction/extraction-dispatcher.ts:111-113 (전체 파일 버퍼링 후 워커 전송)
검증판정: CONFIRMED — 주장 근거 3건 모두 원문과 정확히 일치하며, 반박 가능한 대체 구현을 적극 수색했으나 발견하지 못했다.

(1) 업로드 제한 — /Users/jws/Projects/amic-vault/apps/api/src/modules/document/validators/file-size.validator.ts:3-4에 DEFAULT_DOCUMENT_UPLOAD_MAX_BYTES = 200*1024*1024(200MB), DEFAULT_DOCUMENT_MIGRATION_UPLOAD_MAX_BYTES = 1GB가 그대로 존재. sourceSystem==='migration'일 때만 1GB 허용(line 42). .env.example:20도 DOCUMENT_UPLOAD_MAX_BYTES=209715200(200MB)으로 배포 기본값을 고정. multipart.config.ts의 multer fileSize limit도 documentUploadMaxBytes()를 사용하므로 일반 업로드는 기본 200MB에서 차단된다.

(2) 미리보기 전체 버퍼링 — preview.service.ts:129에서 `await streamToBuffer(object.body)`로 S3 객체 전체를 메모리에 적재 후, Range 요청도 line 141 `full.subarray(...)`로 전체 버퍼에서 잘라낸다. DOCX 변환 경로(line 184)도 원본 전체를 버
개선안: 1단계: 일반 업로드 기본 한도를 1GB로 상향하되 S3 멀티파트 업로드(presigned part URL)로 API 경유 바이트를 제거. 2단계: 미리보기는 S3 GET Range를 어댑터에 추가해 브라우저 Range 요청을 스토리지 Range로 패스스루(현재 어댑터는 Range 미지원). 3단계: 추출은 워커가 storage_uri를 받아 직접 S3에서 스트리밍하도록 계약 변경(사전서명 GET URL 전달)해 API 이중 버퍼링 제거. 4단계: 대용량 파일 스모크(1GB 합성 파일 업로드→미리보기→추출)를 통합테스트 게이트에 추가.

--- (medium/SHALLOW) 버전관리 표준(v0.1 내부초안→v1.0 고객송부→v2.x 상대방→Final→Execution Copy, Clean/Markup)이 정수 version_no로만 모델링됨
요구출처: 사양서 §6.2 버전관리 표준 표 (v0.1/v1.0/v2.0/v2.1/v3.0/Final/Execution Copy/Clean·Markup)
현재상태: document_versions는 1부터 증가하는 정수 version_no와 current/superseded 상태만 가진다. 사양의 의미론적 버전 라벨(v0.1 내부초안, v1.0 고객송부본, Execution Copy)과 Clean/Markup 구분을 저장할 컬럼이 없다. 문서 단위 status(client_sent/final/executed)는 있으나 '어떤 버전이 고객송부본인가'를 버전 레벨에서 식별할 수 없다. filename-metadata.parser가 파일명에서 v1/최종 라벨을 '제안'만 하고 버리며 저장되지 않는다. 편집 라이프사이클의 subversion(내부초안 반복)은 v0.x의 정신을 일부 구현했지만 공식 버전 라벨 체계는 없다.
증거: db/migrations/0029_create_document_versions.sql (version_no integer, 라벨 컬럼 없음); apps/api/src/modules/document/version-number.resolver.ts (max+1 정수 증가만); apps/api/src/modules/document/filename-metadata.parser.ts:34-41 (versionLabel 추출 후 제안으로만 반환); db/migrations/0092_create_document_editing_foundation.sql:241-320 (subversion은 내부 반복만 담당)
개선안: 1단계: document_versions에 version_label(semver 유사 major.minor), version_significance(internal_draft/client_sent/counterparty_sent/negotiation/final/execution_copy), rendition_type(clean/markup) 컬럼 추가. 2단계: 문서 status 전이(고객송부 등)와 버전 라벨 부여를 하나의 트랜잭션 액션('v1.0으로 고객송부')으로 묶는 서비스 메서드와 감사 이벤트 추가. 3단계: markup 버전은 base clean 버전 FK를 갖게 해 Clean/Markup 쌍을 비교 화면에 연결. 4단계: 업로드 시 filename 파서의 versionLabel 제안을 라벨 필드 기본값으로 승격.

--- (medium/MISSING) 폴더 업로드(폴더 구조 보존 업로드) 미구현
요구출처: 사양서 §6.2 '대량 업로드, 폴더 업로드', §16.1 MVP '폴더/태그 혼합 구조'
현재상태: 업로드 입력은 multiple 파일 선택만 지원하고 webkitdirectory/디렉터리 드래그앤드롭 처리 코드가 없다. 폴더 경로를 보존해 매핑할 서버측 필드(상대경로)도 업로드 DTO에 없다.
증거: apps/web/src/components/document/document-upload-panel.tsx:265-269 (input type=file multiple만, webkitdirectory 부재); packages/shared/src/dto/document/upload-document.dto.ts:12-31 (상대경로/폴더 필드 없음)
개선안: 1단계: 업로드 패널에 디렉터리 선택(webkitdirectory)과 DataTransferItem.webkitGetAsEntry 기반 폴더 드래그앤드롭을 추가. 2단계: 업로드 필드에 sourceRelativePath를 추가하고 governed folder(이미 있는 saved_searches 기반 폴더, 0083)와 별개로 물리 폴더 트리 테이블 또는 문서 태그로 보존. 3단계: 배치 업로드 세션(위 배치 파이프라인)과 결합해 폴더 단위 진행률·실패 재시도를 제공.

--- (medium/MISSING) 인쇄/복사 제한이 없음 — 다운로드 통제는 구현되었으나 뷰어 단 억제는 전무
요구출처: 사양서 §6.2 '워터마크, 다운로드/인쇄/복사 제한', §7.2 '열람, 다운로드, ... 인쇄, 복사 ... 를 분리'
현재상태: 다운로드는 권한 평가 + 사유코드 요구(DOWNLOAD_REASON_REQUIRED) + 감사 기록으로 실제 통제된다(잘 구현됨). 그러나 인쇄·복사는 권한 모델에 별도 액션으로 존재하지 않고, 미리보기는 브라우저 네이티브 PDF 뷰어 iframe이라 인쇄/저장/텍스트 복사가 모두 자유다. contextmenu/user-select 억제나 뷰어 정책 코드가 없다.
증거: apps/api/src/modules/document/document-lifecycle.service.ts:254-276 (다운로드 사유코드 통제는 구현됨); apps/web/src/components/document/document-action-center.tsx:2452-2457 (네이티브 iframe PDF 뷰어 — 인쇄/복사 제한 불가 구조); grep 결과: web 전체에 print/copy/contextmenu 억제 코드 부재
개선안: 1단계: permission 정책 속성에 can_print/can_copy 액션을 추가(0046 permission_policy_attributes 확장). 2단계: 미리보기를 PDF.js 자체 렌더러로 교체해 텍스트 레이어 비활성(복사 차단), 인쇄 버튼 제거, 뷰어 위 사용자 워터마크 오버레이를 정책 기반으로 적용. 3단계: 제한 문서는 다운로드 자체를 워터마크본으로 대체하고 인쇄 이벤트를 감사에 남기는 print-to-audit 흐름 추가.

--- (medium/MISSING) Track Changes(Word 변경추적) 및 PDF 주석 추출 미구현
요구출처: 사양서 §6.2 'Track Changes 및 PDF 주석 추출'
현재상태: docx 파서는 본문 텍스트만 추출하며(w:ins/w:del 변경추적 마크업 처리 없음), pypdf 기반 PDF 파서도 annotation 추출 코드가 없다. 협상 문서의 마크업 히스토리가 검색·AI 계층에 전혀 반영되지 않는다.
증거: workers/ingestion/app/parsers/docx.py (본문 추출만); workers/ingestion/app/parsers/pdf.py:17-24 (page.extract_text만, annotations 미접근); grep 결과: workers/api 전체에 track change/revision/annotation 처리 코드 부재
개선안: 1단계: docx 파서를 lxml 직접 파싱으로 확장해 w:ins/w:del/w:comment를 구조화(author/date/변경 전후 텍스트) 추출, canonical_documents에 revisions JSONB 필드 추가. 2단계: pypdf의 /Annots 순회로 하이라이트·노트 주석을 추출해 동일 구조에 저장. 3단계: 버전 비교 화면(Clean/Markup)과 계약 인텔리전스 모듈에 이 데이터를 공급해 '상대방 마크업 반영본' 분석 요구(§6.2 문서 비교 고도화)의 기반으로 사용.

--- (medium/MISSING) HTML/ZIP 업로드 자체가 차단됨 (지원 포맷 목록 대비 결손)
요구출처: 사양서 §6.2 'Word, PDF, Excel, PPT, MSG, EML, HTML, TXT, ZIP, HWP/HWPX 지원 구조'
현재상태: 기본 허용 확장자 목록에 html/htm과 zip이 없어 업로드가 UNSUPPORTED_FILE_TYPE으로 거부된다(env로 확장자만 추가해도 MIME 스니퍼 테이블에 없어 실패). ZIP 해제 인제스트(압축 내 파일 개별 등록) 개념도 없다. 나머지 사양 포맷(doc/docx/xls/xlsx/ppt/pptx/msg/eml/txt/hwp/hwpx)은 업로드 허용이 확인됨.
증거: apps/api/src/modules/document/validators/file-extension.validator.ts:8-28 (html/zip 부재); apps/api/src/modules/document/validators/mime-type.validator.ts:19-39 (supportedMimeExtensions에 html/zip 부재)
개선안: 1단계: html/htm(text/html 스니핑, 저장 시 sanitize된 뷰만 미리보기 제공)과 zip(application/zip 매직바이트)을 밸리데이터에 추가. 2단계: ZIP은 업로드 후 ingestion worker에서 안전 해제(zip bomb 한도, 경로 traversal 차단)해 내부 파일을 개별 문서로 등록하고 원본 ZIP과 부모-자식 링크 유지. 3단계: html은 본문 텍스트 추출 파서(bs4/lxml)를 extract_router에 추가.

--- (medium/SHALLOW) 미리보기 포맷 커버리지가 PDF/DOCX 2종뿐이고 대용량에 취약한 전체 버퍼링 구조
요구출처: 사양서 §6.2 문서 링크/열람 흐름, 담당 차원 요구 '미리보기(PDF.js/Office viewer)'
현재상태: PDF는 원본 스트림, DOCX는 LibreOffice 변환 아티팩트 캐시로 실동작한다(운영 가능). 그러나 xls/xlsx/ppt/pptx/hwp/hwpx/msg/eml/이미지는 PREVIEW_CONVERSION_UNAVAILABLE로 미리보기가 불가하고, 클라이언트는 PDF.js가 아닌 브라우저 네이티브 iframe이라 하이라이트·페이지 썸네일·검색어 점프 같은 DMS급 뷰어 기능이 없다(검색 hit fragment 전달만 있음). 변환은 요청 경로에서 동기 수행되어 최초 열람 지연이 크다.
증거: apps/api/src/modules/preview/preview.service.ts:123-126,172-177 (pdf 외에는 docx만, 그 외 conversionUnavailable); workers/ingestion/app/convert_router.py (docx-to-pdf 단일 변환 엔드포인트); apps/web/src/components/document/document-action-center.tsx:2452-2457 (네이티브 iframe)
개선안: 1단계: LibreOffice 변환 대상을 xls/xlsx/ppt/pptx/doc(및 hwpx via hwp 필터 가능 시)로 확장 — convert_router에 범용 /convert/office-to-pdf 추가. 2단계: 변환을 업로드 직후 비동기 사전 생성(pg-boss preview 큐)으로 옮겨 최초 열람 지연 제거, 실패 시 document_preview_artifacts.status='failed' 재시도. 3단계: 클라이언트를 PDF.js 기반 커스텀 뷰어로 교체해 검색어 하이라이트/썸네일/정책 기반 복사·인쇄 억제/화면 워터마크를 통합. 4단계: eml/msg는 파싱된 envelope+본문을 HTML 렌더로 표시.

--- (medium/NOT_OPERATIONAL) 저장계층 암호화 훅이 Noop — encryption_key_id가 항상 null인 인터페이스 전용 구현
요구출처: 사양서 §7/§11 보안 요구(저장 암호화·키 관리) 및 §5.3 무결성 체계의 전제, file_objects.encryption_key_id 스키마 의도
현재상태: EncryptionHook 인터페이스와 file_objects.encryption_key_id 컬럼은 준비되어 있으나 프로덕션 모듈 배선이 NoopEncryptionHook 고정이라 애플리케이션 레벨(테넌트별 envelope) 암호화가 없다. MinIO/S3 서버측 암호화 헤더(x-amz-server-side-encryption)도 어댑터가 설정하지 않는다.
증거: apps/api/src/modules/storage/storage.module.ts:20 (ENCRYPTION_HOOK → NoopEncryptionHook 고정); apps/api/src/modules/storage/noop-encryption.hook.ts:12-23 (passthrough, encryptionKeyId: null); apps/api/src/modules/storage/s3-storage.adapter.ts:85-103 (SSE 헤더 미설정)
개선안: 1단계: 최소선으로 S3 put에 SSE-KMS(or SSE-S3) 헤더를 추가하고 버킷 정책으로 강제. 2단계: 테넌트별 DEK를 KMS envelope로 관리하는 KmsEncryptionHook 구현(스트리밍 AES-256-GCM, encryption_key_id에 키 참조 기록) 후 모듈 배선을 env 기반 전환. 3단계: 키 로테이션 배치와 키 폐기(테넌트 오프보딩 crypto-shredding) 절차를 tools/ 게이트로 추가.

====================================================================================================
[document-editing] maturity=OPERATIONAL

구현요약:
편집 라이프사이클 코어(check-out/check-in)는 실제 운영 가능한 수준으로 구현되어 origin/main에 병합됨(commit 077b420 "DMS-EDIT-LIFECYCLE"). (1) DB: db/migrations/0092_create_document_editing_foundation.sql — document_edit_sessions(활성 잠금 partial unique index, RLS FORCE, 상태-타임스탬프 CHECK), document_subversions(vN.M 내부 검토본, 파일해시, visibility_scope, client_save_id 멱등키), document_subversion_reviewers/review_decisions(리뷰어 ACL+결정), document_versions에 promoted_from_subversion_id/published_by 컬럼 추가. (2) API: apps/api/src/modules/document/document-editing.service.ts(2,375줄) — checkout(FOR UPDATE + base version stale 충돌 감사), heartbeat TTL 연장, saveSubversion(pg_advisory_xact_lock으로 subversion_no 직렬화, 저장 실패 시 스토리지 보상 삭제, clientSaveId 멱등 재시도), check-in/cancel, promote(전원승인 리뷰 게이트 assertReviewGateSatisfied → 현재 버전 supersede → 새 공식 버전 생성 → extraction 큐+검색 인덱스 sync 훅 → 감사이벤트), 모든 경로에 permission/ethical-wall fail-closed + 법적보존 immutable-state guard. (3) Web: apps/web/src/components/document/document-action-center.tsx(2,660줄)에 편집 세션 시작/체크인/취소, 편집 패키지(vault_text vs binary_roundtrip), 텍스트 네이티브 편집기(textarea), 저장본 업로드, 검토본 목록·리뷰어 지정·승인/변경요청·공식 승격 UI가 apps/web/src/lib/api-client.ts:307-522의 실제 엔드포인트에 연결됨. /documents/[id]?edit=1 인텐트 자동화(editIntentFromParams, 자동 checkout→패키지 준비) 구현. documents 라우트는 hidden-routes에 봉인되지 않은 가시 라우트. (4) Outlook 편집 링크: apps/api/src/modules/outlook/outlook-document-insertion.service.ts:99가 /documents/{id}?edit=1&versionId=...#document-editing 경로를 생성하고 apps/web/src/app/outlook-addin/outlook-addin-client.tsx:365-366이 Office.js로 메일 본문에 편집 바로가기를 삽입(권한·legal hold·멱등·감사 포함). (5) 검증: tests/integration/document-access/document-editing-lifecycle.spec.ts가 실제 NestJS AppModule+Postgres로 checkout→네이티브 저장→리뷰→check-in→공식 승격 E2E를 검증(합성 목이 아님). 반면 Word/Office 네이티브 편집·co-authoring·WOPI는 코드 부재(ADR-016 "read/download-only launch" 결정으로 의도적 유예, Office 문서는 binary_roundtrip 다운로드-업로드 순환), §6.2 문서비교 고도화(조항별 diff·법률적 의미 요약·상대방 유리 변경 탐지·삭제조항 복원·변경요약 이메일 초안)는 전면 미구현, Track Changes 추출은 [[ADD:]]/<ins> 합성 마커 regex(apps/api/src/modules/contract-intel/contract-parser.ts:44-49)일 뿐 실제 DOCX w:ins/w:del 미해석, PDF 주석 추출 부재(workers/ingestion/app/parsers/pdf.py는 pypdf 텍스트만). OPERATIONAL 판정은 편집 라이프사이클 코어에 한정되며, 컨셉이 요구한 비교·Office 통합 축은 findings의 critical 갭임.


--- (critical/MISSING) 문서비교 고도화(§6.2) 전면 미구현 — 조항별 diff, 법률적 의미 요약, 상대방 유리 변경 탐지, 삭제조항 복원, 변경요약 이메일 초안 모두 부재
요구출처: 사양서 §6.2 '문서 비교 고도화': 두 버전 간 조항별 변경사항 추출, 변경사항의 법률적 의미 자동 요약, 상대방에게 유리한 변경 탐지, 삭제된 조항 복원 및 협상 포지션별 변경 분류, 고객에게 보낼 변경요약 이메일 초안 생성 (+ §13 성능목표: 100페이지 Word 비교 30초 이내)
현재상태: apps/api 전 모듈에 두 버전 비교 엔드포인트가 없고(grep 'compare/diff/비교' 결과는 메타데이터 diff_keys 감사 로깅뿐), web UI에도 버전 비교 화면이 없음(document-action-center는 버전 목록/추가만 제공). 유일한 인접 기능은 contract-intel의 버전별 조항/정의어/redline 해시 추출(contract_redline_changes)과 rule engine의 redline_change_count 메트릭인데, 이는 단일 버전 내 마커 파싱이지 버전 간 비교가 아님. AI 요약 파이프라인도 없음 — packages/ai/src/index.ts는 로컬 Gemma 라우트 설정 타입만 export하고, '이메일 초안/summariz' 검색 결과는 마이그레이션 러너의 카운트 요약뿐.
증거: apps/api/src/modules/document/document.service.ts:171 (documentMetadataDiffKeys — 메타데이터 변경 감사용 diff가 유일한 'diff'); apps/api/src/modules/contract-intel/contract-intel.service.ts:538-599 (contract_redline_changes 조회 — 버전 간 비교 아님, 해시/오프셋만 저장); packages/ai/src/index.ts:1-50 (RetrievalOrchestrator 'not-implemented-before-r6' + LocalGemma 라우트 설정만 존재); apps/web/src/components/document/document-action-center.tsx (버전 목록/추가 UI만 존재, 비교 UI 부재)
검증판정: CONFIRMED — 갭 주장의 핵심(§6.2 문서비교 고도화 5개 기능 전면 미구현)은 정확하다. (1) 두 버전 비교 엔드포인트 부재 확인 — apps/api 전체에서 diff는 메타데이터 감사용 diff_keys(document.service.ts:171 documentMetadataDiffKeys, matter/client/dd 동종 패턴)뿐이고, document-editing.controller.ts의 전 엔드포인트는 edit-sessions/subversions/check-in/promote/reviews로 compare 라우트가 없다. (2) contract_redline_changes는 단일 버전 내 마커 파싱 산출물임을 확인 — contract-parser.ts:44-48이 [[INS:]]/[[DEL:]]/<ins>/<del> 정규식 파싱이고, db/migrations/0054의 테이블 정의도 version_id 단위 change_type/offset/text_hash만 저장하며, contract-rule-engine.ts:222의 redline_change_count는 그 개수 집계다. 버전 간 비교 로직은 어디에도 없다. (3) web UI에 버전 비교/redline 화면 0건 — document-action-center.tsx는 버전 목록/추가 + subversion 편집·리뷰 UI만 제공. (4) 5개 세부 기능 개별 부재 확인:
개선안: 3단계로 구축: (1) 비교 기반 — ingestion worker(workers/ingestion)에 DOCX→정규화 조항 트리 추출 엔드포인트를 추가하고(기존 contract-parser의 조항 경계 로직을 워커로 이관·강화), 버전 쌍에 대해 조항 정렬(헤딩 해시 매칭 + 임베딩 유사도) 후 조항 단위 LCS diff를 수행하는 document_version_comparisons / comparison_clause_changes 테이블과 POST /documents/{id}/comparisons API를 pg-boss 비동기 잡으로 구현(100p/30s 목표는 워커 프로세스 풀로 충족). (2) 법률 의미 계층 — 기존 AI 정책 게이트(ai-policy, Permission-before-AI)와 로컬 Gemma 라우트를 재사용해 조항 변경별 요약·유불리 분류(당사자 역할은 matter party 데이터 활용)·협상 포지션 태깅을 생성하고 AI_RESPONSE 감사에 연결. 삭제조항 복원은 base version 조항 트리에서 원문 오프셋 참조로 구현. (3) 산출 계층 — 비교 결과 화면(/documents/[id] 비교 탭, clean/markup 토글)과 변경요약 이메일 초안 생성기를 Outlook send-file 모듈과 연결. 순서상 (1)을 편집 라이프사이클의 subversion↔공식버전 비교에 먼저 적용하면 즉시 체감 가치가 남.

--- (critical/MISSING) Word/Office 네이티브 편집·co-authoring 부재 — Office 문서는 다운로드-업로드 순환(binary_roundtrip)만 가능
요구출처: 요구 컨셉 'Office(Word) 통합 편집, co-authoring, Word/HWP 네이티브 편집 연동(WOPI/Office Online, 로컬 앱 핸드오프)이 있는가 아니면 업로드-다운로드 순환뿐인가' + 사양서 §12.3 Microsoft 365(Word/Excel/PowerPoint) 연동
현재상태: 업로드-다운로드 순환이 맞음. NATIVE_EDITABLE_MIME_TYPES가 text/plain·markdown·csv·json 텍스트 계열로 한정되고 그 외(docx/pdf/hwp 전부)는 editPackageMode='binary_roundtrip' — 사용자가 기준 파일을 내려받아 로컬에서 편집 후 검토본으로 재업로드. WOPI 호스트 엔드포인트(CheckFileInfo/GetFile/PutFile/Lock), ms-word: 프로토콜 핸들러, Office Online 연동 코드가 리포 전체에 없음. ADR-016이 'read/download-only launch'를 명시하고 Office 통합을 M365/OneDrive 레인으로 유예했으며, 프로덕션 UI 스모크가 co-authoring/live edit 주장을 능동적으로 금지함. office-production-open-save-sync-claim.ts는 Vault 통합이 아니라 OneDrive 소스 컷오버 마이그레이션 레인의 외부 증적 클레임 게이트임. apps/desktop(Tauri)은 로컬 편집 브리지가 아닌 thin shell.
증거: apps/api/src/modules/document/document-editing.service.ts:73-79,264-266 (NATIVE_EDITABLE_MIME_TYPES 텍스트 한정, editPackageMode 분기); docs/adr/ADR-016-document-editing-and-office-flow.md ('read/download-only launch' 결정, Option B Office integration 유예); tools/release/check-production-ui-smoke.mjs:1157,1208-1209,1934-1935 (co-authoring/live edit 클레임 금지 게이트); apps/api/src/tools/office-production-open-save-sync-claim.ts:1-60 (마이그레이션 컷오버 클레임 도구 — Vault 네이티브 Office 편집 아님)
검증판정: CONFIRMED — 모든 주장 근거를 원문 대조로 확인했고, 반박 증거 수색은 실패했다. (1) apps/api/src/modules/document/document-editing.service.ts:73-79에서 NATIVE_EDITABLE_MIME_TYPES는 application/json, text/csv, text/markdown, text/plain 4종이며, 260-266행의 isNativeEditableMimeType은 'text/*' 접두어 전체 + 위 set만 허용 — docx/xlsx/pptx/pdf/hwp는 전부 editPackageMode='binary_roundtrip'. (2) docs/adr/ADR-016은 "Adopt a read/download-only launch model" 결정과 "Office live edit or coauthoring claims" 금지를 명시하고, Option B(Office integration)를 "deferred to the Microsoft 365/OneDrive lane"으로 유예. ADR-017도 "Status: Accepted for planning gate only". (3) tools/release/check-production-ui-smoke.mjs의 officeOneDriveAdrGateFiles 게이트는 'no coauthoring live edit claim' 패턴을 강제하
개선안: 이미 완성된 edit session 잠금 모델 위에 WOPI 호스트를 얹는 것이 최단 경로: (1) document_edit_sessions의 lock_token_hash를 실제 클라이언트 왕복 토큰으로 승격하고 WOPI Lock/Unlock/RefreshLock을 세션 상태에 매핑, (2) /wopi/files/{fileId} CheckFileInfo·GetFile·PutFile 엔드포인트를 구현해 PutFile을 saveSubversion 경로에 연결(해시 검증·감사 재사용), (3) M365 WOPI 계약(CSPP) 또는 자체 호스팅 Collabora/OnlyOffice로 브라우저 편집 제공, (4) co-authoring은 WOPI coauth 확장 또는 SharePoint Embedded 위임으로 2단계 도입, (5) 로컬 앱 핸드오프는 Tauri shell에 ms-word:ofe|u|<wopi-url> 프로토콜 실행 + 세션 heartbeat 에이전트를 추가. ADR-016 Review Trigger('lock/check-out/check-in schema and APIs are approved')는 이미 충족되었으므로 ADR 개정부터 시작.

--- (high/SHALLOW) Track Changes 추출이 합성 마커 regex — 실제 DOCX 리비전(w:ins/w:del) 미해석, 소비 UI도 프로덕션 봉인
요구출처: 사양서 §6.2 Document Vault: 'Track Changes 및 PDF 주석 추출'
현재상태: contract-parser.ts의 redline 파싱은 [[ADD:...]]/[[DEL:...]]/<ins>/<del> 합성 마커를 regex로 찾는 것이 전부 — 실제 Word Track Changes XML(w:ins, w:del, w:rPrChange, 작성자/일시 메타데이터)은 어디서도 해석하지 않음. ingestion worker의 docx 파서는 w:t 텍스트 노드만 추출하므로 삽입 리비전은 확정 텍스트처럼 흡수되고 삭제 리비전(w:delText)은 무음 소실됨. 추출 결과(contract_redline_changes)도 오프셋+해시만 저장하고, 이를 소비하는 /contracts 화면은 hidden-routes로 프로덕션 차단 상태('운영 노출 차단' 렌더링). 즉 데이터 수집은 합성 경로, 노출은 봉인.
증거: apps/api/src/modules/contract-intel/contract-parser.ts:44-49 (redlinePatterns — [[ADD:]]/[[DEL:]]/<ins>/<del> 합성 마커 regex); workers/ingestion/app/parsers/docx.py (w:t 노드만 순회 — w:ins/w:del 리비전 미처리); apps/web/src/app/(app)/hidden-routes.test.tsx:18-56 (ContractsPage가 route-blocked 상태 렌더 검증)
검증판정: CONFIRMED — 모든 인용이 정확하고 반증이 발견되지 않음. (1) apps/api/src/modules/contract-intel/contract-parser.ts:44-49의 redlinePatterns는 [[ADD:]]/[[DEL:]]/<ins>/<del> 합성 마커 regex 4개가 전부이며, workers/ingestion/app/parsers/contract.py도 동일한 합성 regex의 Python 복제본(그마저 extract_router/main.py에 미연결, 자체 테스트만 참조). (2) workers/ingestion/app/parsers/docx.py는 w:t 노드만 순회(17/26/55행) — iter(w:t)가 w:ins 하위로 내려가므로 삽입 리비전은 확정 텍스트처럼 흡수되고, w:delText 요소는 어디서도 읽지 않아 삭제 리비전은 무음 소실됨. 주장 그대로. (3) apps/workers/packages/db/tools 전체 소스 grep에서 w:ins|w:del|delText|rPrChange|trackChanges 히트 0건 — 실제 Track Changes XML 해석 구현이 리포 어디에도 없음. (4) db/migrations/0054의 contract_redline_changes는 change_type/offset/text_hash/parser_version만 저장 — 리비전 작성자/일시 컬럼 없음. (5)
개선안: (1) workers/ingestion docx 파서를 확장해 w:ins/w:del/w:moveFrom/w:moveTo/w:rPrChange를 작성자·타임스탬프와 함께 구조화 리비전 레코드로 추출하는 /extract-revisions 엔드포인트 추가(python-docx 하위 lxml 직접 순회), (2) extraction-dispatcher에 리비전 레인을 추가해 contract_redline_changes를 실제 리비전 provenance(author_hash, revision_ts, change_type)로 채우도록 스키마 확장 마이그레이션, (3) 편집 라이프사이클과 연결 — binary_roundtrip 검토본 저장 시 base version 대비 Track Changes를 자동 추출해 리뷰어 게이트 화면에 변경 목록을 표시, (4) 거버넌스(원문 미저장) 제약이 필요하면 리비전 텍스트는 AI 레인에서만 휘발 처리하고 저장은 해시+오프셋 유지.

--- (high/MISSING) PDF 주석(코멘트/하이라이트) 추출 부재
요구출처: 사양서 §6.2 Document Vault: 'Track Changes 및 PDF 주석 추출'
현재상태: PDF 파서는 pypdf PdfReader로 페이지 텍스트만 추출하며 /Annots 딕셔너리(주석·하이라이트·스티키노트·프리텍스트)를 읽는 코드가 리포 어디에도 없음(grep 'annot' 결과는 Python typing Annotated뿐). 변호사 검토 워크플로의 핵심 입력인 PDF 마크업이 시스템에 전혀 수집되지 않음.
증거: workers/ingestion/app/parsers/pdf.py (pypdf page.extract_text()만 수행, /Annots 미처리); workers/ingestion/app/extract_router.py:28-37 (pdf/docx/hwpx 텍스트 추출 라우팅이 전부)
검증판정: CONFIRMED — (1) 주장 근거 인용 정확성 확인: /Users/jws/Projects/amic-vault/workers/ingestion/app/parsers/pdf.py는 pypdf PdfReader로 page.extract_text()만 수행(29줄 전체 확인, /Annots·page.annotations 접근 없음). 암호화 PDF는 실패 처리, 텍스트 없으면 OCR 대기 처리뿐. /Users/jws/Projects/amic-vault/workers/ingestion/app/extract_router.py:29-38의 _parse()는 pdf/docx/hwpx 확장자별 텍스트 추출 라우팅이 전부이며, ExtractResponse 스키마(status/extraction_method/body_text/confidence/failure_reason_code)에 주석 필드 자체가 없음. (2) 리포 전역 수색 결과 반박 증거 부재: 'annot' 대소문자 무시 grep의 코드 히트는 전부 `from __future__ import annotations`(Python), `betaCannotSee*` 테스트 변수명, typing Annotated뿐. '/Annots', 'FreeText', 'Squiggly', 'StrikeOut', '/Subtype', sticky note 관련 코드 0건. 대체 PDF 라이브러리(pymupdf/fitz, pdfp
개선안: ingestion worker에 pymupdf(fitz) 기반 주석 추출 엔드포인트를 추가해 주석 타입·페이지·좌표·작성자·내용을 구조화 추출하고, document_annotations 테이블(RLS+tenant 스코프, 기존 마이그레이션 컨벤션 준수)에 저장 후 문서 상세의 감사 타임라인/검토본 패널에 노출. 편집 라이프사이클의 subversion이 PDF인 경우 주석 diff(추가/삭제된 주석)를 리뷰 게이트 화면에 표시하는 것까지가 iManage급 기준.

--- (medium/NOT_OPERATIONAL) 편집 라이프사이클이 릴리스 스모크·운영 증적 체계에 미포함 — 운영 검증 공백
요구출처: 요구 컨셉 '현재 브랜치의 편집 라이프사이클 후보가 실제 운영 가능한 수준인지 집중 점검' + 탐사 시작점 tools/release/dms-main-loop-smoke.mjs
현재상태: 코드 자체는 main에 병합된 운영급이지만, DMS 릴리스 스모크(dms-main-loop-smoke.mjs)는 업로드→미리보기→버전→검색→감사→legal hold→권한거부만 검증하고 checkout/check-in/subversion/promote 스텝이 전혀 없음(grep 무히트). 검증은 리포 내부 통합테스트(document-editing-lifecycle.spec.ts — 실 DB E2E로 품질은 높음)뿐이고, docs/current-code-state.md는 이 기능을 'candidate'로 표기하며 전체 릴리스 상태가 TECHNICAL-READY/EXTERNAL-EVIDENCE-REQUIRED. 즉 외부 스테이징/프로덕션 런타임에서 편집 루프가 돌았다는 증적 레인이 없음.
증거: tools/release/dms-main-loop-smoke.mjs:79-330 (DMS-SMOKE 체크 목록에 편집 라이프사이클 부재); docs/current-code-state.md:54-56 ('DMS editing lifecycle candidate' 표기); tests/integration/document-access/document-editing-lifecycle.spec.ts:204-234 (리포 내부 E2E만 존재)
개선안: dms-main-loop-smoke.mjs에 DMS-SMOKE-01x 시리즈로 checkout→edit package→subversion 저장→리뷰어 지정/승인→check-in→promote→버전 목록에서 promoted_from_subversion_id 확인→감사 이벤트(DOCUMENT_CHECKED_OUT/…/VERSION_PROMOTED) 검증 스텝을 추가하고, 기존 launch closeout 증적 체계(owner evidence receipt)에 편집 레인을 등록해 candidate 딱지를 제거.

--- (medium/QUALITY) 잠금 운영성 부족 — 백그라운드 만료 스위퍼·관리자 강제 해제·실사용 잠금 토큰 부재
요구출처: 요구 컨셉 'check-out/check-in' 엔터프라이즈 심층(iManage/NetDocuments급 잠금 관리: 관리자 unlock, 만료 알림, 클라이언트 토큰)
현재상태: 잠금 만료는 접근 시점 lazy 처리뿐(isExpired→expireSession이 checkout/heartbeat/조회 경로에서만 호출) — 소유자가 이탈하면 다른 사용자가 접근을 시도하기 전까지 세션이 'active'로 남고 만료 알림도 없음. 관리자/matter owner의 강제 해제 경로가 없음(requireOwnedActiveSession이 lock_owner_user_id === actorUserId 강제, cancel도 소유자 전용). lock_token_hash는 서버가 랜덤 생성 후 즉시 폐기하는 vestigial 값으로 어떤 클라이언트도 토큰을 왕복하지 않음(WOPI/데스크톱 연동 대비 미완). document 모듈에 cron/스케줄 잡 없음.
증거: apps/api/src/modules/document/document-editing.service.ts:305-311 (randomLockTokenHash — 토큰 생성 즉시 해시만 저장, 반환 없음); apps/api/src/modules/document/document-editing.service.ts:1966-1982 (requireOwnedActiveSession — 소유자 외 해제 불가); apps/api/src/modules/document/document-editing.service.ts:2001-2032 (expireSession — 접근 경로에서만 호출, 스위퍼 부재)
개선안: (1) 기존 pg-boss 큐(extraction-queue 패턴 재사용)로 만료 세션 스위퍼 잡을 추가해 DOCUMENT_LOCK_EXPIRED 감사 + dms_notifications 알림 생성, (2) matter owner/tenant admin용 강제 해제 엔드포인트(사유코드 필수, DOCUMENT_CHECKIN_CANCELLED + 관리자 액션 감사)를 추가, (3) checkout 응답에 잠금 토큰 원문을 1회 반환하고 save/check-in에서 토큰 해시 검증을 요구해 향후 WOPI/데스크톱 클라이언트의 세션 위조를 방지.

--- (medium/SHALLOW) HWP 지원이 hwpx 텍스트 추출뿐 — 바이너리 HWP는 명시적 거부, HWP 편집 연동 전무
요구출처: 사양서 §6.2 'HWP/HWPX 지원 구조' + 요구 컨셉 'Word/HWP 네이티브 편집 연동'
현재상태: extract_router가 OLE 시그니처(바이너리 .hwp)를 UNSUPPORTED_HWP_BINARY로 즉시 실패 처리하고, hwpx만 ZIP/XML 섹션 텍스트 추출. 한국 로펌 실무 문서의 상당수가 .hwp인데 텍스트 추출·검색 인덱싱·비교 대상에서 제외됨. 편집은 binary_roundtrip 순환에 의존(이는 HWP 특성상 합리적이나, 추출 실패로 검토본 인덱싱/AI 대상에서도 빠짐).
증거: workers/ingestion/app/extract_router.py:29-31 (payload.startswith(b'\xd0\xcf\x11\xe0') → UNSUPPORTED_HWP_BINARY); workers/ingestion/app/parsers/hwpx.py:25-42 (hwpx ZIP 섹션 텍스트 추출만)
개선안: ingestion worker에 hwp5(pyhwp) 또는 한컴 SDK 기반 .hwp→텍스트/hwpx 변환 레인을 추가하고, 변환 실패 시 ocr_pending과 유사한 hwp_conversion_pending 상태로 격리. 편집 측은 로컬 한컴오피스 핸드오프(Tauri 프로토콜 실행 + edit session heartbeat)를 Word 핸드오프와 동일 아키텍처로 제공.

--- (medium/NOT_OPERATIONAL) 스캔 PDF OCR 미구현 — ocr_pending 상태로 영구 대기하는 봉인 레인
요구출처: 사양서 §6.2 'OCR, PDF/Word 텍스트 추출' 및 §16.1 MVP 필수 기능 'OCR'
현재상태: 텍스트 없는 PDF는 ExtractionResult.ocr_pending()으로 표시만 되고 실제 OCR 엔진(tesseract 등) 연동이 없음. 마이그레이션 러너들이 'ocr_pending_requires_ocr_lane: true'로 카운트만 하며 처리 레인 자체가 부재 — 스캔본은 편집 검토·검색·비교 파이프라인 전체에서 제외됨.
증거: workers/ingestion/app/parsers/pdf.py:27-28 (본문 없으면 ocr_pending 반환, 후속 처리 없음); apps/api/src/tools/gemma-customer-wide-prep-runner.ts:142 (ocr_pending_requires_ocr_lane: true — 레인 부재 인정)
개선안: ingestion worker에 tesseract(kor+eng) 또는 PaddleOCR 기반 /ocr 엔드포인트를 추가하고, extraction-dispatcher가 ocr_pending 상태 버전을 pg-boss 재큐잉으로 OCR 레인에 흘려 extraction_status를 ready로 승격시키는 비동기 파이프라인 완성. 편집 차원에서는 OCR 텍스트가 있어야 스캔 계약서도 비교·조항 추출 대상이 됨.

--- (low/MISSING) 버전관리 표준 라벨(v0.1 내부초안/v1.0 고객송부본/Execution Copy/Clean·Markup) 부재
요구출처: 사양서 §6.2 '버전관리 표준' 표(v0.1 내부초안 ~ Execution Copy 체결본, Clean/Markup 구분)
현재상태: 버전은 정수 version_no와 내부 검토본 vN.M(subversion) 조합만 존재. 고객송부본/상대방송부본/협상본/최종본/체결본 같은 법률 실무 의미 라벨과 Clean/Markup 구분이 스키마·UI 어디에도 없음(grep '체결본|클린본|Execution Copy' 무히트). promote 시 publish_reason_code(바운디드 코드)가 있어 확장 접점은 마련되어 있음.
증거: apps/api/src/modules/document/version-number.resolver.ts (정수 버전 번호만 관리); db/migrations/0092_create_document_editing_foundation.sql:424-438 (publish_reason_code — 의미 라벨 확장 접점)
개선안: document_versions에 version_label(내부초안/고객송부본/상대방송부본/협상본/최종본/체결본) + clean_markup 구분 컬럼을 바운디드 enum으로 추가하는 마이그레이션과, promote/버전 업로드 UI에서 라벨 선택 + Records(체결본→records-canonical 후보 자동 연계) 거버넌스 규칙을 구현.

--- (low/QUALITY) Vault 네이티브 편집기가 1MB 텍스트 textarea — 법률문서 편집 도구로는 최소 기능
요구출처: 요구 컨셉 '엔터프라이즈 DMS의 핵심인 편집 워크플로' (Vault 내 편집 경험)
현재상태: vault_text 모드는 text/*, json, csv, markdown 한정 + NATIVE_EDIT_MAX_BYTES 1MB 제한이며 UI는 플레인 textarea(서식·구조 없음). 로직 자체는 견고(UTF-8 fatal 디코드, 크기 제한, 기준 해시 검증)하나 실제 법률문서(docx/hwp/pdf)에는 적용 불가한 보조 경로.
증거: apps/api/src/modules/document/document-editing.service.ts:73-79 (NATIVE_EDIT_MAX_BYTES=1_000_000, 텍스트 MIME 한정); apps/web/src/components/document/document-action-center.tsx:1023-1040 (textarea 기반 네이티브 편집기)
개선안: WOPI/Office 통합(상위 critical finding)이 본선이므로 네이티브 편집기는 마크다운 프리뷰 + 조항 아웃라인 수준으로만 보강하고, 리소스는 WOPI 경로에 집중. 단 편집 패키지 UI에 '이 파일 형식은 로컬 편집 후 재업로드' 안내를 유지해 사용자 혼선 방지.

====================================================================================================
[email-vault] maturity=NOT_OPERATIONAL

구현요약:
Email Vault는 두 갈래로 구현되어 있다. (1) 수동 EML 임포트 경로는 실질적이다: POST /matters/:matterId/emails → EmailService.importRawEmail (apps/api/src/modules/email/email.service.ts:389-564)이 원문을 실제 스토리지에 저장(StorageService.putEmailRawObject)하고, email_messages/email_participants/email_document_links/email_matter_filings 스키마(db/migrations/0040~0043)에 기록하며, message_id_hash 유니크 기반 중복 차단(레이스 처리·감사이벤트·스토리지 보상삭제 포함, email.service.ts:438-563), MIME 첨부 자동분리 후 각 첨부를 정식 문서로 업로드하고 email_document_links로 원문 관계 유지(email.service.ts:1138-1205, email-attachment.parser.ts), 첨부 임포트 시 DLP 스캔(실제 정규식 검출기: 주민번호·외국인등록번호·계좌·여권·카드(Luhn)·이메일·전화, dlp/sensitive-data.detector.ts:43-94), from/to/cc 파싱과 도메인 기반 외부참여자 판정(packages/shared/src/email/email-metadata.ts), References/In-Reply-To 해시 기반 쓰레드 요약, 권한(canUploadToMatter/ethical wall) 검사, 실DB 통합테스트(tests/integration/document-access/email-filing.spec.ts, cross-tenant RLS 테스트 포함)까지 갖췄다. 웹에서는 Matter 상세의 이메일 타임라인(components/matter/matter-email-timeline.tsx)과 문서 상세의 관련 이메일 표시가 실제 API에 연결되어 있다. (2) Outlook add-in 경로는 정교한 골격이다: office.js를 로드하는 실제 태스크페인 UI(apps/web/src/app/outlook-addin/outlook-addin-client.tsx), 커밋된 매니페스트+Smart Alerts 런타임(apps/web/public/outlook-addin/manifest.xml), m365/outlook 컨트롤러의 filing/send-policy/send-file/attachment-acquisition/document-insertion/folder-mapping/session-exchange API 8종, 전용 스키마(0070~0074), 링(R0~R3)·증거참조·감사가용성 기반 운영게이트(outlook-operational-gate.ts), 릴리스 검증 도구(tools/release/check-outlook-*.ts/mjs, render-outlook-manifest.mjs)가 있다. 그러나 add-in DTO는 해시 전용(제목해시·참여자도메인해시·첨부ID해시)이라 이메일 본문이 서버에 도달할 수 없고, Graph 첨부 트랜스포트는 항상 거부하는 Disabled 구현이 주입되며(outlook-graph-attachment-transport.ts:38-45, outlook.module.ts:53-55), 신원검증기도 항상 DENY(outlook-identity-verifier.ts:31-36), 기능 플래그는 전부 기본 off이고 EV-OUTLOOK-002/003 증거는 blocked 상태(docs/release/evidence-register.md:40-41)다. queued된 filing request를 실제 이메일 레코드로 이행하는 워커/소비자가 없어(email_record_id는 읽기만 존재) add-in 경로로는 이메일이 저장되지 않는다. EML 업로드 API조차 웹 UI 호출부가 없어(api-client.ts에는 listMatterEmailTimeline만 존재) 최종사용자 관점에서 이메일 캡처 수단이 사실상 부재하다. Gmail add-on과 MSG 파싱은 미구현이며 신뢰도 4단계 자동저장도 없다.


--- (critical/NOT_OPERATIONAL) Outlook add-in으로 이메일을 실제 저장할 수 없음 — 콘텐츠 전달 경로와 이행(fulfillment) 파이프라인 부재
요구출처: 사양서 §6.3 'Outlook add-in 제공, 이메일을 Matter에 저장'
현재상태: add-in은 해시 전용 스냅샷(제목해시, 참여자 도메인해시, 첨부ID해시)만 서버에 보내고 filing request를 status='queued'로 INSERT할 뿐이다. 이 큐를 소비해 실제 메일 원문을 가져와 email_messages로 이행하는 워커·크론·서비스가 리포지토리 어디에도 없고(outlook_filing_requests의 status를 'processing'/'completed'로 갱신하는 코드는 cancel 외에 전무, email_record_id는 읽기만 존재), Graph API 트랜스포트는 항상 'integration_gate_closed'로 거부하는 DisabledOutlookGraphAttachmentTransport가 DI로 고정 주입되어 있다. 즉 add-in의 '파일링' 버튼은 영원히 queued 상태의 요청 레코드만 남긴다.
증거: apps/api/src/modules/outlook/outlook.service.ts:161-186 (queued INSERT만 수행); apps/api/src/modules/outlook/outlook-graph-attachment-transport.ts:38-45 (항상 denied 반환); apps/api/src/modules/outlook/outlook.module.ts:53-55 (Disabled 트랜스포트 고정 바인딩); apps/web/src/lib/outlook-addin/outlook-item.ts:1-80 (해시 전용 DTO, 본문/바이트 미전송); db/migrations/0070_create_outlook_filing_requests.sql:168 (completed 상태는 스키마에만 존재)
검증판정: CONFIRMED — 모든 주장 근거가 현재 워킹트리에서 정확히 확인되었고, 반박 증거 수색(전 리포지토리 grep: apps/api, apps/web, apps/desktop, workers, packages, tools, db, deploy, infra)에서도 이행 파이프라인은 발견되지 않았다.

(1) 인용 정확성 — 전부 일치:
- apps/api/src/modules/outlook/outlook.service.ts:161-186 createFilingRequest는 outlook_filing_requests에 INSERT(스키마 DEFAULT 'queued')와 첨부 해시행 INSERT, 감사로그만 수행. 원문/바이트 처리 없음.
- apps/api/src/modules/outlook/outlook-graph-attachment-transport.ts:38-45 DisabledOutlookGraphAttachmentTransport.acquire는 무조건 { status:'denied', reasonCode:'integration_gate_closed' } 반환.
- apps/api/src/modules/outlook/outlook.module.ts:52-55 OUTLOOK_GRAPH_ATTACHMENT_TRANSPORT를 Disabled 구현에 무조건 바인딩. src 전체에서 이 인터페이스의 다른 구현체는 존재하지 않음(dist/는 동일 코드의
개선안: 1단계: Microsoft Graph 실연동 트랜스포트 구현(MSAL/NAA 토큰 브로커 + GET /me/messages/{id}/$value 및 attachments API, 승인 스코프 매트릭스는 이미 outlook-graph-scopes.ts에 준비됨). 2단계: outlook_filing_requests 큐를 소비하는 백그라운드 워커(BullMQ/pg-boss)를 추가해 queued→processing→completed 상태기계를 구동하고, 취득한 MIME을 기존 EmailService.importRawEmail로 위임해 email_messages·email_document_links·email_matter_filings에 이행. 3단계: 이행 완료 시 email_record_id를 역기입하고 add-in 상태 폴링 UI와 연결. 4단계: 실패/재시도·부분첨부 이행에 대한 사가 보상 로직과 감사이벤트 추가.

--- (critical/NOT_OPERATIONAL) Outlook 통합이 삼중 봉인 상태 — 신원검증기 상시 DENY, 기능 플래그 전부 기본 off, 배포 증거 blocked
요구출처: 사양서 §6.3 'Outlook add-in 제공'
현재상태: add-in 세션 교환의 신원검증기가 DefaultOutlookIdentityVerifier로 고정되어 무조건 DENY를 반환하므로 add-in 세션 수립 자체가 불가능하다. 8개 기능 플래그(OUTLOOK_ADDIN_ENABLED 등)는 env 미설정 시 전부 false이고, 운영게이트는 EVREF 형식의 운영자 승인·매니페스트 검증·롤백 리허설 증거를 요구하는데 evidence-register상 EV-OUTLOOK-002(매니페스트 검증)·003(Graph 동의)은 blocked이다. 즉 실제 M365 테넌트 배포·동의·검증이 한 번도 이뤄지지 않은 계획 단계다. 게이트 설계 자체는 엔터프라이즈급 롤아웃 통제로 잘 만들어져 있으나, 통제 대상인 통합 본체가 없다.
증거: apps/api/src/modules/outlook/outlook-identity-verifier.ts:31-36 (항상 DENY); apps/api/src/modules/outlook/outlook-operational-gate.ts:80-96,231-257 (기본 off 플래그와 증거참조 게이트); docs/release/evidence-register.md:40-41 (EV-OUTLOOK-002/003 blocked); apps/api/src/modules/outlook/outlook-auth.service.ts:86-96 (검증 실패 시 policy_denied)
검증판정: CONFIRMED — 모든 핵심 근거가 현재 워킹트리에서 그대로 검증되었고, 반박 증거는 발견되지 않았다.
개선안: 1단계: Exchange identity token(NAA/SSO) 실검증기 구현 — Microsoft identity platform의 서명검증(JWKS)·aud/tid 검증·mailbox 바인딩 매칭. 2단계: 파일럿 M365 테넌트에서 매니페스트 사이드로드 검증(EV-OUTLOOK-002)과 admin consent(EV-OUTLOOK-003)를 실제 수행해 증거참조 발급. 3단계: R0_ADMIN_ONLY 링부터 플래그를 켜고 준비된 운영게이트·롤백 런북을 그대로 사용해 R3까지 승격. 기존 게이트/감사 설계는 재사용 가치가 높으므로 교체하지 말 것.

--- (high/SHALLOW) Matter 자동추천 신뢰도 4단계(95%+ 자동저장/80-95 확인/50-80 후보/50미만 수동) 미구현 — 고정점수 규칙 매칭만 존재
요구출처: 사양서 §6.3 신뢰도 표 (95% 이상 자동 저장 / 80~95% 확인 후 저장 / 50~80% 후보 제시 / 50% 미만 수동)
현재상태: 추천은 두 곳 모두 이진 규칙 매칭이다: EmailService.suggestMattersForEmail은 제목 SQL LIKE(매터코드/명 포함 여부, +70점)와 참여자 도메인-매터/클라이언트 도메인 일치(+30점), SearchService.suggestMatters(add-in용)는 제목 sha256 해시의 완전일치와 도메인 해시 일치로 동일한 70/30 고정점수를 낸다. 확률 보정된 신뢰도, 4단계 밴드별 처리 분기, 95%+ 자동저장 어디에도 없다. AUTOFILE은 폴더 매핑 기반 '작업 기록'만 남기는 API(recordAutofileJob)로 존재하며 기본 off 플래그 뒤에 있고 실행 엔진이 없다. 과거 파일링 이력·쓰레드 연속성·발신자별 학습 신호도 미사용.
증거: apps/api/src/modules/email/email.service.ts:809-848 (LIKE 기반 70/30 점수); apps/api/src/modules/search/search.service.ts:746-825 (해시 완전일치 70/30 점수); apps/api/src/modules/outlook/outlook-folder-mapping.service.ts:410-455 (autofile은 job 기록만); packages/shared/src/email (신뢰도 밴드 타입 부재)
검증판정: CONFIRMED — 주장 근거 4건 모두 정확함을 코드로 확인. (1) apps/api/src/modules/email/email.service.ts 786-853행 suggestMattersForEmail: 제목 SQL LIKE(matter_code/matter_name 포함, 70점) + 참여자 도메인=matter/client metadata_json domain 일치(30점)의 고정점수 합산이며 가능한 점수는 100/70/30뿐. (2) apps/api/src/modules/search/search.service.ts 746-825행 buildMatterSuggestionQuery: subject sha256 해시 완전일치 + 도메인 해시 일치의 동일 70/30 고정점수. (3) apps/api/src/modules/outlook/outlook-folder-mapping.service.ts 410-451행 recordAutofileJob: outlook_autofile_jobs 테이블 insert + 감사로그만 수행. 이 테이블의 소비자는 리포 전체에서 같은 서비스의 insert/select와 마이그레이션 0074뿐 — 실행 엔진(worker/cron) 부재. AUTOFILE은 outlook-operational-gate.ts에서 OUTLOOK_AUTOFILE_ENABLED='true' + ring/evidence 조건이 모두 충족돼야 열리는 기본
개선안: 1단계: 신호 확장 — conversationId/References 기반 '같은 쓰레드가 이미 파일링된 Matter'(최강 신호), 발신자 주소해시별 과거 파일링 분포, 참여자 도메인·제목 토큰을 피처로 하는 스코어러를 EmailService에 도입. 2단계: 로지스틱 캘리브레이션으로 0~100 신뢰도를 산출하고 shared DTO에 confidenceBand('auto_file'|'confirm'|'candidate'|'manual') 필드 추가. 3단계: 95%+ 밴드는 filing을 자동 생성하되 감사이벤트+사용자 알림/되돌리기 UI를 붙이고, add-in과 웹 타임라인 UI에서 밴드별 UX 분기. 4단계: 사용자 수정(추천 거부/변경)을 학습 피드백 테이블로 적재해 주기 재보정.

--- (high/MISSING) Gmail add-on 완전 부재
요구출처: 사양서 §6.3 'Outlook add-in 및 Gmail add-on 제공'
현재상태: 리포지토리 전체(apps/api, apps/web, packages, workers, db, tools)에서 Gmail/Google Workspace add-on 관련 코드·스키마·매니페스트가 전무하다 (node_modules 부산물 제외 grep 0건).
증거: 리포지토리 전역 grep 'gmail' 결과: apps/web/.next 빌드 산출물의 서드파티 package.json만 검출, 소스 0건
검증판정: CONFIRMED — 갭 주장이 정확하다. (1) 주장 근거 재현: 전역 grep 'gmail'은 apps/web/.next/standalone 하위 서드파티 package.json(저자 이메일 수준)만 검출, 소스 0건 — 인용 정확. (2) apps/api/src/modules 전체(email, outlook, integrations 포함), apps/web, packages, workers, db/migrations, tools에서 gmail/google workspace/apps script/CardService 패턴 grep 0건, *gmail* 파일명·appsscript.json·*.gs 매니페스트 0건. (3) apps/web/src/app/(app)/integrations/에는 matter-app과 outlook만 존재하고, db/migrations에도 Outlook 게이트(0072)와 provider-중립 email_messages(0040/0041) 스키마만 있을 뿐 Gmail 관련 스키마 없음. Gmail은 docs/package/03_Pyramid_PBS_DBS_Structure.md:155("Gmail Integration | Add-on / Filing UI")와 04_System_Architecture_Blueprint.md:11("Outlook/Gmail add-in") 등 설계 문서에 계획으로만 등장. 유일한 완화 요소는 e
개선안: Outlook fulfillment 파이프라인 완성 후 착수. Google Workspace Add-on(Apps Script CardService 또는 alternate runtime) + Gmail API(users.messages.get format=RAW)로 RFC822 원문을 취득해 기존 importRawEmail 경로를 재사용하는 것이 최소 비용 경로다. Outlook의 filing request/운영게이트/감사 모델을 sourceClient='gmail-addon'으로 일반화하고, OAuth 동의·도메인 위임 증거 게이트를 동일 패턴으로 복제.

--- (high/STUB) MSG 파싱 미구현 — 의도적 연기 상태로 원문 보존만 수행
요구출처: 사양서 §6.3 'MSG/EML 파싱'
현재상태: TS 서비스와 Python 워커 양쪽 모두 .msg는 parser='msg', parse_status='pending_unsupported', failure_reason_code='UNSUPPORTED_MSG'로 기록하고 메타데이터·첨부·참여자 추출 없이 원문 바이트만 보존한다. 한국 로펌 환경에서 지배적인 Outlook .msg 반출 파일이 검색·추천·쓰레딩·첨부분리에서 전부 배제된다.
증거: apps/api/src/modules/email/email.service.ts:575-585 (msg → pending_unsupported); workers/ingestion/app/parsers/email.py:152-158 (parse_msg_skeleton 주석: intentionally deferred)
검증판정: CONFIRMED — 주장 근거 인용이 두 파일 모두 정확하다. apps/api/src/modules/email/email.service.ts:575-585는 .msg 확장자를 parser='msg', parseStatus='pending_unsupported', failureReasonCode='UNSUPPORTED_MSG', metadata=null, attachments=[]로 기록하고, workers/ingestion/app/parsers/email.py:152-158의 parse_msg_skeleton은 "MSG parsing is intentionally deferred" 주석과 함께 빈 스켈레톤만 반환한다(이 함수는 워커 프로덕션 코드 어디에서도 호출되지 않고 테스트에서만 참조됨 — 주장보다 오히려 더 미완성). 리포지토리 전체(apps/api, apps/web, workers, packages, db/migrations, tools)를 수색해도 MSG 파싱 라이브러리(extract-msg, msg-reader, olefile, CFBF/OLE2 등)가 의존성·코드 어디에도 없다. 워커 pyproject.toml 의존성은 fastapi/pypdf/python-docx/python-multipart/uvicorn뿐이다. 공유 타입(email-types.ts)과 마이그레이션(0040_create_email_messages.sql)은 pending
개선안: 워커(Python)에 extract-msg 또는 compoundfiles+red-black CFB 파서를 도입해 MSG→정규화 메타데이터+첨부 추출을 구현하고, API가 pending_unsupported 레코드를 워커에 재처리 위임하는 배치 재파싱 잡을 추가(파서 버전 컬럼으로 재처리 추적). MSG 내장 첨부(msg 안의 msg) 재귀 처리와 RTF 본문 디코딩(RTFDE)까지 포함해야 실사용 수준이 된다.

--- (high/NOT_OPERATIONAL) EML 업로드·이메일 파일링 웹 UI 부재 — 이메일 캡처 기능 전체가 최종사용자에게 미노출
요구출처: 사양서 §6.3 '이메일을 Matter에 저장' (운영 가능성)
현재상태: API에는 업로드(POST /matters/:id/emails)·파일링(POST /emails/:id/file)·추천(GET /emails/:id/matter-suggestions) 엔드포인트가 있으나, 웹 클라이언트에는 호출 코드가 전혀 없다(api-client.ts에는 listMatterEmailTimeline 읽기 전용만 존재, 웹 소스에서 .eml/.msg accept 0건). /files 업로드 패널도 이메일 경로를 지원하지 않는다. Outlook add-in도 봉인 상태이므로, 사용자가 이메일을 Vault에 넣을 수 있는 실제 수단이 현재 하나도 없다 — 타임라인 UI는 읽을 데이터가 생길 수 없는 읽기 화면이다.
증거: apps/web/src/lib/api-client.ts:214 (email 관련 유일한 함수 listMatterEmailTimeline); apps/api/src/modules/email/email.controller.ts:75-124 (UI 미연결 4개 라우트); 웹 소스 grep '.eml|.msg|message/rfc822' 0건 (테스트 제외); apps/web/src/app/(app)/matters/[matterId]/page.tsx:138 (읽기 전용 타임라인만 렌더)
검증판정: CONFIRMED — 주장 근거 4건 모두 정확함을 확인했다. (1) apps/web/src/lib/api-client.ts:213-214의 listMatterEmailTimeline이 email 모듈 관련 유일한 웹 호출이며, POST /matters/:id/emails, POST /emails/:id/file, GET /emails/:id/matter-suggestions에 대한 웹 호출 코드는 리포지토리 전체(미커밋 워킹트리 포함)에서 0건이다. (2) apps/api/src/modules/email/email.controller.ts의 4개 라우트와 email.service.ts(EML/MSG 파싱, 마이그레이션 0040-0043, 통합테스트)는 서버측으로 완성돼 있으나 UI 미연결이 맞다. (3) 웹 소스에서 .eml/.msg/message/rfc822 accept 0건 확인. (4) matters/[matterId]/page.tsx:138은 읽기 전용 MatterEmailTimeline만 렌더한다. 반박 시도: (a) Outlook add-in 웹 UI(apps/web/src/app/outlook-addin/)가 POST /m365/outlook/filing-requests로 이메일 파일링을 시도하는 완성된 화면이지만, outlook-operational-gate.ts가 기본 OUTLOOK_ADDIN_ENABLED=false fail-close
개선안: 단기(1스프린트): Matter 상세와 /files에 EML/MSG 드래그앤드롭 업로드 카드를 추가하고 uploadRawEmailToMatter를 호출, 업로드 직후 matter-suggestions를 표시해 파일링 확인 플로우 완성. 중기: 업로드 시 tenantDomains를 서버측 테넌트 설정에서 자동 주입(현재 클라이언트 필드 의존)하고, 중복(EmailDuplicateMessageError→VALIDATION_FAILED) 시 기존 이메일로 안내하는 UX 추가.

--- (high/SHALLOW) 외부 송부 전 DLP 검사가 콘텐츠 무검사 — 해시 기반 경고 3종뿐이며 그마저 게이트 봉인
요구출처: 사양서 §6.3 '외부 송부 전 DLP 검사'
현재상태: Outlook Smart Alerts 송신정책(evaluateSendPolicy)은 no_matter/wrong_matter/external_recipient 3개 경고코드와 승인(acknowledge) 플로우만 구현했다. add-in이 해시만 전송하므로 서버는 본문·첨부 내용을 볼 수 없어 주민번호·계좌 등 민감정보 콘텐츠 검사가 원천적으로 불가능하다. 실제 콘텐츠 DLP(SensitiveDataDetector)는 존재하지만 '수신' 이메일 첨부 임포트 시에만 실행된다(인그레스 DLP). 송신 경로 전체가 SEND_FILE/SMART_ALERTS 플래그 뒤에 봉인되어 있기도 하다.
증거: apps/api/src/modules/outlook/outlook-send-file.service.ts:328-363 (경고 3종만 평가, DLP 호출 없음); apps/api/src/modules/email/email.service.ts:1207-1224 (DLP는 임포트 시점에만); apps/api/src/modules/dlp/sensitive-data.detector.ts:43-94 (검출기는 실구현); apps/web/public/outlook-addin/smart-alerts.js (OnMessageSend 런타임 존재)
검증판정: CONFIRMED — 모든 인용이 정확함을 직접 확인했다. (1) outlook-send-file.service.ts:328-363의 evaluatePolicyInternal은 no_matter/wrong_matter/external_recipient 3개 경고코드와 permission_denied 차단만 평가하며 DLP 호출이 전혀 없다(outlook 모듈 전체 grep에서 dlp 참조 0건). createSendFileRequest는 acknowledgedWarningCodes 승인 플로우 그대로이며 send_and_file 요청 행만 기록한다. (2) packages/shared/src/outlook/outlook-types.ts:122-185의 evaluateOutlookSendPolicySchema는 64-hex 해시 필드만 허용(.strict())하고 본문/제목 평문 필드가 없어 서버측 콘텐츠 검사가 구조적으로 불가능하다. (3) apps/web/public/outlook-addin/smart-alerts.js는 클라이언트에서 모두 SHA-256 해시화 후 전송하며 클라이언트측 콘텐츠 스캔도 없고, 오류 시 completeAllow로 fail-open(228-230행)이라 주장보다 오히려 더 약하다. (4) DlpService 호출처는 저장소 전체에서 3곳뿐: email.service.ts:1216(수신 첨부 임포트 인그레스), external
개선안: 아키텍처 선택 필요: (a) add-in 클라이언트측 DLP — SensitiveDataDetector 규칙을 shared 패키지로 옮겨 Smart Alerts OnMessageSend 핸들러에서 item.body.getAsync로 본문·첨부를 로컬 스캔하고 결과 해시+판정만 서버에 보고(제로 콘텐츠 유출 원칙 유지), 또는 (b) Graph 연동 완성 후 서버측 스캔 — send-and-file 이행 워커가 원문 취득 시 DlpService.scanAndRecord(sourceType='email_egress')를 실행하고 block 판정 시 송신 차단. (a)를 1차로, (b)를 이행 파이프라인과 함께 2차로 구현하고 dlp_findings에 egress 소스타입을 추가.

--- (medium/SHALLOW) 참여자 분류가 내부/외부 이진 판정 — 고객/상대방/내부자 3분류 미구현
요구출처: 사양서 §6.3 '발신자·수신자·참조자 자동 인식, 고객/상대방/내부자 분류'
현재상태: from/to/cc 역할 인식과 주소 정규화는 구현됐으나 분류는 isOutside(테넌트 도메인 포함 여부) 단일 불리언이다. 게다가 tenantDomains는 업로드 요청 필드로 클라이언트가 제공해야 하며 미제공 시 전원 내부자로 처리된다(tenantDomains.size===0이면 isOutside 항상 false). Matter의 parties 테이블(고객/상대방 taxonomy 존재, migration 0020-0022)과의 대조가 전혀 없어 '고객' vs '상대방' 구분이 불가능하다.
증거: packages/shared/src/email/email-metadata.ts:94 (isOutside 이진 판정); apps/api/src/modules/email/email.service.ts:413 (fields.tenantDomains ?? [] — 클라이언트 의존); db/migrations/0020_create_parties.sql, 0021_party_taxonomy_check.sql (미사용 party taxonomy); email_participants 스키마(0041)에 분류 컬럼 부재
개선안: 1단계: 테넌트 도메인을 서버측 테넌트 설정 테이블로 이전해 요청 필드 의존 제거. 2단계: 파일링 대상 Matter의 parties(및 client 도메인/연락처)와 참여자 주소·도메인을 매칭해 participant_class('internal'|'client'|'opposing'|'other_external') 컬럼을 email_participants에 추가. 3단계: 이 분류를 추천 신뢰도(상대방 도메인 매칭은 wrong_matter 신호), 특권 태그 제안(상대방 수신 시 privilege waiver 경고), 송신 DLP(상대방에게 특권문서 송부 차단)에 연결.

--- (medium/SHALLOW) 쓰레드 단위 관리가 카운트/해시 요약 수준 — 쓰레드 뷰·쓰레드 단위 파일링 부재
요구출처: 사양서 §6.3 '이메일을 Matter에 저장하고 쓰레드 단위로 관리'
현재상태: References/In-Reply-To를 해시로 정규화해 저장하고 SQL 서브쿼리로 관련 이메일 수(thread_related_count)와 참조해시 10개를 DTO에 담는 것이 전부다. UI는 '관련 이메일 N건' 텍스트만 표시하고 쓰레드 그루핑 뷰, 쓰레드 전체 일괄 파일링, 새 회신 자동 동일-Matter 파일링(쓰레드 연속성), 대화 타임라인이 없다. Outlook 측 conversationIdHash는 저장만 되고 활용처가 없다.
증거: apps/api/src/modules/email/email.service.ts:281-289 (threadSummary — 카운트와 해시 슬라이스); apps/api/src/modules/email/email.service.ts:869-936 (필링 목록 쿼리, 그루핑 없음); apps/web/src/components/matter/matter-email-timeline.tsx:27-29 ('관련 이메일 N건' 표시뿐)
개선안: 1단계: email_threads 테이블(루트 메시지해시/conversation_id_hash 기준) 도입과 임포트 시 쓰레드 배정(유니온파인드식 참조 병합). 2단계: Matter 타임라인을 쓰레드 그루핑 아코디언으로 재구성하고 쓰레드 단위 일괄 파일링/특권태그 API 추가. 3단계: 신규 이메일이 기존 파일링된 쓰레드에 속하면 해당 Matter를 최고 신뢰도 추천(자동저장 밴드)으로 승격 — 이것이 iManage 수준 '쓰레드 연속성 자동파일링'의 핵심.

--- (medium/QUALITY) RFC2047 인코딩 워드 미디코딩 — 한국어 제목이 깨진 채 저장되어 제목 기반 추천·표시 무력화
요구출처: 사양서 §6.3 'MSG/EML 파싱' + Matter 자동 추천 (품질 전제)
현재상태: packages/shared의 eml-parser(71줄)와 email-metadata에 =?UTF-8?B?...?= / =?EUC-KR?B?...?= 인코딩 워드 디코딩이 없어, 한국어 제목·표시명이 원시 인코딩 문자열 그대로 subject에 저장된다. 결과적으로 타임라인 UI에 깨진 제목이 표시되고, 제목 LIKE/해시 매칭 기반 Matter 추천과 특권 키워드 휴리스틱(privilegeTagSuggestion)이 한국어 메일에서 전혀 동작하지 않는다. 본문도 buffer.toString('utf8') 고정이라 EUC-KR 메일은 손상된다. 참고로 Python 워커 파서(policy.default)는 올바르게 디코딩하지만 파이프라인에 연결되지 않은 데드코드다.
증거: packages/shared/src/email/eml-parser.ts (71줄, encoded-word 처리 부재); packages/shared/src/email/email-metadata.ts:136-156 (subject 원시값 사용); apps/api/src/modules/email/email.service.ts:588 (body.toString('utf8') 고정); workers/ingestion/app/parsers/email.py:122-149 (올바른 파서가 미연결 상태)
개선안: 단기: shared eml-parser에 RFC2047 디코더(B/Q 인코딩, UTF-8/EUC-KR/CP949 iconv 변환)를 추가하고 charset 파라미터 기반 본문 디코딩 적용. 중장기: 파싱 책임을 Python 워커(email.parser + extract-msg)로 일원화하고 API는 원문 보존+워커 콜백으로 메타데이터를 갱신하는 비동기 파이프라인으로 전환 — 현재 데드코드인 workers/ingestion/app/parsers/email.py를 그 진입점으로 활성화.

--- (medium/QUALITY) 첨부 파서가 단층 multipart만 처리 — 중첩 multipart 내 첨부 누락, quoted-printable 미지원
요구출처: 사양서 §6.3 '첨부파일 자동 분리 및 원문 이메일과의 관계 유지' (품질)
현재상태: extractEmlAttachments는 최상위 boundary 한 단계만 분리한다. multipart/mixed 안의 multipart/related·alternative에 중첩된 첨부(실무 메일의 흔한 구조, 특히 인라인 이미지+첨부 혼재)는 탐지되지 않는다. content-transfer-encoding은 base64 외에는 utf8 원문 취급이라 quoted-printable 첨부는 손상 저장된다. message/rfc822 내장 이메일(포워드 첨부) 재귀 처리도 없다.
증거: apps/api/src/modules/email/email-attachment.parser.ts:83-91 (단층 partSections); apps/api/src/modules/email/email-attachment.parser.ts:75-81 (base64 외 인코딩 미처리); apps/api/src/modules/email/email-attachment.parser.spec.ts (40줄 — 중첩/QP 케이스 미검증)
개선안: 재귀 MIME 트리 파서로 교체(중첩 boundary 재귀 순회, QP 디코딩, message/rfc822 내장메일을 별도 이메일로 재귀 임포트+쓰레드 연결). 앞 항목과 동일하게 Python 워커의 iter_attachments 기반 구현으로 일원화하는 것이 가장 경제적이다. 대용량 첨부는 스트리밍 처리와 25MB 상한 정책 재검토 포함.

--- (medium/NOT_OPERATIONAL) 인제스천 워커에 이메일 비동기 파이프라인 부재 — 워커 이메일 파서는 데드코드
요구출처: 사양서 §6.3 전반 (대량 이메일 처리 아키텍처) + §9 확장성(이메일 수천만 건)
현재상태: workers/ingestion에는 parse_eml_envelope/parse_msg_skeleton이 있으나 main.py는 convert/extract 라우터만 등록하고 어디서도 email 파서를 호출하지 않는다(테스트 파일만 참조). 이메일 파싱·첨부분리·DLP는 전부 API 요청 스레드에서 동기 처리되므로 PST/mailbox 대량 마이그레이션(사양서 §632: 이메일 PST/Outlook mailbox 소스)이나 수천만 건 규모 처리가 불가능한 구조다.
증거: workers/ingestion/app/main.py:6-11 (convert/extract 라우터만 등록); workers/ingestion/app/parsers/email.py (테스트 외 참조 0건); apps/api/src/modules/email/email.service.ts:425-564 (요청 동기 처리)
개선안: 워커에 /email/parse 엔드포인트(또는 큐 컨슈머)를 추가해 파싱·첨부분리를 오프로드하고, API는 원문 저장+parse_status='pending' 기록 후 워커 완료 콜백으로 메타데이터 갱신하는 2단계 커밋으로 전환. 이어서 PST 벌크 임포터(libpff 기반)를 워커에 추가하면 사양서의 레거시 메일박스 마이그레이션 요구까지 커버된다.

--- (low/NOT_OPERATIONAL) 이메일-문서 링크 조회 API 미노출 — 서비스 메서드가 컨트롤러에 연결되지 않음
요구출처: 사양서 §6.3 '첨부파일 자동 분리 및 원문 이메일과의 관계 유지' (양방향 탐색)
현재상태: EmailService.listDocumentLinksForEmail/listEmailLinksForDocument는 권한 필터링까지 갖춘 완성 로직이지만 email.controller.ts의 4개 라우트에 포함되지 않아 호출 경로가 없다. 문서 상세의 '관련 이메일'은 Matter 전체 타임라인을 받아 클라이언트에서 documentIds로 필터링하는 우회 방식이라 이메일이 많은 Matter에서 비효율적이고, 문서→원문 이메일 직접 내비게이션(원문 다운로드 포함)이 없다.
증거: apps/api/src/modules/email/email.service.ts:939-989 (미노출 서비스 메서드 2개); apps/api/src/modules/email/email.controller.ts:26-31 (라우트 4개에 링크 조회 없음); apps/web/src/components/document/document-action-center.tsx:1799-1801 (타임라인 전체 조회 후 클라이언트 필터)
개선안: GET /documents/:id/email-links, GET /emails/:id/document-links 라우트를 노출하고 문서 상세의 관련 이메일 섹션을 직접 조회로 교체. 이메일 원문(.eml) 통제 다운로드 엔드포인트(감사이벤트+다운로드 사유 코드, 기존 문서 다운로드 패턴 재사용)를 추가해 원문 관계 유지 요구를 완결.

====================================================================================================
[search-retrieval] maturity=OPERATIONAL

구현요약:
핵심 키워드+메타데이터+권한내 검색은 실운영 가능한 수준으로 구현되어 있다. (1) 인덱스: PostgreSQL FTS 기반 `document_search_index` 테이블(tsvector GENERATED 컬럼 + GIN 인덱스, RLS 강제) — db/migrations/0036_create_document_search_index.sql. (2) 비동기 인덱싱 파이프라인: pg-boss 큐(retry 5회, backoff, dead-letter 큐, singletonKey 중복방지) — apps/api/src/modules/search/index/indexing.service.ts, 문서 업로드/버전추가/편집/추출 라이프사이클에 SearchIndexSyncHook로 실제 연결됨(apps/api/src/modules/document/document.service.ts, document-version.service.ts, document-editing.service.ts, extraction-dispatcher.ts). 관리자 reindex API와 search health 엔드포인트 존재(index/reindex.controller.ts). (3) 권한내 검색이 가장 깊다: matter/document/ethical-wall 3중 SQL 스코프 필터(ACL explicit DENY, 조건 fail-closed, limited_reviewer explicit allow, insider/excluded 윤리장벽 + break-glass 2인 승인 오버라이드) — apps/api/src/modules/search/permission/{matter,document,wall}-scope.filter.ts, deny-all 기본 프로바이더, 거부 검색까지 감사로그(query는 sha256 해시로만 기록). (4) 메타/버전 검색: matterId/clientId/문서유형/비밀등급/privilege/legal hold/records status/날짜/버전상태(current·superseded·all) 필터와 10종 facet — query/search-filter.builder.ts, search-query.builder.ts. (5) 웹 UI /search는 실제 API(POST /search)에 연결된 가시 라우트로 저장검색(personal/matter-team/admin-shared), URL 프라이버시 모드, facet, 하이라이트 스니펫 포함 — apps/web/src/app/(app)/search/search-client.tsx(729줄). (6) 통합테스트 17개 파일 3,734줄이 실제 DB로 권한/월/시맨틱/인덱스 경로를 검증 — tests/integration/search-permission/. (7) pgvector 스키마(document_chunks parent-child, document_chunk_embeddings)와 semantic/hybrid 쿼리 빌더, AI 자연어 질의용 retrieval orchestrator(question classifier + hybrid chunk retrieval)도 존재 — 0049 마이그레이션, semantic/document-chunker.ts, ai/retrieval/retrieval-orchestrator.service.ts. 그러나 '의미검색'의 실체는 16차원 결정론적 해시 벡터이고, OpenSearch/Elastic·실임베딩모델·OCR엔진·이메일 본문검색·판례법령검색은 부재하며, 조항검색 UI(/contracts)와 자연어 질의는 사용자에게 노출되지 않는다.


--- (critical/SHALLOW) 의미검색(벡터)이 실제 임베딩 모델이 아닌 16차원 해시 벡터 — 의미 유사도가 아니라 토큰 중복 측정
요구출처: 사양서 §6.5 의미검색, §11.2 Vector DB, §13 의미검색 5초
현재상태: pgvector 스키마와 semantic/hybrid 쿼리 빌더는 존재하지만, 임베딩이 sha256 토큰 해시를 16차원으로 접은 결정론적 벡터다. 마이그레이션 주석 스스로 'local deterministic vector used for R6 retrieval tests'라고 명시한다. packages/ai는 Ollama /api/generate(텍스트 생성)만 호출하고 임베딩 API 호출 코드가 전혀 없다. 동의어·패러프레이즈·한영 교차 의미 매칭이 원리적으로 불가능하며, 사실상 조악한 lexical overlap 점수다. DEC-11로 외부 임베딩 모델이 의도적으로 차단된 상태다. 또한 embedding 컬럼에 HNSW/IVFFlat ANN 인덱스가 없어 전 청크 순차 스캔이다.
증거: apps/api/src/modules/search/semantic/local-embedding.ts:6-23 (deterministicEmbeddingVector: sha256 토큰 해시 → 16차원); db/migrations/0049_create_document_chunks_and_embeddings.sql (embedding vector(16), COMMENT '16 dimension local deterministic vector used for R6 retrieval tests and local-only semantic search'); packages/ai/src/index.ts:146-200 (Ollama /api/generate만 존재, embed 엔드포인트 호출 없음); apps/api/src/modules/search/index/search-index.repository.ts:317 (인덱싱 시에도 동일 해시 벡터 저장); db/migrations 전체에 hnsw/ivfflat 인덱스 없음 (grep 0건); docs/ledger/decision.md:15 (DEC-11: local_gemma만 허용, 외부 모델 route 차단)
검증판정: CONFIRMED — 주장 근거를 전부 원문 대조했고 반박 증거 수색(전체 리포지토리)에서도 실제 임베딩 구현이 발견되지 않았다.

(1) 인용 정확성 — 모두 정확:
- /Users/jws/Projects/amic-vault/apps/api/src/modules/search/semantic/local-embedding.ts:6-23: deterministicEmbeddingVector가 토큰별 sha256 digest의 상위 16바이트를 부호(+/-) 붙여 16차원으로 누적 후 정규화. 동일 토큰이 겹칠수록 코사인 유사도가 오르는 구조로, 의미 유사도가 아닌 (해시 충돌 노이즈 섞인) 토큰 중복 측정이 맞다.
- packages/shared/src/ai/chunk.ts:5: `export const aiEmbeddingDimension = 16 as const;`
- db/migrations/0049_create_document_chunks_and_embeddings.sql:80: `embedding vector(16) NOT NULL`, COMMENT(134행): '16 dimension local deterministic vector used for R6 retrieval tests and local-only semantic search.' 인용 그대로 존재. 테이블 COMMENT(131행)도 'local deterministic embeddings
개선안: 1) 임베딩 서비스 도입: 로컬 우선 정책 유지 시 Ollama /api/embed 또는 자체 호스팅 multilingual-e5-large/BGE-M3(한국어 강함, 1024차원)로 packages/ai에 embedText() 추가. 2) 마이그레이션: document_chunk_embeddings에 model_route별 차원 분리 테이블(또는 vector(1024) 컬럼 신설) + HNSW 인덱스(m=16, ef_construction=64), model_route CHECK 제약 완화. 3) 인덱싱 파이프라인: 기존 pg-boss ai-prep 큐에서 실제 임베딩 생성으로 교체하고 stale 플래그 기반 백필 잡 추가. 4) 쿼리 경로: search-query.builder.buildVector의 vectorParam을 실임베딩으로 교체(코드 구조는 이미 준비됨). 5) 스케일 단계에서 Qdrant/pgvector 파티셔닝(tenant별 파티션)으로 tenant별 벡터 인덱스 분리라는 §13 요구 충족.

--- (high/NOT_OPERATIONAL) semantic/hybrid 검색 모드가 웹 UI에 전혀 노출되지 않음 — 사용자는 keyword 모드만 사용 가능
요구출처: 사양서 §6.5 의미검색·통합 제공, §5 '키워드+메타데이터+의미검색+그래프검색+AI 질의'
현재상태: API는 mode: keyword|semantic|hybrid를 받고(searchQuerySchema) 서버 로직·통합테스트까지 있으나, 웹 검색 화면의 requestForState()는 mode를 설정하지 않아 항상 기본값 keyword로 전송된다. 웹 코드 전체에서 'semantic|hybrid' 문자열은 result-card 테스트 파일 1곳 외 사용처가 없다. 즉 의미검색은 테스트에서만 도달 가능한 합성 경로다.
증거: packages/shared/src/search/search-query.dto.ts:29-30 (searchModes = ['keyword','semantic','hybrid']); apps/web/src/app/(app)/search/search-client.tsx (requestForState에 mode 필드 없음, 'mode:' grep 0건); grep -rl 'semantic|hybrid' apps/web/src → components/search/result-card.test.tsx 1건뿐; tests/integration/search-permission/sear…18929 tokens truncated…점을 병행.

--- (medium/MISSING) PDF §7.1 Gemma 구조화 역할 중 Fact/Issue/Risk/그래프 후보 생성 미구현
요구출처: Obsidian 컨셉 §7.1 (Gemma가 조항구조·정의어·회의록 쟁점·Fact/Issue/Risk/RFI 후보·그래프 노드/관계 후보 생성)
현재상태: Gemma prep은 document_profile·key_fields·date_facts·people_organizations·keyword_tags·filing_suggestions·source_outline·retrieval_hints 8종으로 문서분류·당사자·날짜·키워드 추출은 실제 수행한다(§7.1의 1~3번 충족). 그러나 조항구조/정의어 추출은 Gemma가 아닌 결정적 파서(contract-parser.ts)만 있고, Fact/Issue/Risk/RFI 후보 생성과 Neo4j류 그래프 노드·관계 후보 생성(candidate→확정 승인 플로우)은 코드 흔적이 없다. 그래프(0053 knowledge_graph)는 결정적 조인 기반 사실만 저장.
증거: tools/evalset/local-ai-eval.ts:17-26 (aiPrepArtifactKinds 8종 — fact/issue/risk 후보 없음); apps/api/src/modules/contract-intel/contract-parser.ts (결정적 파서); db/migrations/0053_create_knowledge_graph.sql (후보/승인 상태 컬럼 없는 확정 사실 그래프)
개선안: 1) ai-prep 아티팩트 kind에 fact_candidates/issue_candidates/risk_candidates를 추가하고 기존 grounded JSON 스키마(claims.kind에 이미 'issue','risk','timeline' 존재)를 재사용해 청크 인용 강제 생성. 2) knowledge_graph에 status(candidate/confirmed)·created_by(model/human)·confidence 컬럼을 추가하는 마이그레이션 후, Gemma 추출 people_organizations/date_facts를 후보 엣지로 적재. 3) 검토자 승인 UI(work-queue에 승인 태스크 타입 추가)로 candidate→confirmed 전환 — 컨셉 문서의 '모델 생성 관계는 확정 금지' 원칙과 정합.

====================================================================================================
[knowledge-graph] maturity=NOT_OPERATIONAL

구현요약:
실제 구현된 것: (1) Postgres 파생 지식그래프 — graph_nodes/graph_edges/graph_sync_runs 테이블(db/migrations/0053_create_knowledge_graph.sql)에 RLS·멱등 업서트·stale 마킹·감사로그(GRAPH_SYNCED/GRAPH_QUERY_EXECUTED/GRAPH_CONSISTENCY_CHECKED)를 갖춘 진짜 동기화/조회/일관성 로직이 있다(apps/api/src/modules/graph/graph-sync.service.ts, graph-query.service.ts, graph-consistency.service.ts). Neo4j가 아닌 것은 문서화된 의도적 결정이다(docs/architecture/R7_knowledge_graph.md:12 "No Neo4j driver... PostgreSQL/RLS-backed derived graph tables"). (2) 그래프 조회는 permission-before-graph 계약(canReadMatter → 검색 권한 SQL scope 주입 후 엣지 조인)을 실제로 구현했고 AI Evidence Pack에 graphFacts로 주입된다(apps/api/src/modules/ai/features/ai-summary.service.ts:109-127, generation/evidence-prompt.compiler.ts:61-116) — GraphRAG-lite의 실코드 경로. (3) 계약 인텔리전스 — contract_classifications/contract_clauses/contract_defined_terms(conflict_status 포함)/contract_redline_changes/playbook_rules 마이그레이션(0054)과 정규식 기반 결정론적 파서·분류기·룰엔진(contract-intel 모듈 1,032줄), 조항은행 조회 API(listClauseBank). (4) Fact Ledger — litigation_facts(status draft/verified/disputed/withdrawn + citation_refs, 0057), litigation_evidence_items, litigation_issue_nodes, dd_rfis/dd_issues/dd_risks(0056)와 대응 서비스(litigation.service.ts 899줄, dd.service.ts 1,019줄). (5) Citation 강제 — 로컬 Gemma(Ollama, http://127.0.0.1:11434) 생성 시 JSON 스키마로 claim.source_refs를 evidence pack 허용 ref enum으로 강제하고(local-gemma-generation.service.ts:279-346) grounded-output.guard로 검증, citation-mapper가 권한검사 후 chunk를 실제 문서로 해석하며 AI_CITED_DOCUMENT 감사기록을 남긴다. (6) pgvector 실사용 — search-query.builder.ts:580의 코사인 연산(<=>). 그러나: 그래프 노드 택소노미는 7종(issue/risk는 reserved 미사용)·엣지 7종(HAS_* + RELATED_TO)뿐이고, 임베딩은 16차원 SHA256 해시 벡터(합성)이며, 그래프에는 웹 UI가 전혀 없고(apps/web/src/lib/api에 graph 클라이언트 부재), /contracts·/dd·/litigation 라우트는 RouteBlockedState로 봉인되어 있다(hidden-routes.test.tsx). 동기화는 firm_admin 수동 POST /graph/sync뿐이다. 최근 커밋의 LawOS canonical reflection runner(tools/migration/lawos-canonical-matter-reflection.mjs)는 Matter 코드 정합성 반영 도구로 지식그래프 자체와는 무관하다.


--- (critical/MISSING) 그래프 노드/엣지 택소노미가 컨셉의 21종 노드 중 7종(실사용 5종)만 존재 — Fact/Party/Authority/WorkProduct/Citation 등 핵심 노드와 SUPERSEDES/AMENDS/DEFINES/EVIDENCED_BY/CITES 엣지 전부 부재
요구출처: Obsidian+LLM 컨셉(concept-obsidian-llm.txt:71-112 노드/엣지 목록) + 사양서 §6.4 (고객·사건·문서·조항·법률쟁점·판례·담당자·이메일·AI세션 노드)
현재상태: graph_nodes.node_type CHECK는 client/matter/document/version/clause/issue/risk 7종만 허용하고, issue/risk의 source_table은 'reserved_issue'/'reserved_risk'로 예약만 된 채 코드 어디에서도 채워지지 않는다. 엣지는 HAS_MATTER/HAS_DOCUMENT/HAS_VERSION/HAS_CLAUSE/HAS_ISSUE/HAS_RISK/RELATED_TO 7종뿐. Party(parties, 0020), DefinedTerm(contract_defined_terms), Fact(litigation_facts), RFI(dd_rfis), Issue(dd_issues/litigation_issue_nodes) 등이 관계테이블로는 존재하지만 그래프에 투영되지 않는다. Person/Entity/Authority/WorkProduct/Citation/TimelineEvent/Obligation/Approval/NegotiationPosition은 테이블 자체가 없다(마이그레이션 102개 전수 확인).
증거: db/migrations/0053_create_knowledge_graph.sql:118-131 (node_type/source_table CHECK, reserved_issue/reserved_risk); db/migrations/0053_create_knowledge_graph.sql:192-201 (edge_type 7종); apps/api/src/modules/graph/graph-sync.service.ts:98-159 (client/matter/document/version/chunk만 동기화); db/migrations/0057_create_litigation_vault.sql:165 (litigation_facts는 그래프 미연결); db/migrations/0056_create_dd_vault.sql:82-306 (dd_rfis/dd_issues/dd_risks 그래프 미연결)
검증판정: CONFIRMED — 모든 주장 근거를 원문 대조 및 전수 수색으로 검증했으며 반박 증거를 찾지 못했다.

(1) 인용 정확성: db/migrations/0053_create_knowledge_graph.sql 118-120행의 node_type CHECK는 정확히 'client','matter','document','version','clause','issue','risk' 7종만 허용하고, 121-131행의 source_table CHECK에 'reserved_issue'/'reserved_risk'가 예약되어 있다. 192-202행의 edge_type CHECK는 HAS_MATTER/HAS_DOCUMENT/HAS_VERSION/HAS_CLAUSE/HAS_ISSUE/HAS_RISK/RELATED_TO 7종뿐이다. 0054~0097까지 이후 마이그레이션 중 graph_nodes/graph_edges를 ALTER하는 것은 전무하다(grep 결과 0건).

(2) 실사용 5종 확인: 리포지토리 전체에서 "INSERT INTO graph_nodes/graph_edges"는 apps/api/src/modules/graph/graph-sync.service.ts 단 한 곳뿐이며, 이 서비스는 client(72-80행)/matter(81-89)/document(100-108)/version(109-117)/clause(document_chunks 기반, 143-1
개선안: 1단계: 기존 관계테이블(parties, contract_clauses, contract_defined_terms, litigation_facts, litigation_evidence_items, litigation_issue_nodes, dd_rfis/dd_issues/dd_risks)을 graph_nodes로 투영하는 매퍼를 GraphSyncService에 추가하고 node_type/edge_type CHECK를 확장(마이그레이션). EVIDENCED_BY(fact→evidence→document), DEFINES(version→defined_term), CONTAINS_CLAUSE(version→contract_clause), REQUIRES_ACTION(issue→rfi) 엣지부터 시작. 2단계: document_versions.supersedes 체인에서 SUPERSEDES 엣지, 문서 관계 테이블에서 AMENDS 엣지 파생. 3단계: Authority/WorkProduct/TimelineEvent/Obligation/NegotiationPosition 신규 테이블+노드 추가. 기존 adapter-isolation 계약(R7_knowledge_graph.md) 덕분에 스토어 교체 없이 확장 가능하다.

--- (critical/SHALLOW) 의미검색·GraphRAG용 벡터가 실제 임베딩 모델이 아닌 16차원 SHA256 토큰 해시 — 벡터 인덱스는 pgvector 실물이나 의미론은 합성
요구출처: 요구 컨셉 'GraphRAG+벡터인덱스', 사양서 §6.5 의미검색, Obsidian 컨셉(concept-obsidian-llm.txt:3 'GraphRAG·벡터')
현재상태: document_chunk_embeddings.embedding은 vector(16)이고 테이블 코멘트가 'local deterministic vector used for R6 retrieval tests and local-only semantic search'라고 자인한다. 임베딩 생성은 토큰별 SHA256 다이제스트 바이트를 더해 정규화하는 결정론 함수(local-embedding.ts)로, 동의어·의미 유사성을 전혀 포착하지 못한다. 검색쿼리는 pgvector 코사인(<=>)을 실제 사용하지만 입력 벡터가 해시이므로 사실상 토큰 일치 검색이다. 외부 임베딩은 DEC-11로 차단되어 있고, 유사조항 검색은 어디에도 없다.
증거: apps/api/src/modules/search/semantic/local-embedding.ts:6-23 (SHA256 기반 deterministicEmbeddingVector); db/migrations/0049_create_document_chunks_and_embeddings.sql:80 (vector(16)), 134-137 (테이블 코멘트); apps/api/src/modules/search/query/search-query.builder.ts:580 (<=> 코사인 연산 실사용); db/migrations/0049_create_document_chunks_and_embeddings.sql:78-79 (model_route CHECK = 'local_gemma' 고정)
검증판정: CONFIRMED — 모든 인용 근거가 정확히 일치한다. (1) apps/api/src/modules/search/semantic/local-embedding.ts:6-23의 deterministicEmbeddingVector는 토큰별 SHA256 다이제스트 바이트를 부호(다이제스트 바이트 홀짝)와 함께 합산 후 L2 정규화하는 순수 해시 함수로, 어떤 모델 추론도 없다. (2) db/migrations/0049 라인 80은 vector(16), 라인 78은 model_route CHECK='local_gemma' 고정(이름뿐, 실제 Gemma 미관여), 라인 131-135 코멘트는 'local deterministic embeddings only... blocked by DEC-11'과 '16 dimension local deterministic vector used for R6 retrieval tests and local-only semantic search'로 자인한다. (3) search-query.builder.ts:580의 <=> 코사인은 실사용이나 입력이 해시 벡터다. 반박 수색 결과: 임베딩 생성 경로는 3곳(색인 write: search-index.repository.ts:317, 검색 query: search.service.ts:317, AI retrieval/GraphRAG: retrieval-orchestrator.service.t
개선안: 이미 Ollama 게이트웨이(LocalGemmaGateway, 127.0.0.1:11434)가 운영 중이므로 DEC-11(외부 모델 차단)을 지키면서 로컬 임베딩 모델(bge-m3 또는 multilingual-e5-large, 한국어 법률문서 지원)을 같은 게이트웨이로 서빙. 마이그레이션으로 vector(1024) 컬럼+HNSW 인덱스 추가, model_route에 신규 라우트 허용, 기존 16차원은 폴백으로 유지. 재색인은 기존 reindex.service의 stale 파이프라인 재사용. 이후 contract_clause_chunks에 조항 단위 임베딩을 추가해 '유사조항 검색'(매도인측 SPA 책임한도 조항 등 §6.4 질의)을 clause-bank API에 연결.

--- (critical/NOT_OPERATIONAL) 지식그래프 전체가 웹 UI 미연결·수동 관리자 동기화 — 그래프/계약/DD/송무 화면이 봉인(RouteBlockedState)되어 실사용 불가
요구출처: 사양서 §6.4 (관계 기반 지식검색을 사용자에게 제공), 사양서 247줄 (Matter 화면에서 쟁점·관련 지식 확인)
현재상태: 웹 앱에 그래프 관련 페이지·API 클라이언트가 전혀 없다(apps/web/src/lib/api에 graph 없음, 'graph' 검색 결과는 Outlook의 Microsoft Graph뿐). GraphRAG를 쓰는 유일한 경로인 POST /ai/summaries도 웹에서 호출하는 곳이 없다. /contracts, /dd, /litigation, /launch, /scale 라우트는 모두 RouteBlockedState('운영 노출 차단')를 렌더하며 hidden-routes.test.tsx가 봉인 유지를 테스트로 강제한다. contract-intel 웹 API 클라이언트(apps/web/src/lib/api/contract-intel.ts)는 존재하지만 어떤 페이지도 import하지 않는다. 그래프 동기화는 firm_admin/security_admin 전용 수동 POST /graph/sync뿐이고 문서 업로드/버전 추가 시 자동 동기화 훅이 없어 그래프는 항상 stale 상태로 남는다.
증거: apps/web/src/app/(app)/hidden-routes.test.tsx:18-56 (5개 라우트 봉인 검증); apps/web/src/app/(app)/contracts/page.tsx:1-6 (RouteBlockedState만 렌더); apps/api/src/modules/graph/graph.controller.ts:52-58 (sync는 admin 롤 전용); apps/api/src/modules/ai/features/ai-summary.controller.ts:22-26 (POST /ai/summaries — 웹 호출처 없음); docs/current-code-state.md:31-32 (운영 표면은 /files /search /work /notifications 뿐)
검증판정: CONFIRMED — 모든 주장 근거가 원문과 일치함을 직접 확인했다. (1) apps/web/src/app/(app)/hidden-routes.test.tsx:18-56은 /launch /scale /contracts /dd /litigation 5개 라우트가 '운영 노출 차단'만 렌더하고 RFI-001/EV-001/FACT-001/PLD-001 등 실데이터 문자열이 없음을 테스트로 강제한다. (2) contracts/dd/litigation/launch/scale의 page.tsx 5개 모두 RouteBlockedState만 렌더하는 6줄 파일이다. (3) apps/web/src/lib/api에 graph 클라이언트가 없고, 웹 전체에서 'graph' 검색 결과는 Outlook의 Microsoft Graph i18n 문자열과 HTMLParagraphElement 오탐뿐이다. (4) contract-intel.ts는 자기 spec 파일만 import하며 어떤 페이지도 사용하지 않는다(dd.ts/litigation.ts/scale.ts도 동일). (5) POST /ai/summaries의 호출처는 tests/integration/ai-summaries.spec.ts뿐이고 웹 AI 클라이언트는 /ai/prep/feedback, /ai/ops/health, /ai/ops/metrics만 호출한다. (6) POST /graph/sync는 firm_admin/s
개선안: 1단계: 문서 라이프사이클 이벤트(업로드·버전추가·삭제·텍스트추출완료)에 outbox 패턴으로 matter 단위 그래프 증분 동기화를 트리거(현재 syncMatter는 이미 멱등이므로 재사용 가능), 감사로그 유지. 2단계: /matters/[matterId]에 '관련 지식' 패널을 추가해 GET /graph/facts를 소비(권한 스코프는 이미 서버에서 강제됨). 3단계: 봉인된 /contracts·/dd·/litigation 라우트를 외부 스모크 증거 확보 후 단계적으로 개방 — 백엔드 API·마이그레이션·권한이 이미 완비되어 있어 UI 개방이 최소비용 경로다.

--- (high/SHALLOW) Citation Ledger가 생성 시점 강제(실구현)에 그치고 영속 원장이 없으며, Fact Ledger는 citation 없는 Fact를 허용
요구출처: Obsidian 컨셉(concept-obsidian-llm.txt:21 'Citation Ledger — 모든 주장과 원문 근거의 연결', 665 'Citation Ledger 생성') + 요구 컨셉 'citation 없는 Fact 금지'
현재상태: 잘 구현된 부분: 로컬 Gemma 생성은 JSON 스키마에서 claim.source_refs를 evidence pack 허용 ref의 enum으로 강제해 citation 없는 claim이 구조적으로 생성 불가하고(grounded-output.guard가 재검증), citation-mapper는 권한검사 후 chunk→문서 해석과 AI_CITED_DOCUMENT 감사기록을 남긴다. citation-verifier는 UNCITED_CLAIM/UNKNOWN_CITATION 경고와 법적결론 자동승인 차단을 구현했다. 부족한 부분: 주장↔근거 연결이 영속 원장 테이블로 저장되지 않는다 — ai_sessions는 response_hash/response_length 등 해시만 저장하고 claim·citation 행은 어디에도 없어 사후 조회·감사·재검증이 불가하다. litigation_facts.citation_refs는 DEFAULT ARRAY[]::text[]로 빈 배열을 허용해 'citation 없는 Fact 금지'가 스키마·서비스 어디서도 강제되지 않는다.
증거: apps/api/src/modules/ai/generation/local-gemma-generation.service.ts:279-346 (source_refs enum 스키마 강제); apps/api/src/modules/ai/citation/citation-verifier.ts:15-40 (경고만 반환); db/migrations/0050_create_ai_sessions.sql:15-18 (response_hash만 저장, claim/citation 테이블 부재); db/migrations/0057_create_litigation_vault.sql:182-185 (citation_refs 빈 배열 허용); apps/api/src/modules/ai/citation/citation-mapper.service.ts:44-60 (해석·감사는 실구현)
검증판정: OVERSTATED — 인용된 근거 5건은 모두 정확했다. (1) local-gemma-generation.service.ts:279-346은 claims/sections의 source_refs를 evidence ref enum + minItems:1로 스키마 강제하고 grounded-output.guard.ts:19-29가 빈 refs를 throw로 재검증한다. (2) citation-verifier.ts는 UNCITED_CLAIM/UNKNOWN_CITATION 경고 반환과 legalConclusionAutoApproval:false 고정뿐, 차단은 아니다. (3) 0050 ai_sessions는 prompt/response 해시·길이만 저장한다. (4) 0057 litigation_facts.citation_refs는 DEFAULT ARRAY[]::text[]이고, packages/shared/src/litigation/litigation-types.ts:143의 zod도 .default([])에 min 없음, litigation.service.ts createFact(205-267행)도 입력을 그대로 삽입해 'citation 없는 Fact 금지'는 어디서도 강제되지 않는다(0056 dd_vault도 동일). 그러나 "claim·citation 행은 어디에도 없어 사후 조회·감사·재검증이 불가"는 과장이다. 반증 3건: (a) 0050의 ai_sessi
교정된 상태: 실제 상태: (1) 생성 시점 강제는 주장대로 실구현(JSON 스키마 enum + grounded-output.guard 재검증). (2) 영속성은 경로별로 다르다 — [대화형 AI summary 경로] claim 텍스트·claim↔citationRef 매핑은 미저장(ai_sessions.response_hash만)이라 claim 단위 사후 재검증 불가가 맞지만, citation 단위로는 ai_session_chunks 행(0050:78-145)과 AI_CITED_DOCUMENT 감사 행(ai-audit-recorder.service.ts:166-193, ai_session_id·document_id·version_id·chunk_id·hash 포함)이 영속 저장되어 '어떤 세션이 어떤 문서/chunk를 인용했는지' 사후 조회·감사는 가능하다. [AI prep(파일정리) 경로] ai_prep_artifacts.payload_json(0064)에 claims[]와 claim별 source_refs가 통째로 영속 저장되고, 0068 마이그레이션의 DB CHECK 함수가 'claim마다 source_refs ≥1, refs는 top-level source_refs 소속 + ^chunk: 형식, 법적결론 금지'를 DB 레벨에서 강제한다 — 이 경로는 영속 claim↔citation 원장 + uncited claim 금지가 이미 구현됨. (3
개선안: 1단계: ai_claims / ai_claim_citations 테이블(claim_id, session_id, claim_hash, kind, is_legal_conclusion, citation → chunk_id/version_id/char span, verification_status) 마이그레이션을 추가하고 LocalGemmaGenerationService 완료 시점에 영속화 — 기존 grounded output DTO가 이미 이 구조를 갖고 있어 매핑만 필요. 2단계: litigation_facts에 status='verified' 전이 시 citation_refs 비어있음 금지 CHECK 또는 서비스 가드 추가. 3단계: 원장을 graph_nodes(Citation 노드) + EVIDENCED_BY 엣지로 투영해 '모든 주장↔원문근거' 그래프 질의를 가능하게 한다.

--- (high/MISSING) AI생성(proposed) 노드와 변호사확정(confirmed) 노드 분리가 그래프에 없음 — AI 산출물이 그래프 노드로 전혀 유입되지 않음
요구출처: Obsidian 컨셉(concept-obsidian-llm.txt:395 'confidence: confirmed/likely/disputed/unknown', 668 '검토 완료 후 confirmed 상태로 변경', 789 'reviewStatus: unreviewed/reviewed/confirmed/rejected')
현재상태: graph_nodes에는 reviewStatus/provenance(AI생성 vs 사람확정) 차원이 없다 — stale boolean과 source_hash뿐. litigation_facts에는 draft/verified/disputed/withdrawn 상태가 있으나 이는 사람이 수동 기록하는 관계테이블이고 그래프와 무관하다. AI 파이프라인(요약·prep)의 산출 claim은 어떤 경로로도 그래프 노드나 Fact 후보로 저장되지 않으므로 'AI가 제안하고 변호사가 확정하는' 순환 자체가 존재하지 않는다.
증거: db/migrations/0053_create_knowledge_graph.sql:115-141 (graph_nodes 컬럼에 review/provenance 부재); db/migrations/0057_create_litigation_vault.sql:176-178 (fact status는 존재하나 그래프 미연결); apps/api/src/modules/ai/prep/ai-prep.repository.ts:206 (graphFacts: [] — prep은 그래프에 쓰지 않음)
검증판정: CONFIRMED — 모든 인용 근거가 정확하며, 반증 수색에서도 갭을 뒤집는 구현이 발견되지 않았다. (1) 0053 마이그레이션의 graph_nodes에는 source_hash/stale뿐 review·provenance 차원이 없고, 이후 어떤 마이그레이션(~0097)도 graph_nodes를 변경하지 않는다. 'issue'/'risk' 노드 타입의 source_table 'reserved_issue'/'reserved_risk'는 리포 전체에서 해당 마이그레이션 외 어디에서도 참조되지 않는 예약 슬롯이다. (2) graph-sync.service.ts는 clients/matters/documents/document_versions/document_chunks에서만 결정론적으로 노드를 파생하며 AI 산출물 유입 경로가 없다. (3) 유일한 AI↔그래프 연결은 반대 방향(ai-summary.service.ts:109가 graph facts를 evidence pack으로 읽음)이다. (4) ai-prep.repository.ts는 graphFacts: []를 하드코딩. (5) AI 아티팩트 수준의 리뷰 장치(ai_prep_artifacts의 rejected 상태, ai_prep_feedback_items의 useful/incorrect/stale, HUMAN_REVIEW_REQUIRED 경고)는 존재하지만 이는 아티팩트 QA일 뿐 그래프 노드나 Fact
개선안: graph_nodes에 provenance('derived'|'ai_proposed'|'human_confirmed')와 review_status 컬럼 추가 마이그레이션 후, (1) AI claim 영속화(위 finding) 시 ai_proposed Fact 노드 생성, (2) 변호사 확인 UI 액션으로 human_confirmed 전이 + 감사이벤트(FACT_CONFIRMED), (3) GraphRAG 프롬프트 컴파일러(evidence-prompt.compiler)가 confirmed/unreviewed를 구분 표기하도록 확장 — 컨셉 799줄의 'confirmed 정보와 unreviewed 정보를 구분해서 답변' 요건을 충족시킨다.

--- (high/SHALLOW) 조항은행(clause bank)이 matter 범위 해시/오프셋 목록에 그침 — 재사용 승인 워크플로·전사 큐레이션·유사조항 검색 부재
요구출처: 요구 컨셉 '조항은행(재사용승인), 유사조항 검색', 사양서 703줄 '고급 조항은행', §6.4 질의 예시(매도인측 SPA 책임한도 조항 검색)
현재상태: listClauseBank는 단일 matter 범위에서 contract_clauses의 kind/number/hash/offset 목록을 반환하는 조회 API일 뿐이다(조항 본문은 의도적으로 미저장 — canonical_documents에서 권한검사 후 해석하는 설계). 전사(cross-matter) 조항 라이브러리, 재사용 승인 상태(approved/deprecated), 조항 버전 계보, 유사조항 벡터검색이 모두 없다. 파서 자체도 정규식 헤딩 매칭(Article/Section/제N조)이고 redline은 [[ADD:]]/[[DEL:]]/<ins>/<del> 마커 기반이라 실제 Word 변경추적(DOCX w:ins/w:del)을 읽지 못한다. 웹 클라이언트(contract-intel.ts)는 존재하나 사용 페이지가 없다.
증거: apps/api/src/modules/contract-intel/contract-intel.service.ts:366-400 (matter 범위 목록 조회); apps/api/src/modules/contract-intel/contract-parser.ts:38-49 (정규식 조항/마커 기반 redline); db/migrations/0054_create_contract_intelligence.sql:128-163 (contract_clauses — 승인/재사용 컬럼 없음); apps/web/src/lib/api/contract-intel.ts (페이지에서 미사용)
검증판정: CONFIRMED — 모든 인용 근거가 정확함을 직접 확인했다. (1) listClauseBank(contract-intel.service.ts:366-400 + queryClauseBank:482+)는 matterId 필수(contract-types.ts:83-89 zod schema)인 단일 matter 범위 조회이며 kind/number/offset/heading_hash/text_hash만 반환한다. 컨트롤러 엔드포인트는 POST /process, POST /playbook-rules, GET /clause-bank, GET /rule-findings 4개뿐 — 전사 조항 라이브러리·승인·유사검색 엔드포인트 없음. (2) 파서(contract-parser.ts:38-49)는 정규식 헤딩 매칭(Article/Section/Clause/제N조)이고 redline은 [[ADD:]]/[[DEL:]]/<ins>/<del> 마커 기반. Python 워커(workers/ingestion/app/parsers/contract.py)도 동일 로직(r8-local-v1)이며, DOCX 추출기(workers/ingestion/app/parsers/docx.py)는 모든 w:t 텍스트를 무차별 수집할 뿐 w:ins/w:del 변경추적 처리 코드가 리포 전체에 전무하다. (3) 0054 마이그레이션의 contract_clauses에 승인/재사용/deprecated 컬럼
개선안: 1단계: clause_bank_entries 테이블(tenant 전사 범위, source clause 참조, 승인상태 draft/approved/deprecated, 승인자, 사용횟수, 태그) + 승인 워크플로 API 추가. 2단계: 조항 단위 임베딩(위 임베딩 개선과 연동)으로 유사조항 검색 엔드포인트 구현 — 권한 스코프는 기존 SearchPermissionScopeProvider 재사용. 3단계: ingestion 워커의 docx 파서(workers/ingestion/app/parsers/docx.py)에 Word 변경추적(w:ins/w:del) 추출을 추가해 redline 파싱을 실물화. 4단계: 봉인된 /contracts UI를 조항은행 브라우저로 개방.

--- (medium/SHALLOW) Conflict Detector가 '그래프 vs 원본 RDB 드리프트' 검사에 한정 — 문서 간 불일치·버전충돌·근거누락 감지 부재
요구출처: 요구 컨셉 'Conflict Detector(문서간 불일치·버전충돌·근거누락)', Obsidian 컨셉의 문서 간 모순 감지
현재상태: GraphConsistencyService는 missing_document_node/missing_version_node/stale_document_node/edge_points_to_stale_node 4종의 파생-인덱스 드리프트만 검사한다(잘 만든 인프라 정합성 도구이나 법률적 충돌 감지가 아님). 문서 간 내용 불일치, 버전 계보 충돌(SUPERSEDES/AMENDS 부재로 표현 자체가 불가), 근거 없는 주장/Fact 탐지는 없다. 유일한 내용 수준 충돌 감지는 contract_defined_terms.conflict_status(동일 버전 내 정의어 중복)뿐이다.
증거: apps/api/src/modules/graph/graph-consistency.service.ts:28-105 (4종 드리프트 CTE); db/migrations/0054_create_contract_intelligence.sql:259-260 (defined term conflict_status); db/migrations/0053_create_knowledge_graph.sql:192-201 (SUPERSEDES/AMENDS 엣지 부재)
개선안: SUPERSEDES/AMENDS 엣지 도입(택소노미 확장 finding과 연동) 후 3종 검사기를 GraphConsistencyService에 추가: (1) 버전충돌 — 동일 문서 패밀리에서 current 버전 다중/계보 단절 감지, (2) 문서간 정의어·조항 불일치 — matter 내 여러 계약의 contract_defined_terms definition_hash 교차 비교, (3) 근거누락 — verified 상태 litigation_facts 중 citation_refs 빈 항목과 EVIDENCED_BY 엣지 없는 Fact 노드 목록화. 결과는 기존 drift DTO 패턴(ID-only)으로 반환해 민감정보 계약을 유지.

--- (medium/MISSING) 상대방 로펌 협상패턴·고객 Playbook 고도화 부재 — playbook_rules는 실구현이나 범용 룰 3종에 그침
요구출처: 사양서 §6.4 질의 예시('이 상대방 로펌이 과거 자주 요구한 indemnity 조항은?'), 703줄 '상대방 로펌 패턴분석', 요구 컨셉 '고객 Playbook, 상대방 협상패턴'
현재상태: playbook_rules 테이블과 결정론적 룰엔진(required_clause/prohibited_term/threshold 3종, evaluatePlaybookRule)은 실제 동작하는 구현이고 감사로그(PLAYBOOK_RULE_CHANGED, CONTRACT_RULE_EVALUATED)도 있다. 그러나 고객별 Playbook 스코프(client_id 연결 없음, matter_id만 옵션), NegotiationPosition 노드/테이블, 상대방 로펌(Counterparty Counsel) 추적, 협상 이력 축적·패턴 분석은 전혀 없다. parties 테이블(0020)에 상대방 정보는 있으나 협상 데이터와 연결되지 않는다. 'negotiation'은 문서 lifecycle status 값으로만 존재한다.
증거: db/migrations/0054_create_contract_intelligence.sql:365-419 (playbook_rules — client 스코프 없음); apps/api/src/modules/contract-intel/contract-rule-engine.ts (3종 룰 평가); db/migrations/0033_documents_lifecycle_columns.sql:33 ('negotiation'은 문서 상태값); db/migrations/0020_create_parties.sql (상대방 정보 존재, 협상 데이터 미연결)
개선안: 1단계: playbook_rules에 client_id 스코프 추가(고객 Playbook). 2단계: negotiation_positions 테이블(matter, party, issue, position summary, source document/clause 참조, 회차) + redline 파싱 결과와 연결. 3단계: 상대방 로펌 엔터티(parties 확장 또는 counsel 테이블)별 조항 요구 이력을 집계하는 분석 뷰를 만들고, 조항 임베딩 기반 클러스터링으로 '자주 요구한 조항' 질의를 GraphRAG 경로에 노출.

--- (medium/MISSING) LLM Wiki/Obsidian 스타일 마크다운 뷰 재생성 기능 전무
요구출처: Obsidian 컨셉 문서 전반(제목 자체가 Obsidian+LLM+Neo4j Matter Graph; Wiki 재생성 파이프라인 concept-obsidian-llm.txt:644-668)
현재상태: 그래프/Fact/문서 데이터를 마크다운 위키 페이지(Matter 개요, Issue 페이지, 백링크)로 재생성하는 코드가 API·웹·워커 어디에도 없다. 'wiki/markdown' 검색은 records 모듈의 무관한 매치뿐이다. Evidence pack → 프롬프트 컴파일은 있으나 산출물이 위키 문서로 영속화되지 않는다.
증거: apps/api/src 전체 grep 'wiki|markdown|obsidian' — 해당 기능 없음 (records.service.ts 등 무관 매치만); apps/api/src/modules/ai/generation/evidence-prompt.compiler.ts (프롬프트 컴파일만, 위키 영속화 없음)
개선안: matter_wiki_pages 테이블(matter_id, page_kind[overview/issue/party/timeline], markdown_body, source_refs, generation provenance, review_status)을 추가하고, 그래프 facts + Fact Ledger + AI 요약을 입력으로 LocalGemmaGenerationService를 재사용해 페이지를 생성·재생성하는 워커 잡을 만든다. 생성물은 ai_proposed로 저장하고 변호사 확정 후 confirmed 전이(리뷰 분리 finding과 동일 메커니즘). 웹 /matters/[matterId]에 위키 탭으로 노출하며 [[링크]]는 graph_nodes 참조로 해석한다.

--- (medium/QUALITY) 그래프의 'clause' 노드가 계약 인텔리전스의 contract_clauses가 아닌 범용 document_chunks — 두 조항 모델이 이원화되어 그래프 질의에서 법적 조항 의미 상실
요구출처: 요구 컨셉 'CONTAINS_CLAUSE(계약서가 특정 조항을 포함)', Obsidian 컨셉 concept-obsidian-llm.txt:99
현재상태: GraphSyncService.listClauseChunks는 document_chunks(chunk_kind='parent', 텍스트 분할 단위)를 clause 노드로 동기화한다. 정작 조항 구조(Article/Section/제N조, 계층, 정의어 연결)를 가진 contract_clauses는 그래프에 투영되지 않아, §6.4의 '조항 유형 기반 관계 질의'가 그래프에서 불가능하다. contract_clause_chunks가 두 모델의 브리지 테이블로 이미 존재하지만 그래프 동기화가 이를 쓰지 않는다.
증거: apps/api/src/modules/graph/graph-sync.service.ts:134-159 (document_chunks를 clause로 업서트); db/migrations/0053_create_knowledge_graph.sql:121-131 (source_table에 contract_clauses 없음); db/migrations/0054_create_contract_intelligence.sql:189-226 (contract_clause_chunks 브리지 존재하나 미활용)
개선안: graph_nodes.source_table 허용값에 contract_clauses를 추가하는 마이그레이션 후, GraphSyncService가 contract_clauses를 CONTAINS_CLAUSE 엣지(version→clause)로 동기화하고 기존 chunk 기반 clause 노드는 'text_chunk' 노드타입으로 개명해 역할을 분리한다. contract_clause_chunks 브리지로 clause↔chunk ALIGNED_WITH 엣지를 만들어 GraphRAG가 조항 컨텍스트와 검색 chunk를 함께 획득하게 한다.

--- (medium/SHALLOW) 그래프 질의가 단일 홉 엣지 목록 조회(limit 50)에 한정 — 다중 홉 경로 탐색·그래프 알고리즘 불가
요구출처: 사양서 §6.4 (관계 기반 질의: 상대방로펌→조항→협상이력 등 다중 홉), Obsidian 컨셉 721-768줄 (Matter→Issue→RFIItem, Party→NegotiationPosition→Issue→Clause 경로 질의)
현재상태: GET /graph/facts는 HAS_MATTER 1건 + matter 범위 문서 엣지 최대 50건을 평면 목록으로 반환하는 단일 홉 조회다. 재귀 CTE, 경로 탐색, 이웃 확장 API가 없어 컨셉의 다중 홉 질의(예: Issue를 거쳐 RFI→답변문서로 도달)를 표현할 수 없다. Postgres 선택 자체는 문서화된 결정이며 RLS·권한 재사용 이점이 실재하므로 스토어가 문제가 아니라 질의 계층의 깊이가 문제다.
증거: apps/api/src/modules/graph/graph-query.service.ts:70 (limit 50), 91-118 (단일 홉 목록); docs/architecture/R7_knowledge_graph.md:24-31 (Postgres 어댑터 선택 근거); apps/api/src/modules/graph/graph.controller.ts:60-69 (facts 엔드포인트가 유일한 질의 표면)
개선안: 1단계: 권한 스코프가 주입된 재귀 CTE 기반 n-hop 이웃/경로 API(GET /graph/neighborhood?nodeId&depth<=3)를 추가 — 기존 permission-before-graph 계약을 depth마다 적용. 2단계: 질의 수요가 검증되면 어댑터 계약(GRAPH-TAXO-NODEEDGE-TUW-002)을 활용해 Apache AGE(Postgres 확장, RLS 유지 가능) 또는 Neo4j를 read-replica 프로젝션으로 도입하고 Cypher 질의를 개방 — 쓰기 원본은 Postgres에 유지해 감사·RLS 보증을 지킨다. 3단계: GraphRAG 컨텍스트 빌더가 단순 facts 목록 대신 질의 유형별(question-classifier가 이미 graph 패턴을 분류함) 경로 서브그래프를 evidence pack에 주입하도록 확장.

--- (high/NOT_OPERATIONAL) Fact Ledger·Issue·RFI 백엔드는 실구현이나 운영 봉인 상태 — 사양서 Fact Ledger 요건의 마지막 단계(노출) 미완
요구출처: 요구 컨셉 'Fact Ledger', Obsidian 컨셉 5.4 Fact Ledger 모델(concept-obsidian-llm.txt:387-406), 사양서 703줄 '송무 Fact Ledger'
현재상태: litigation_facts(코드·요약·날짜·draft/verified/disputed/withdrawn·materiality·citation_refs·증거 FK)와 litigation_evidence_items(custody/admitted 상태), litigation_issue_nodes(계층), dd_rfis 등 스키마와 서비스 로직(899+1,019줄, 권한·감사 포함)은 관계테이블 기반으로 견고하게 구현되어 있다. 그러나 /litigation·/dd 라우트가 RouteBlockedState로 봉인되어 최종 사용자가 접근할 수 없고, Fact가 그래프 노드로 투영되지 않으며 EVIDENCED_BY 관계 질의도 불가하다. 과장 방지 관점에서: 이 백엔드는 STUB이 아니라 진짜 구현이다 — 문제는 노출과 그래프 통합이다.
증거: db/migrations/0057_create_litigation_vault.sql:165-233 (litigation_facts 전체 스키마); apps/api/src/modules/litigation/litigation.service.ts:139-184 (증거 등록 실로직); apps/web/src/app/(app)/hidden-routes.test.tsx:41-53 (FACT-001 등 노출 차단 검증); apps/web/src/app/(app)/dd/page.tsx, litigation/page.tsx (RouteBlockedState)
검증판정: CONFIRMED — 모든 인용 근거가 정확하고, 반박 증거를 찾기 위한 적대적 수색에서도 갭 주장을 뒤집을 구현이 발견되지 않았다.

[1] 백엔드가 실구현임을 확인 (주장의 '과장 방지' 부분 정확): db/migrations/0057_create_litigation_vault.sql 165-233행에 litigation_facts가 정확히 존재 — fact_code 정규식 CHECK, fact_summary(1-2000자+시크릿 차단), draft/verified/disputed/withdrawn 상태, low/medium/high/critical materiality, citation_refs(최대 20개+금칙어 CHECK), litigation_evidence_items FK, 테넌트 RLS(FORCE), 컬럼 단위 GRANT까지 갖춤. litigation.service.ts는 정확히 899줄, dd.service.ts는 정확히 1,019줄(주장 수치 일치). service.ts 124-191행 createEvidence는 assertCanEditMatter/assertCanReadDocument 권한 검사, 문서 버전-매터 일치 검증, LIT_EVIDENCE_CHANGED 감사 로그를 포함한 실로직. LitigationModule/DdModule은 apps/api/src/app.module.ts 67-68행에 실제 등록되어 있고, tests/i
개선안: 외부 스모크·오너 증거 게이트(docs/current-code-state.md의 릴리스 경계)를 통과시켜 /litigation·/dd 라우트를 개방하는 것이 최우선 — 백엔드가 완성되어 있어 UI 클라이언트 연결만 필요하다. 동시에 Fact/Evidence/Issue/RFI를 그래프 노드로 투영(택소노미 확장 finding)하고, Fact 상세 화면에서 EVIDENCED_BY 체인(Fact→Evidence→Document→Version)을 시각화해 컨셉의 'Fact Ledger가 그래프의 1급 시민'이라는 요건을 완성한다.

--- (low/STUB) issue/risk 노드타입이 예약만 되고 영구 미사용 — dd_issues/dd_risks/litigation_issue_nodes와 단절
요구출처: 사양서 §6.4 (법률쟁점 노드), Obsidian 컨셉 Issue/Risk 노드
현재상태: graph_nodes CHECK에 issue/risk 타입과 source_table 'reserved_issue'/'reserved_risk'가 정의되어 있으나 API 코드 전체에서 이 값을 쓰는 곳이 없다(grep 0건). 실제 이슈/리스크 데이터는 dd_issues·dd_risks·litigation_issue_nodes에 존재하지만 그래프 동기화 대상이 아니어서 HAS_ISSUE/HAS_RISK 엣지도 생성되지 않는다.
증거: db/migrations/0053_create_knowledge_graph.sql:128-129 (reserved_issue/reserved_risk); apps/api/src grep 'reserved_issue|reserved_risk' — 참조 0건; db/migrations/0056_create_dd_vault.sql:230-306 (dd_issues/dd_risks 존재)
개선안: source_table 허용값을 dd_issues/dd_risks/litigation_issue_nodes로 교체하는 마이그레이션 후 GraphSyncService에 이슈/리스크 매퍼를 추가 — HAS_ISSUE(matter→issue), HAS_RISK(matter→risk), 그리고 issue 계층(parent_issue_id)을 RELATED_TO 또는 신규 HAS_SUB_ISSUE 엣지로 투영. 택소노미 확장 finding의 1단계에 포함해 함께 처리하는 것이 효율적이다.

====================================================================================================
[workflows-collab] maturity=NOT_OPERATIONAL

구현요약:
백엔드에는 4개 워크플로 축이 모두 실제 Postgres 스키마와 NestJS 서비스로 존재한다. (1) 계약 인텔리전스: regex 기반 분류기/조항 파서/redline 파서와 playbook 룰엔진, 조항은행 조회가 구현되어 있고(apps/api/src/modules/contract-intel/{contract-classifier.ts,contract-parser.ts,contract-rule-engine.ts,contract-intel.service.ts}, db/migrations/0054_create_contract_intelligence.sql), 감사로그·권한스코프가 결합되어 있다. (2) DD Vault: dd_rfis/dd_data_room_mappings/dd_issues/dd_risks CRUD + traceability(apps/api/src/modules/dd/dd.service.ts, db/migrations/0056_create_dd_vault.sql). (3) 송무 Vault: 증거/사실(Fact Ledger)/쟁점트리/서면 CRUD + case map(apps/api/src/modules/litigation/litigation.service.ts, db/migrations/0057_create_litigation_vault.sql). (4) External Portal: 워크스페이스/외부사용자/토큰 기반 secure link(만료·회수·NDA 게이트·워터마크 플래그·공유 전 DLP 스캔+오버라이드 감사)·Q&A(질문/내부답변)·접근 감사와 access_count가 실 DB로 구현되어 있고(apps/api/src/modules/external/external.service.ts, db/migrations/0058·0059), 외부용 웹 클라이언트(/external/[token], apps/web/src/app/(external)/external/[token]/external-portal-client.tsx)가 공개 API에 실제 연결되어 있다. RLS 강제·통합테스트(tests/integration/{contract-intel,dd-vault,litigation-vault,external-core,external-portal-gate}.spec.ts)도 존재한다. /work·/notifications는 production 가시 라우트로 work_items/notifications 테이블(0084~0086)에 연결된 운영급 화면이다. 그러나 /contracts·/dd·/litigation 웹 라우트는 RouteBlockedState로 전면 봉인('Out of scope' hidden)되어 있고, 외부 포털은 문서 바이트를 실제로 전달하는 경로(뷰어·다운로드 상환 엔드포인트)가 없으며, 링크/워크스페이스 생성 내부 관리 UI도 없다. 15단계 계약검토·DD·송무 워크플로의 자동화 단계(자동분류→매핑, 누락알림, 보고서초안, OCR, 기일관리, Closing Binder, archive)와 워크플로 오케스트레이션 엔진은 코드 흔적이 없다.


--- (critical/NOT_OPERATIONAL) 계약/DD/송무 워크플로 웹 화면이 전부 프로덕션 봉인(RouteBlockedState) 상태
요구출처: 사양서 §9.1~9.3 — 계약검토·M&A실사·송무 워크플로는 사용자 대면 운영 화면이어야 함
현재상태: /contracts, /dd, /litigation 라우트는 features.ts에서 production='hidden', group='Out of scope'로 분류되고 페이지 컴포넌트는 RouteBlockedState 한 줄만 렌더링한다. hidden-routes.test.tsx가 이 봉인을 회귀 테스트로 고정하고 있다. 백엔드 API(DdController, LitigationController, ContractIntelController)는 app.module.ts에 모두 마운트되어 있으나 이를 호출하는 웹 화면이 존재하지 않는다.
증거: apps/web/src/lib/features.ts:156-176; apps/web/src/app/(app)/litigation/page.tsx:4-6; apps/web/src/app/(app)/hidden-routes.test.tsx:18-56; apps/api/src/app.module.ts:65-69
검증판정: CONFIRMED — 인용된 근거 4건 모두 현재 워킹트리에서 정확히 일치함. (1) apps/web/src/lib/features.ts:156-176에서 /contracts, /dd, /litigation은 group='Out of scope', production='hidden', showInNavigation=false이며, canRoleViewRoute(179-183행)는 'hidden'이면 역할과 무관하게 false를 반환한다. (2) 세 라우트의 page.tsx는 모두 RouteBlockedState 한 줄만 렌더링하고, 각 라우트 디렉터리에는 page.tsx 외 다른 파일이 없다. (3) hidden-routes.test.tsx:18-56이 차단 문구('운영 노출 차단') 렌더링과 도메인 콘텐츠(RFI-001, EV-001, FACT-001, PLD-001, Witness timeline) 미노출을 회귀 테스트로 고정한다. (4) app.module.ts:65-68에 ContractIntelModule, DdModule, LitigationModule이 마운트되어 있다. 반박 시도 결과: 웹 API 클라이언트 래퍼(apps/web/src/lib/api/dd.ts, litigation.ts, contract-intel.ts)는 존재하나 자기 spec 파일 외에는 어디서도 import되지 않는 고아 코드다. git 이력상 실제 API를 호출하던
개선안: (1) matter 상세를 matter type별 워크스페이스 허브로 재구성: matters/[matterId] 하위에 contracts/dd/litigation 탭을 신설하고 기존 DD/Litigation/ContractIntel API에 연결. (2) features.ts의 3개 라우트를 visible_limited(matter 멤버 역할 기반)로 전환하고 hidden-routes 테스트를 역할 게이트 테스트로 교체. (3) 각 화면은 기존 traceability/caseMap/clause-bank 응답을 재사용하므로 read-only 뷰 → CRUD 폼 순으로 2단계 릴리스.

--- (critical/NOT_OPERATIONAL) 외부 포털이 문서 콘텐츠를 실제로 전달하지 못함 — 뷰어/다운로드 바이트 경로 부재
요구출처: 사양서 §10 — VDR 열람, 다운로드불가 모드, 워터마크 삽입(사용자명·이메일·IP·시간)
현재상태: manifest()와 downloadTicket()은 watermarkRef/downloadRef라는 합성 문자열만 반환하며(watermark:{linkId}:{userId}, download:{linkId}:{random}), downloadRef를 실제 파일 바이트로 상환하는 엔드포인트가 API 어디에도 없다. 웹 포털 클라이언트는 '다운로드가 준비되었습니다' 문구만 표시한다. 워터마크도 렌더링 없이 플래그(watermark_required)와 ref 문자열뿐이다. 즉 NDA·만료·감사 통제는 실동작하지만 정작 공유 대상 문서를 외부인이 볼 수 없다.
증거: apps/api/src/modules/external/external.service.ts:269-275; apps/api/src/modules/external/external.service.ts:688-743; apps/web/src/app/(external)/external/[token]/external-portal-client.tsx:60-65; db/migrations/0058_create_external_core.sql:264
검증판정: CONFIRMED — 모든 인용이 정확하며, 반박 증거를 적극적으로 수색했으나 발견하지 못했다.

(1) 인용 검증 — 전부 정확:
- apps/api/src/modules/external/external.service.ts:269-275: watermarkRefFor()는 `watermark:${link.link_id}:${link.external_user_id}`, downloadRefFor()는 `download:${link.link_id}:${randomBytes(16).toString('hex')}` — 순수 합성 문자열이며 스토리지 키/서명 URL과 무관하다. 생성된 downloadRef는 어떤 테이블에도 저장되지 않고 감사 로그 metadata(scope_id)에만 기록된다.
- external.service.ts:688-743 downloadTicket(): access_count 증가 + EXTERNAL_DOWNLOAD_REQUESTED 감사 로그 후 DTO만 반환. StorageService 미주입(ExternalService 생성자는 Audit/Permission/DocumentPermission/Dlp만 주입) — 바이트 경로 없음.
- 외부 토큰 라우트 전수(external.controller.ts): access/:token, /nda, /manifest, /download-ticket, /qa, /qa/questions 6개뿐.
개선안: (1) GET /external/access/:token/content 스트리밍 엔드포인트 신설: storage 모듈에서 file_object를 읽어 PDF 변환본(preview 모듈의 document_preview_artifacts 재사용)에 서버사이드 워터마크(외부사용자 email_hash 표시명·IP·타임스탬프 오버레이, pdf-lib 또는 ingestion worker의 PDF 파이프라인 확장)를 굽고 short-lived signed URL 또는 직접 스트림으로 반환. (2) 포털에 PDF.js 뷰어 탑재, watermark_required 링크는 view-only(다운로드 버튼 제거) 모드 지원. (3) downloadRef를 external_download_tickets 테이블(단일 사용·TTL)로 영속화해 상환-소진 감사를 남긴다.

--- (critical/NOT_OPERATIONAL) 외부 워크스페이스/링크 발급 내부 관리 UI 부재 — VDR 운영이 API 호출로만 가능
요구출처: 사양서 §10 — 고객별·Matter별 포털 개설, 파일별 권한, 링크 관리; §9.2 고객 포털 개설 단계
현재상태: POST /external/workspaces, /external/users, /external/links, /links/:id/revoke, workspace Q&A 답변 API는 존재하지만 apps/web에서 이를 호출하는 코드는 spec 파일 하나뿐이다. matter 상세의 MatterWorkspaceActions에도 외부 공유 액션이 없어 변호사가 UI에서 링크를 만들거나 회수하거나 Q&A에 답변할 방법이 없다.
증거: apps/api/src/modules/external/external.controller.ts:78-115; apps/web/src/lib/api/external-portal.spec.ts; apps/web/src/components/matter/matter-workspace-actions.tsx:22-49
검증판정: CONFIRMED — 갭 주장을 반박할 증거를 찾지 못했고, 오히려 주장보다 갭이 약간 더 큰 것으로 확인됨.

(1) 인용 정확성 검증:
- /Users/jws/Projects/amic-vault/apps/api/src/modules/external/external.controller.ts:78-115 — 세션 인증 기반 내부 관리 엔드포인트 6개가 실제 존재: POST /external/workspaces(78-82), POST /external/users(84-88), POST /external/links(90-94), POST /external/links/:linkId/revoke(96-100), GET /external/workspaces/:workspaceId/qa(102-105), POST /external/qa/:messageId/answers(107-115). 인용 정확.
- /Users/jws/Projects/amic-vault/apps/web/src/components/matter/matter-workspace-actions.tsx:22-53 — actions 배열은 파일함/검색/작업함/기록 보존/감사 기록 5개뿐. 외부 공유·링크 발급 액션 없음. 인용 정확.
- /Users/jws/Projects/amic-vault/apps/web/src/lib/api/external-portal.ts — 공개(@Public) 토큰 기반 /exte
개선안: matters/[matterId]/sharing 관리 화면 신설: 워크스페이스 생성(만료일 설정), 외부사용자 초대(이메일→email_hash 처리), 문서 선택→링크 발급(DLP 경고 수용 모달 포함), 링크 목록/회수, Q&A 인박스(listWorkspaceQa/createAnswer 연결), 접근 감사 타임라인. 기존 API가 완성되어 있으므로 프론트 작업만으로 운영 가능해지는 최단 경로다.

--- (critical/MISSING) 고객포털 자료 업로드 부재 — M&A 실사 인테이크 흐름의 출발점이 없음
요구출처: 사양서 §9.2 — 자료요청 발송→자료 업로드→자동분류→RFI 매핑; §10 자료요청(RFI 번호별 요청·담당자·기한·상태)
현재상태: 외부 공개 엔드포인트는 access/nda/manifest/download-ticket/qa 뿐이며 외부 사용자가 파일을 업로드할 수 있는 API·테이블·UI가 전혀 없다. dd_data_room_mappings는 내부자가 이미 업로드된 내부 문서를 수동으로 매핑하는 구조다. 포털에 RFI 목록/제출현황/기한 표시도 없다.
증거: apps/api/src/modules/external/external.controller.ts:117-171; apps/api/src/modules/dd/dd.service.ts:251-322; apps/web/src/app/(external)/external/[token]/external-portal-client.tsx:99-120
검증판정: CONFIRMED — 주장 근거 3곳을 직접 읽고, 반박 증거를 리포지토리 전역(apps/api/src/modules 전체, apps/web, workers, packages/shared, db/migrations, tools, 미커밋 워킹트리 변경 포함)에서 수색했으나 외부 업로드 구현의 흔적은 발견되지 않았다.

(1) 인용 정확성 확인 — 모두 정확함:
- apps/api/src/modules/external/external.controller.ts:117-171 — @Public 라우트는 GET access/:token, POST access/:token/nda, GET access/:token/manifest, GET access/:token/download-ticket, GET access/:token/qa, POST access/:token/qa/questions 뿐. 파일 업로드 라우트 없음(멀티파트/스트리밍 처리 코드도 external.service.ts 1,389줄 전체에 없음). 나머지 라우트(workspaces/users/links/answers)는 내부 세션 필수.
- apps/api/src/modules/dd/dd.service.ts:251-322 createMapping — 내부 사용자가 이미 존재하는 documents/document_versions를 dd_data_room_mappings에 INSERT하는 구조. db/mig
개선안: (1) external_upload_requests 테이블 + POST /external/access/:token/uploads(멀티파트→storage 모듈 재사용, 업로드 즉시 quarantine 상태) 신설. (2) 업로드 완료 시 workers/ingestion 파이프라인으로 추출→contract-intel 분류를 태워 dd_data_room_mappings 후보(rfi_id 추천)를 자동 생성. (3) 포털에 RFI별 요청 목록·기한·제출현황 뷰를 추가하고 dd_rfis를 workspace에 연결(rfi→workspace 노출 스코프 컬럼).

--- (high/MISSING) 계약검토 15단계 중 후반 8단계(송부본→마크업수령→버전비교→협상쟁점표→체결본→Closing Binder) 부재, 워크플로 오케스트레이션 없음
요구출처: 사양서 §9.1 — 계약서 검토 워크플로 15단계; §11.1 Workflow 후보 Temporal/Camunda
현재상태: contract-intel은 분류→조항추출→룰평가까지만 있고, 고객 송부본 생성, 상대방 마크업 수령(redline 파서는 [[ADD:]]/[[DEL:]]/<ins>/<del> 합성 마커만 인식하며 실제 DOCX tracked changes 파싱 없음 — ingestion의 docx.py도 본문 텍스트만 추출), 버전 비교 뷰, 협상쟁점표, 체결본 확정, Closing Binder는 코드 흔적이 없다. Temporal/Camunda 등 워크플로 엔진이나 단계 상태머신도 없다(grep 결과 없음). 미커밋 편집 라이프사이클(document-editing.service.ts, 2,375줄)이 edit session/subversion/reviewer gate를 제공하나 계약 워크플로와 연결되어 있지 않다.
증거: apps/api/src/modules/contract-intel/contract-parser.ts:44-49; apps/api/src/modules/contract-intel/contract-intel.service.ts:136-289; apps/api/src/modules/document/document-editing.service.ts:1; workers/ingestion/app/parsers/docx.py
검증판정: OVERSTATED — 주장 근거 인용은 전부 정확했다. (1) contract-parser.ts:44-49의 redlinePatterns는 [[ADD:]]/[[DEL:]]/<ins>/<del> 합성 마커 4종만 인식하고, workers/ingestion/app/parsers/docx.py(74줄)는 w:t 텍스트(문단·표·각주)만 추출하며 w:ins/w:del tracked changes 처리가 전혀 없다. (2) contract-intel.service.ts:136-289 processDocument는 분류→조항/정의어 upsert→합성 redline upsert→감사로그가 전부이고, 컨트롤러 라우트도 process/playbook-rules/clause-bank/rule-findings 4개뿐이다. (3) 'binder' 문자열은 apps/api·apps/web·packages·db·workers 어디에도 없고, 버전 비교(diff/compare) API·UI도 없으며, Temporal/Camunda/zeebe/xstate도 없다. (4) document-editing.service.ts(2,375줄)에는 계약 라이프사이클 상태 참조가 0건이고, 웹의 contract-intel API 클라이언트(apps/web/src/lib/api/contract-intel.ts)는 어떤 페이지/컴포넌트도 import하지 않아 UI 미연결이다. (5) API 전체에서
교정된 상태: 계약검토 후반 8단계는 데이터 모델 수준에서만 존재하고 운영 수준에서는 부재. 구체적으로: packages/domain(document-status.ts)에 client_sent→counterparty_sent→markup_received→negotiation→final→executed를 포함한 완전한 문서 상태머신 정의(allowedDocumentTransitions, canTransitionDocumentStatus)와 스펙 테스트, DB CHECK 제약(migration 0028), 웹 상태 라벨('고객 발송'/'상대방 발송'/'마크업 수령'/'협상')이 이미 존재한다. 그러나 이 상태머신을 호출하는 서비스/엔드포인트가 전무하고(문서 상태 변경은 draft 생성, 삭제/복원, records의 archived/disposal_locked뿐), 송부본 생성·마크업 수령·버전 비교·협상쟁점표·Closing Binder 기능, 워크플로 오케스트레이션 엔진(Temporal/Camunda 등)은 코드에 없다. redline 파서는 합성 마커 전용이고 docx 추출은 tracked changes 미지원이며, contract-intel은 API 4개 라우트만 있고 웹 UI에 연결되지 않았다. 미커밋 편집 라이프사이클(edit session/subversion/reviewer gate, 30+ 라우트)도 계약 상태머신과 연결되어 있지 않다. 결
개선안: (1) contract_review_workflows 테이블(단계 enum 15종 + 전이 감사)을 신설하고 Temporal(또는 pg 기반 경량 상태머신)으로 단계 전이를 오케스트레이션. (2) ingestion worker의 docx 파서에 python-docx 기반 w:ins/w:del tracked-changes 추출을 추가해 contract_redline_changes를 실제 마크업으로 채움. (3) 버전 비교는 document_versions 쌍의 canonical text diff API + 웹 diff 뷰로 구현하고, 협상쟁점표는 redline change×clause×playbook finding 조인 뷰로 시작. (4) Closing Binder는 records 모듈의 hold/disposal 위에 binder manifest(문서 스냅샷 목록+해시) export로 구현.

--- (high/SHALLOW) 조항추출·유형인식·리스크룰엔진이 regex/해시/카운트 수준의 데모 깊이
요구출처: 사양서 §9.1 — 문서유형 자동 인식, 조항 추출, 리스크 룰엔진 실행, AI 1차 검토
현재상태: 분류기는 8개 키워드 regex 가중치 합산(r8-local-v1)이고, 조항 파서는 'Article/Section/제N조' 라인 regex, defined term은 'X means...' 패턴 하나다. 룰엔진은 required_clause(조항 종류 개수), prohibited_term(정규화 용어키 일치), threshold(카운트 비교) 3종만 지원하며 조항 텍스트를 저장하지 않고 해시만 저장하므로 의미 기반 리스크 판정이 원천적으로 불가능하다. AI 경로(clause_analysis/risk_extraction, local Gemma)는 별도 ai-summary 서비스에 있으나 계약 워크플로/룰엔진과 연결되지 않는다.
증거: apps/api/src/modules/contract-intel/contract-classifier.ts:12-21; apps/api/src/modules/contract-intel/contract-parser.ts:38-49; apps/api/src/modules/contract-intel/contract-rule-engine.ts:73-80; packages/shared/src/ai/summary.ts:11-18; apps/api/src/modules/ai/features/ai-summary.service.ts:221-228
검증판정: CONFIRMED — 주장 근거 5건을 전부 원문 대조하고, 반박 증거를 apps/api 전체·apps/web·workers·packages·db/migrations·docs에서 수색했으나 갭 주장을 뒤집을 구현은 없었다.

[인용 검증 — 전부 정확]
1) 분류기: apps/api/src/modules/contract-intel/contract-classifier.ts:12-21 — 정확히 8개 키워드 regex 규칙(weight 0.18~0.46)의 가중치 합산, 버전 'r8-local-v1'. ML/임베딩/LLM 없음.
2) 파서: contract-parser.ts:38-42 — 조항 경계는 '(Article|Section|Clause) N' + '제N조' 라인 regex 1개, defined term은 '"X" (means|shall mean|has the meaning) ...' 패턴 1개, redline은 [[ADD:]]/[[DEL:]]/<ins>/<del> 마커. 매치 실패 시 문서 전체를 'whole-document' 단일 clause로 폴백.
3) 룰엔진: contract-rule-engine.ts:86-88 — required_clause(clauseKind 개수 비교), prohibited_term(normalizedTermKey 완전일치), threshold(4개 카운트 메트릭 eq/gte/lte) 3종만 지원. 그 외 타입은 'ru
개선안: (1) 조항 텍스트(또는 정책 허용 시 chunk 참조)를 contract_clause_chunks에 실제 본문으로 저장하고 기존 pgvector 임베딩 파이프라인(0049)과 연결해 조항 단위 의미 검색을 확보. (2) 분류기를 local Gemma(이미 존재하는 LocalGemmaGenerationService) 프롬프트 분류 + regex fallback 하이브리드로 교체. (3) 룰엔진에 clause-kind별 임베딩 유사도 매칭·표준문안 편차 스코어 룰 타입을 추가하고, 룰 위반 시 ai risk_extraction을 자동 트리거해 finding에 AI 근거를 첨부.

--- (medium/SHALLOW) 조항은행이 재사용 불가능한 해시 목록 — 표준문안 라이브러리 기능 없음
요구출처: 사양서 §9.1 — 조항은행 후보 등록; §부록 '조항은행, 유사문서 검색 ... 고객 playbook'
현재상태: listClauseBank는 clause_id·clause_number·heading_hash·text_hash·카운트만 반환한다. 조항 본문, 승인된 표준문안, fallback 문안, 태그/등급, '후보 등록→승인' 큐레이션 흐름이 없어 조항 재사용·비교가 불가능하다.
증거: apps/api/src/modules/contract-intel/contract-intel.service.ts:482-557; apps/api/src/modules/contract-intel/contract-intel.service.ts:976-993; db/migrations/0054_create_contract_intelligence.sql:128-249
개선안: clause_bank_entries 테이블(승인 상태, 표준/fallback 문안 텍스트, 출처 clause_id, 계약유형·position 태그, 승인자) 신설 + 체결본 처리 시 '조항은행 후보 등록' 자동 생성 → knowledge_manager 역할 승인 큐(/work 재사용). 임베딩 기반 유사 조항 검색을 붙여 초안 작성 시 추천.

--- (high/MISSING) DD 워크플로 자동화 부재 — RFI 템플릿, 자동분류→매핑, 누락알림, 보고서 초안, SPA 추출, archive 전무
요구출처: 사양서 §9.2 — RFI 템플릿 선택, 자료 자동분류, RFI 항목별 매핑, 누락자료 알림, 보고서 초안 생성, SPA 반영사항 추출, 데이터룸 archive
현재상태: dd 모듈은 RFI/매핑/이슈/리스크의 수동 CRUD 원장이다. RFI 템플릿 개념이 없고(0088의 matter template은 DMS 폴더 템플릿), 매핑은 사람이 문서를 골라 넣으며, dd_rfis.overdue는 SQL 계산 컬럼일 뿐 notifications 모듈의 kind 6종(문서처리/records)에 DD 알림이 없다. 보고서 초안 생성기·SPA 반영 추출·archive(freeze/index/export)는 코드 흔적이 없다. dd_issues.report_inclusion 플래그만 존재한다.
증거: apps/api/src/modules/dd/dd.service.ts:136-190; apps/api/src/modules/dd/dd.service.ts:544-570; apps/api/src/modules/notifications/notifications.service.ts:23-29; db/migrations/0056_create_dd_vault.sql:82-306
검증판정: CONFIRMED — 모든 인용 근거가 정확했고, 반박 증거 수색에서 아무것도 나오지 않았다. (1) dd.service.ts:136-190의 createRfi는 단순 INSERT+감사로그이며 템플릿 개념이 없다. (2) dd.service.ts:544-570의 overdue는 SELECT 시점 SQL 계산 불리언일 뿐이고, notifications.service.ts:23-29의 NotificationKind는 주장대로 6종뿐이며 0086 마이그레이션의 DB CHECK 제약이 이 6종만 허용하므로 DD 알림은 삽입 자체가 불가능하다. (3) 0056 마이그레이션은 dd_rfis/dd_data_room_mappings/dd_issues/dd_risks 4개 수동 원장 테이블뿐이고, dd_issues.report_inclusion(251행)이 유일한 보고서 관련 흔적이며, 테이블 코멘트가 'R9 internal-only... must not implement VDR sharing before R11'이라고 명시한다. (4) 0088의 matter template은 document_sets_json 기반 DMS 폴더/문서세트 템플릿으로 RFI 템플릿이 아니다(주장과 일치). (5) 자동분류→매핑: apps/workers(ingestion Python 워커 포함)/packages/tools 전체에서 auto-map·suggest·classify→mapping
개선안: (1) dd_rfi_templates(거래유형별 카테고리 트리) + 템플릿 인스턴스화 API. (2) ingestion 추출 완료 이벤트→contract-intel 분류 결과를 이용한 RFI 매핑 추천 잡(비동기 워커)과 확인 큐(/work의 work_items에 'dd_mapping_review' kind 추가). (3) overdue RFI·미매핑 RFI를 notifications kind로 추가해 기존 알림 센터에 연결. (4) 보고서 초안은 report_inclusion=true 이슈+리스크+citation을 ai-summary(matter_summary) 경로로 합성해 docx export. (5) archive는 workspace 종료 시 관련 문서·Q&A·감사로그 인덱스 manifest를 records 모듈 보존 정책으로 freeze.

--- (high/MISSING) 송무 워크플로의 자동화 단계 부재 — OCR 엔진, 사실관계 추출, 증거번호 자동생성, 판례연결, 준비서면 보조, 기일관리 없음
요구출처: 사양서 §9.3 — OCR, 사실관계 추출, 증거번호 후보 생성, 판례 리서치 연결, 준비서면 초안 보조, 기일 관리
현재상태: ingestion의 PDF 파서는 텍스트가 없으면 ocr_pending 상태만 마킹하고 실제 OCR 엔진(Tesseract 등) 연동이 없다. litigation_facts는 전부 수동 입력(사실관계 자동추출 없음), evidence_code/exhibit_label은 사용자 입력(증거번호 후보 생성 없음), 판례 연결·준비서면 보조 기능 없음, '기일/hearing' 관련 테이블·코드가 저장소 전체에 존재하지 않는다(litigation_pleadings.internal_deadline 단일 날짜 필드뿐).
증거: workers/ingestion/app/parsers/pdf.py:26-28; apps/api/src/modules/litigation/litigation.service.ts:124-191; db/migrations/0057_create_litigation_vault.sql:87-300
검증판정: CONFIRMED — 주장 근거 3개 파일을 모두 직접 확인했고 인용이 정확하다. (1) workers/ingestion/app/parsers/pdf.py:26-28은 텍스트가 없으면 ExtractionResult.ocr_pending()만 반환하며, 워커 pyproject.toml에 OCR 의존성(pypdf/python-docx/fastapi뿐)이 전혀 없고, 테스트명 자체가 test_blank_pdf_is_ocr_pending_without_external_ocr로 외부 OCR 부재를 명세화하고 있다. (2) litigation.service.ts의 createEvidence(124-191)·createFact(205-267)는 사용자 입력값(evidenceCode, exhibitLabel, factSummary)을 그대로 INSERT하는 수동 CRUD이며, 컨트롤러 전체가 CRUD 엔드포인트뿐(suggest/generate/auto 로직 없음). (3) 0057 마이그레이션은 evidence_items/facts/issue_nodes/pleadings 4개 테이블만 생성하고 일정 관련 필드는 litigation_pleadings.internal_deadline(date, 320행) 단 하나다. 적대적 반박 수색 결과: 저장소 전체 ts/tsx/py/sql에서 hearing/기일 0건, OCR 엔진(tesseract/easyocr/paddleocr/a
개선안: (1) ingestion worker에 OCR 스테이지(Tesseract+kor 모델 또는 PaddleOCR)를 추가해 ocr_pending→ready 전이를 실제로 수행(현재 /work의 'OCR 처리 대기' 항목이 실행 경로를 갖게 됨). (2) OCR 완료 문서에 local Gemma 사실추출 태스크(일시·행위자·행위 트리플)를 돌려 litigation_facts 후보를 생성하고 변호사 확인 큐로 연결. (3) 증거번호는 '갑 제N호증' 규칙 엔진(당사자 방향+순번)으로 후보 자동 부여. (4) litigation_hearings 테이블(기일 유형·일시·법정·제출 마감 역산)과 notifications 연동. (5) 판례 연결은 citation_refs를 구조화된 판례 ID 스킴으로 승격.

--- (high/MISSING) VDR 접근 통제 고급 기능 부재 — 외부사용자 2FA, IP 제한, bidder 그룹별 권한, redaction, 열람통계 화면, closing archive
요구출처: 사양서 §10 — 'NDA 동의, 2FA, IP 제한, 링크 만료, 워터마크, 파일별 권한 ... bidder별 권한, 다운로드 불가 모드, redaction, index export, closing archive, 열람통계'
현재상태: 구현된 것은 NDA 게이트·링크 만료·회수·watermark 플래그·DLP 사전 스캔·접근 감사뿐이다. external_secure_links 스키마에 2FA(OTP/이메일 인증), IP allow-list, bidder 그룹 개념이 없고 링크는 사용자×문서 1:1이라 그룹 단위 권한 매트릭스가 불가능하다. 외부 전달용 redaction 없음(DLP redaction은 AI 컨텍스트 전용). 열람통계는 audit_events/access_count 원시 데이터만 있고 문서별 열람자·시간·다운로드 여부 집계 화면이 없다. index export·closing archive 없음.
증거: db/migrations/0058_create_external_core.sql:251-292; apps/api/src/modules/external/external.service.ts:1212-1260; apps/api/src/modules/ai/retrieval/redaction-preprocessor.ts
개선안: (1) external_users에 이메일 OTP 2FA(코드 해시+TTL 테이블)와 workspace 단위 ip_allowlist(CIDR 배열, resolveToken에서 검증) 추가. (2) external_groups(bidder) 테이블 + 그룹×문서세트 권한 매트릭스로 링크 모델을 확장하고 다운로드불가(view-only) 모드를 그룹 정책으로 부여. (3) 열람통계는 EXTERNAL_LINK_ACCESSED/DOWNLOAD 감사 이벤트를 문서×사용자별 집계하는 GET /external/workspaces/:id/stats + 관리 UI. (4) 종료 시 closing archive: 문서 인덱스·Q&A·열람통계 manifest를 PDF/ZIP export하고 workspace를 frozen 상태로 전환.

--- (medium/SHALLOW) Q&A 승인흐름·공개범위 부재 — 질문이 검토 없이 직접 게시되고 답변에 승인 단계가 없음
요구출처: 사양서 §10 기능표 — 'Q&A: 질문/답변, 공개범위, 승인흐름'
현재상태: external_qa_messages는 direction(external_question/internal_answer) 2종과 parent 연결만 있고, 질문 승인/거절 상태, 답변 초안→승인자 검토→게시 흐름, 공개범위(질문자 한정 vs bidder 전체 공개) 필드가 없다. createAnswer는 matter owner 검증 후 즉시 게시된다.
증거: db/migrations/0059_external_portal_gate.sql:122-160; apps/api/src/modules/external/external.service.ts:840-891
개선안: external_qa_messages에 status(draft/pending_approval/published/rejected)와 visibility_scope(asker_only/workspace) 컬럼 추가, 답변 게시를 2단계(작성자≠승인자) 워크플로로 만들고 승인 대기를 work_items에 'external_qa_approval' kind로 연결. listQa는 published+visibility 스코프 필터로 제한.

--- (medium/SHALLOW) Work queue가 워크플로 단계 태스크를 다루지 못함 — DMS 운영 작업(추출실패/OCR대기/메타데이터/records)에 한정
요구출처: 사양서 §9 — 검토 배정, 담당 변호사 검토, RFI 담당자·기한 등 워크플로 태스크 운영
현재상태: /work는 운영 중인 실화면이고 work_items 테이블 기반으로 잘 작동하지만, kind가 records_disposal 2종 + 문서 운영 5종으로 고정되어 있어 계약검토 배정, DD RFI 기한, 이슈 검토, 외부 Q&A 응답 같은 워크플로 태스크가 큐에 나타나지 않는다. 상한 20건, 담당자 지정·재배정 개념도 없다.
증거: apps/api/src/modules/work/work.service.ts:22-29; apps/api/src/modules/work/work.service.ts:404-470; db/migrations/0084_create_dms_work_items.sql:3
개선안: work_items.kind를 확장 가능한 도메인 네임스페이스(contract_review_stage, dd_rfi_due, dd_mapping_review, external_qa_approval, litigation_deadline)로 열고, 각 도메인 서비스가 open/complete 헬퍼(WorkService.openRecordsDisposalWork 패턴 재사용)를 호출하도록 통일. 배정(assigned_to_user_id 재배정 API)·페이지네이션·필터를 /work UI에 추가.

--- (medium/NOT_OPERATIONAL) AI 1차 검토(clause_analysis/risk_extraction)가 계약검토 플로우와 미연결 — 능력은 있으나 소비처 없음
요구출처: 사양서 §9.1 — 'AI 1차 검토' 단계
현재상태: ai-summary 서비스에 clause_analysis·risk_extraction 태스크와 local Gemma 생성 + 인용 검증 경로가 실제로 구현되어 있으나, contract-intel의 process/rule-findings 흐름이나 어떤 웹 화면도 이 태스크를 계약 검토 단계로 호출하지 않는다. 룰엔진 finding과 AI 검토 결과를 결합하는 지점이 없다.
증거: packages/shared/src/ai/summary.ts:11-18; apps/api/src/modules/ai/features/ai-summary.service.ts:59-85; apps/api/src/modules/ai/features/ai-summary.service.ts:221-228
개선안: contract process 완료 이벤트에서 clause_analysis/risk_extraction을 자동 실행해 결과를 contract_review_findings(룰 finding과 동일 스키마의 ai_source 구분)로 저장하고, 계약 검토 화면에서 '룰 위반 + AI 소견 + 인용' 통합 패널로 노출. 사람 검토 승인 시 감사 이벤트(CONTRACT_AI_REVIEW_ACCEPTED)를 남겨 15단계의 'AI 1차 검토→변호사 검토' 전이를 데이터로 고정.

--- (medium/MISSING) DD/송무/계약 워크플로 산출물(보고서·바인더·서면) export 파이프라인 전무
요구출처: 사양서 §9.2 보고서 초안 생성·§9.1 Closing Binder·§10 Archive 'PDF/ZIP export'
현재상태: 저장소 전체에 워크플로 산출물 문서 생성(docx/pdf 합성) 코드가 없다. ingestion worker는 추출/변환(docx→pdf) 전용이며 역방향(구조화 데이터→문서) 생성기가 없다.
증거: workers/ingestion/app/converters/docx_to_pdf.py; apps/api/src/modules/dd/dd.service.ts:487-542
개선안: workers에 문서 합성 워커(python-docx 템플릿 엔진)를 추가하고, traceability/caseMap/clause 데이터를 입력으로 LDD 보고서·협상쟁점표·Closing Binder index를 생성해 documents 파이프라인으로 재유입(버전·감사 자동 획득). 장기적으로는 사내 스킬(ldd-report-generator류)과 동일한 템플릿 체계를 서버 측으로 이식.

====================================================================================================
[enterprise-platform] maturity=NOT_OPERATIONAL

구현요약:
플랫폼 기반층은 실질 구현이 있다. (1) 멀티테넌시: Shared DB + tenant_id + Postgres RLS가 전 테이블에 FORCE ROW LEVEL SECURITY로 적용되고(db/migrations/0001_initial_schema.sql, 0010_schema_migrations_rls.sql, 0061_enterprise_hardening.sql:157-162), 모든 쿼리가 app.current_tenant_id GUC를 세팅하는 tenantQuery/withTenantClient 경유(apps/api/src/common/db/tenant-query.ts, apps/api/src/modules/tenant/tenant.store.ts:17-34). (2) 비동기 파이프라인: pg-boss 기반 큐(트랜잭셔널 enqueue, retry+backoff, dead-letter)가 추출·인덱싱·AI-prep에 실사용(apps/api/src/modules/document/extraction/extraction-queue.service.ts, apps/api/src/modules/search/index/indexing.service.ts). (3) 스토리지: SigV4 직접 구현 S3 어댑터 + MinIO dev 스택(apps/api/src/modules/storage/s3-storage.adapter.ts, infra/docker-compose.dev.yml). (4) 관리자 콘솔: /enterprise 화면이 SSO/BYOK/SIEM/백업/컴플라이언스 + DMS 분류체계·매터템플릿·검색 refiner 관리 API에 실제 연결됨(apps/web/src/app/(app)/enterprise/enterprise-hardening-client.tsx, apps/web/src/lib/api/enterprise.ts, enterprise.controller 28개 라우트, 마이그레이션 0061/0082/0087-0089). (5) 마이그레이션: OneDrive 임포트 도구체인이 프로덕션 wave 011까지 실제 실행됨(tools/migration/onedrive-*.mjs 15종, git log 'production import wave 011'). (6) /metrics Prometheus 텍스트 포맷 엔드포인트(apps/api/src/common/metrics/metrics.controller.ts). (7) HWPX 텍스트 추출 파서(workers/ingestion/app/parsers/hwpx.py). 그러나 Phase 8 핵심 하드닝 항목(SSO 런타임, MFA, BYOK 암호화, SIEM 전송, 백업/DR, HA 배포)은 전부 '해시 기록 등록부(evidence ledger)' 패턴으로만 존재하고 실제 동작 로직이 없으며, infra/ci/prod-gate.yml은 deploy.enabled:false, staging-deploy.yml은 cloud_provider TBD로 프로덕션 배포 자체가 봉인 상태다. 따라서 차원 전체로는 NOT_OPERATIONAL.


--- (critical/STUB) SSO는 SAML 메타데이터 해시 등록부만 존재 — IdP 로그인 런타임(ACS/OIDC) 전무
요구출처: 사양서 §12.3(Azure AD/Google SSO), §20.1 보안 체크리스트, Phase 3/8
현재상태: enterprise_sso_providers 테이블에 idp_entity_id·sso_url_hash·certificate_fingerprint·metadata_hash 등 '해시'만 저장하고 activate는 status 컬럼만 변경한다. spMetadata()가 acsPath '/v1/auth/saml/acs'를 반환하지만 auth.controller에는 login/logout/me/password-reset 5개 라우트뿐 ACS 라우트가 없다. URL을 해시로 저장하므로 런타임 리다이렉트 자체가 불가능한 구조. OIDC(Azure AD/Google) 코드 흔적 없음(passport/openid-client 의존성 부재).
증거: apps/api/src/modules/enterprise/enterprise.service.ts:88-102 (sso_url_hash, metadata_hash만 저장); apps/api/src/modules/enterprise/enterprise.service.ts:337-373 (spMetadata가 존재하지 않는 acsPath 반환); apps/api/src/modules/auth/auth.controller.ts:40-85 (SSO 라우트 부재); apps/api/package.json:13-26 (인증 관련 의존성은 argon2뿐)
검증판정: CONFIRMED — 주장 근거 4개 인용을 모두 원문 대조로 확인했고, 반박 증거 수색에서 아무것도 나오지 않았다. (1) enterprise.service.ts:88-102의 SsoProviderRow는 sso_url_hash·certificate_fingerprint·metadata_hash 등 해시 필드만 보유하며 createSsoProvider는 클라이언트가 이미 해시한 값을 그대로 INSERT한다 — 서버는 실제 IdP URL·인증서를 한 번도 받지 않는다. (2) activateSsoProvider(294-335)는 status='active' UPDATE + 감사로그뿐이다. (3) spMetadata(337-373)는 acsPath '/v1/auth/saml/acs'를 하드코딩 문자열로 반환하며, shared 타입(enterprise-types.ts:420)에서 z.literal로 고정된 상수일 뿐이다. 전체 리포지토리의 *.controller.ts를 grep해도 saml 라우트 핸들러는 0건 — ACS 엔드포인트는 어디에도 존재하지 않는다. (4) auth.controller.ts는 login/logout/me/password-reset 2종의 정확히 5개 라우트뿐이다. (5) 의존성 검증: apps/api/package.json은 argon2뿐이고 pnpm-lock.yaml 전체에서 saml/passport/openid-client/ms
개선안: 1) openid-client 기반 OIDC 코드플로우(Azure AD, Google)를 auth 모듈에 추가하고 tenant별 IdP 설정을 암호화 저장(현재 해시 필드를 실값+KMS 암호화로 전환). 2) SAML2 ACS(/v1/auth/saml/acs) 구현 + 서명 검증 + 기존 세션 발급 경로(session.repository) 재사용. 3) enforcement_mode('password_disabled')를 login()에서 실제로 강제. 4) SCIM 2.0 프로비저닝으로 사용자 수명주기 자동화. 기존 등록부 테이블은 감사 레이어로 유지하되 런타임 설정 소스로 승격.

--- (critical/STUB) MFA 미구현 — mfa_enabled 사용자는 'mfa_not_available'로 로그인 자체가 차단되는 fail-closed 스텁
요구출처: 사양서 §12.3, §20.1(SSO/MFA 적용), Phase 3(MFA/SSO)
현재상태: users.mfa_enabled, sessions.mfa_verified 컬럼은 있으나 TOTP/WebAuthn 엔롤·챌린지 로직이 전무. MfaPolicy.evaluate()는 mfaEnabled=true인 사용자를 무조건 거부한다(MFA가 '없어서' 로그인 불가). 즉 스키마만 있고 기능은 음수 상태.
증거: apps/api/src/modules/auth/mfa.policy.ts:12-17 (mfaEnabled면 allowed:false, reason 'mfa_not_available'); db/migrations/0001_initial_schema.sql:44 (mfa_enabled 컬럼); db/migrations/0008_sessions.sql:8 (sessions.mfa_verified 미사용)
검증판정: CONFIRMED — 주장 근거 인용이 모두 정확하며, 반박 증거가 전혀 발견되지 않음. (1) apps/api/src/modules/auth/mfa.policy.ts:11-16에서 mfaEnabled=true면 무조건 {allowed:false, reason:'mfa_not_available'} 반환 — 인용 정확. 같은 모듈의 mfa.policy.spec.ts 테스트명이 'fails closed while TOTP is not implemented'로 코드베이스 스스로 스텁임을 명시. (2) auth.service.ts:105-125에서 거부 시 LOGIN_FAILURE 감사로그 후 authRequired(reason) throw — mfa_enabled 사용자는 로그인 자체가 차단됨(우회 경로 없음). (3) 리포지토리 전체(apps/api, apps/web, workers, packages, tools, db)에서 totp/webauthn/otpauth/speakeasy/otplib/fido/passkey/mfa_secret/mfa_challenge/backup_code/recovery_code grep 결과 코드 히트 0건 — docs/package/codex의 백로그·아키텍처 문서에만 계획으로 언급. MFA 엔롤·챌린지 엔드포인트가 어떤 컨트롤러에도 없음(auth.controller.ts:59는 mfaEnabled 플래그 에코만). (4) se
개선안: 1) TOTP(RFC6238) 엔롤/검증 엔드포인트 + 복구코드, 2) WebAuthn/패스키 추가, 3) 로그인 2단계 플로우(임시 세션 → MFA 검증 → sessions.mfa_verified=true 승격)로 기존 세션 모델에 연결, 4) 역할·테넌트 정책 기반 MFA 강제(firm_admin/security_admin 필수), 5) MFA 이벤트를 기존 audit_events 액션 체계에 추가. 스키마가 이미 있으므로 정책·플로우 구현만으로 승격 가능.

--- (critical/STUB) BYOK/테넌트별 암호화키: 키 '참조 해시' 등록부 + Noop 암호화 훅 — 데이터 경로에 테넌트 암호화가 전혀 없음
요구출처: 사양서 §20.1(tenant별 암호화 키 분리), §17.2 BYOK, Phase 8
현재상태: enterprise_key_references는 key_ref_hash·key_fingerprint 문자열만 저장하고 verify는 아무 암호학적 검증 없이 status='active'로 UPDATE. 스토리지 계층에는 EncryptionHook 인터페이스가 있으나 유일한 구현이 NoopEncryptionHook(encryptionKeyId: null, 바이트 그대로 통과). KMS/HSM 연동 코드 없음.
증거: apps/api/src/modules/storage/noop-encryption.hook.ts:11-24 (passthrough, encryptionKeyId null); apps/api/src/modules/enterprise/enterprise.service.ts:441-482 (verifyKeyReference가 상태만 변경); apps/api/src/modules/storage/encryption-hook.interface.ts (인터페이스만 존재); db/migrations/0061_enterprise_hardening.sql:170-204 (해시 필드 스키마)
검증판정: CONFIRMED — 모든 주장 근거를 원문 대조하고 리포지토리 전체를 적대적으로 수색했으나 갭 주장을 반박할 증거를 찾지 못했다.

(1) 인용 정확성 — 전부 일치:
- apps/api/src/modules/storage/noop-encryption.hook.ts:11-24 — beforePut은 body/contentLength/contentType을 그대로 반환하며 encryptionKeyId: null, afterGet도 body 그대로 반환. 순수 passthrough 확인.
- apps/api/src/modules/enterprise/enterprise.service.ts:441-482 — verifyKeyReference는 `UPDATE enterprise_key_references SET status='active', last_verified_at=now()` 후 감사로그만 기록. KMS 호출, 서명 검증, 테스트 암복호화 등 암호학적 검증 코드가 전무.
- apps/api/src/modules/storage/encryption-hook.interface.ts — 인터페이스 + DI 심볼(ENCRYPTION_HOOK)만 존재.
- db/migrations/0061_enterprise_hardening.sql — key_ref_hash·key_fingerprint는 64자 hex CHECK만 있는 참조 해시 필드. key_label CHEC
개선안: 1) envelope encryption 구현: 테넌트별 KEK(외부 KMS: AWS KMS/Azure Key Vault/로컬 HSM 추상화) + 파일오브젝트별 DEK, EncryptionHook.beforePut/afterGet에 AES-256-GCM 스트리밍 적용. 2) file_objects에 encryption_key_id 실기록(인터페이스에 이미 필드 존재). 3) 키 회전 잡(pg-boss 재사용)과 rotation_due_at 연동. 4) verifyKeyReference를 KMS Sign/Decrypt 왕복 검증으로 교체. 기존 등록부·감사 이벤트(BYOK_KEY_REFERENCE_CHANGED)는 그대로 활용.

--- (critical/NOT_OPERATIONAL) 백업·복구: RPO 15분/RTO 1시간 요구 대비 실제 백업이 없음 — row-count 매니페스트 기록이 백업을 대체
요구출처: 사양서 §13(RPO 15분/RTO 1시간), §20.1(백업·복구 리허설), Phase 8(고가용성·DR)
현재상태: createBackupSnapshot은 12개 테이블의 행 수를 세어 해시 매니페스트를 INSERT할 뿐 데이터 바이트를 어디에도 백업하지 않는다. tools/db에는 migrate/seed/schema-hash만 있고 pg_dump/WAL/PITR 도구 없음. scale 모듈의 migration_drills도 소요시간·해시를 자가 보고하는 ledger. 오브젝트 스토리지 복제/버전닝 설정도 없음.
증거: apps/api/src/modules/enterprise/enterprise.service.ts:566-625 (스냅샷=row count 매니페스트); apps/api/src/modules/enterprise/enterprise.service.ts:1613-1626 (tenantRowCounts가 count(*)만 수집); tools/db (check-migration-conventions/migrate/schema-hash/seed만 존재); apps/api/src/modules/scale/scale.service.ts:96-106 (MigrationDrillRow 자가보고 스키마)
검증판정: OVERSTATED — 코드 인용은 전부 정확함을 확인했다. (1) apps/api/src/modules/enterprise/enterprise.service.ts:566-625 createBackupSnapshot은 tenantRowCounts→hashJson→enterprise_backup_snapshots INSERT만 수행하며 데이터 바이트를 어디에도 복사하지 않는다. (2) 같은 파일 202-215행 backupTables는 정확히 12개 테이블이고 1613-1626행 tenantRowCounts는 테이블별 `SELECT count(*)`만 수집한다. (3) tools/db에는 check-migration-conventions.mjs/config.mjs/migrate.mjs/schema-hash.mjs/seed.mjs만 존재하고 pg_dump·pg_restore·pg_basebackup·wal-g·pgbackrest·PITR 참조는 리포 전체(코드·스크립트·CI·infra)에서 0건이다. (4) apps/api/src/modules/scale/scale.service.ts:95-104 MigrationDrillRow는 소요시간·schema hash를 자가 보고하는 ledger 스키마가 맞다. (5) enterprise.controller.ts에는 POST/GET /backups/snapshots만 있고 restore·verify 엔드포인트가 없으며
교정된 상태: 앱 내 백업 기능(createBackupSnapshot)은 12개 테이블 row-count 해시 매니페스트 기록뿐이며 데이터 바이트 백업·restore·verify 엔드포인트·예약 백업 잡·pg_dump/WAL/PITR 도구는 리포 어디에도 없다(주장 그대로). 단, 운영 계층에는 부분 구현이 존재한다: (a) 모든 production patch/마이그레이션 전 수동 RDS snapshot 생성이 배포 게이트로 강제되고 문서화됨(infra/ci/staging-deploy.yml pre_migration_backup_snapshot, docs/release/production-execution-preflight.md), (b) staging에서 RDS snapshot→restore→검증 리허설 1회 완료 및 LRB-012 승인(RESTORE-DRILL-AWS-001), (c) production S3 버전닝·암호화 활성화가 ledger에 기록됨(PROD-BACKUP-AWS-001) — 복제(cross-region replication)는 없음. 이 백업들은 배포 시점 이벤트 구동 수동 스냅샷이므로 RPO는 '마지막 배포 이후 경과 시간'이 되며, RPO 15분/RTO 1시간 목표는 리포에 정의조차 없고 자동화된 연속 백업(PITR/WAL 아카이빙/스케줄 스냅샷)의 코드·config 증거가 없어 충족을 입증할 수 없다.
개선안: 1) Postgres WAL 아카이빙 + PITR(pgBackRest/WAL-G)로 RPO 15분 달성, 2) 오브젝트 스토리지 버전닝+교차리전 복제, 3) 주기적 자동 복구 리허설 파이프라인(스테이징에 restore→schema-hash·row-count 대조→기존 backup_snapshots 테이블에 '검증됨' 증거 기록)으로 현재의 ledger를 실백업 검증 결과로 채움, 4) RTO 측정을 리허설 잡의 실측치로 자동 기록.

--- (critical/NOT_OPERATIONAL) 고가용성/프로덕션 인프라 부재 — 배포 파이프라인이 의도적으로 비활성(deploy.enabled:false), 클라우드 대상 미정
요구출처: 사양서 §11.1(Kubernetes/Terraform), §13(가용성 99.9→99.99%, 동시 10,000명)
현재상태: infra에는 dev용 docker-compose(postgres+minio+ingestion)만 있고 k8s 매니페스트/Terraform 없음. prod-gate.yml은 deploy.enabled:false, staging-deploy.yml은 deployment_enabled:false에 cloud_provider/region/registry/secrets 전부 TBD_APPROVAL_REQUIRED. 멀티AZ, LB, 오토스케일, 리드레플리카 등 가용성 장치 전무. 게이트 계약 문서 자체는 정교함.
증거: infra/ci/prod-gate.yml:40-43 (deploy.enabled: false); infra/ci/staging-deploy.yml:7-26 (deployment_enabled: false, cloud_provider: TBD_APPROVAL_REQUIRED); infra/docker-compose.dev.yml (dev 전용 단일 노드 스택); docs/current-code-state.md:50-53 (TECHNICAL-READY / EXTERNAL-EVIDENCE-REQUIRED 상태)
검증판정: OVERSTATED — 인용된 파일 자체는 정확하다: infra/ci/prod-gate.yml:40-42는 deploy.enabled: false, infra/ci/staging-deploy.yml:8,19-26은 deployment_enabled: false에 required_inputs 전부 TBD_APPROVAL_REQUIRED, infra/docker-compose.dev.yml은 dev 전용 단일노드 스택(postgres+minio+ingestion)이며, 리포지토리 전체에 k8s 매니페스트/Terraform/Helm/Pulumi가 전무하고(.tf, Chart.yaml 등 0건), 멀티AZ/오토스케일/리드레플리카 설정도 코드·IaC 형태로는 존재하지 않는다. .github/workflows/ci.yml에도 배포 잡은 없다.

그러나 "클라우드 대상 미정"과 "프로덕션 인프라 부재"라는 핵심 서술은 리포지토리 자체 기록과 정면 배치된다. (1) docs/release/launch-blocker-ledger.md:12-19에서 LRB-001~004, 008이 모두 approved 상태로 AWS Seoul ap-northeast-2, Amazon ECR, AWS Secrets Manager+KMS, CloudWatch/SNS가 확정되어 있다 — staging-deploy.yml의 TBD는 "이후 release-change PR로 wiring된다"는 의도
교정된 상태: 클라우드 대상은 미정이 아니라 AWS Seoul(ap-northeast-2)로 승인·확정되어 있고(LRB-001~004, 008 approved), 리포 기록상 스테이징과 프로덕션 모두 ECS Fargate + RDS + ALB + ECR + Secrets Manager/KMS + CloudWatch로 수동 프로비저닝·배포되어 "DEPLOYED - POST-LAUNCH MONITORING" 상태이며, 실 고객 문서 22,286건이 11개 import wave로 적재되고 ALB/ECS/RDS 알람 모니터링이 활성이다. 다만 이 모든 인프라는 IaC(Terraform/k8s) 없이 콘솔/수동 작업으로 관리되고 증거는 reference-only(엔드포인트·ARN 비공개)라 리포에서 재현·검증 불가하며, 자동 배포 파이프라인은 의도적으로 비활성(deploy.enabled:false, 수동 out-of-band 배포만 수행), 멀티AZ·오토스케일·리드레플리카 등 명시적 HA 구성 증거는 실제로 전무하다. 즉 갭의 실체는 "프로덕션 인프라 부재"가 아니라 "IaC·자동화 파이프라인 부재 + HA 구성 미확인의 수동 운영 프로덕션"이다.
개선안: 1) Terraform으로 managed Postgres(HA, 멀티AZ) + k8s(EKS/AKS) + 오브젝트 스토리지 프로비저닝, 2) api/web/ingestion Dockerfile은 이미 있으므로 Helm 차트 + HPA + PodDisruptionBudget 작성, 3) staging-deploy.yml의 TBD 입력을 실값으로 채우는 release-change PR 절차 실행, 4) SLO 99.9% 계측(에러버짓)을 관측성 스택과 연동. 순서: staging 실배포 → 외부 스모크(기존 tools/release/staging-smoke.mjs 재사용) → prod-gate 활성화.

--- (high/STUB) SIEM 연동: 이벤트 전송 로직이 없는 'export 기록'만 존재 — status가 'recorded' 단일값
요구출처: 사양서 Phase 8(SIEM 연동), Level 5(SIEM)
현재상태: createSiemExport는 audit_events의 seq 범위·건수를 세어 매니페스트 해시를 INSERT할 뿐, syslog/webhook/s3 어느 sink로도 바이트를 내보내지 않는다. endpoint는 해시로만 저장되어 실제 목적지를 알 수도 없고, 상태 enum이 'recorded' 하나뿐이라 전송 성공/실패 개념 자체가 없다. 지속 스트리밍(CDC)도 없음.
증거: apps/api/src/modules/enterprise/enterprise.service.ts:484-546 (INSERT만 수행); apps/api/src/modules/enterprise/enterprise.service.ts:117-127 (SiemExportRow.status: 'recorded' 고정); db/migrations/0061_enterprise_hardening.sql:210-234 (endpoint_hash 컬럼)
검증판정: CONFIRMED — 주장 근거 3곳을 모두 원문으로 검증했고 전부 정확하다. (1) apps/api/src/modules/enterprise/enterprise.service.ts:484-546의 createSiemExport는 auditBounds()로 audit_events의 seq 범위·건수를 집계하고 manifest 해시를 계산해 enterprise_siem_exports에 INSERT + 감사로그(SIEM_EXPORT_RECORDED)만 수행한다. HTTP/소켓/S3 호출 코드가 전혀 없다. (2) status는 3중으로 'recorded' 단일값이다: 서비스 타입(enterprise.service.ts:125 status: 'recorded'), 공유 zod 스키마(packages/shared/src/enterprise/enterprise-types.ts:479 z.literal('recorded')), DB 제약(db/migrations/0061_enterprise_hardening.sql: CHECK (status = 'recorded')). 이후 마이그레이션 0062~0097 어디에서도 이 enum을 확장하지 않았다. (3) endpoint_hash는 char(64) hex 해시만 저장하며 요청 DTO도 endpointHash(hash64Schema)만 받으므로 실제 목적지 URL은 시스템 어디에도 존재하지 않는다. 반증 수색 결과: a
개선안: 1) audit_events에 대한 outbox/체크포인트 테이블 + pg-boss 딜리버리 워커로 syslog(RFC5424)/HTTPS webhook/S3 배치 전송 구현, 2) sink 설정은 실 endpoint를 암호화 저장(BYOK 구현과 연계), 3) 전송 상태머신(pending→delivered/failed, 재시도·백프레셔), 4) OCSF/CEF 매핑 레이어. 기존 seq 기반 매니페스트는 무결성 증빙으로 재활용 가능.

--- (high/NOT_OPERATIONAL) M365 Graph API 실연동 부재 — Outlook 통합은 정책·게이트·스키마는 깊으나 Graph 전송체가 '항상 거부' 스텁
요구출처: 사양서 §12.3(Microsoft 365: Outlook/Graph API), Phase 2(Outlook add-in)
현재상태: outlook 모듈은 12개 라우트, filing request, folder mapping, send-file, 문서삽입, 롤아웃 링(R0~R3) 게이트까지 정교하게 구현됐지만, 첨부 획득의 실제 전송체 기본 구현이 DisabledOutlookGraphAttachmentTransport로 'integration_gate_closed'를 무조건 반환한다. 리포지토리 전체에 graph.microsoft.com 호출 코드가 없음(데스크톱 origin guard 제외). MSAL/토큰 교환 클라이언트도 없음.
증거: apps/api/src/modules/outlook/outlook-graph-attachment-transport.ts:37-45 (Disabled 구현이 기본 바인딩); apps/api/src/modules/outlook/outlook-operational-gate.ts:1-60 (게이트·링 체계는 실구현); db/migrations/0071_create_outlook_auth_graph_gate.sql (게이트 스키마)
검증판정: CONFIRMED — 갭 주장의 모든 핵심 근거가 정확히 검증됨. (1) apps/api/src/modules/outlook/outlook-graph-attachment-transport.ts:37-45의 DisabledOutlookGraphAttachmentTransport.acquire()는 입력을 무시(void input)하고 무조건 {status:'denied', reasonCode:'integration_gate_closed'}를 반환하는 스텁이며, outlook.module.ts:52-55에서 OUTLOOK_GRAPH_ATTACHMENT_TRANSPORT 토큰에 useExisting으로 하드 바인딩됨(환경변수 조건부 교체 코드 없음, 토큰 참조 파일은 리포지토리 전체에서 3개뿐 — 모두 outlook 모듈 내부). (2) 미커밋 변경 포함 워킹트리 전체 grep에서 graph.microsoft.com 문자열이 단 한 건도 없음 — 주장이 예외로 둔 데스크톱 origin guard(apps/desktop/src-tauri/src/origin_guard.rs)조차 login.microsoftonline.com만 허용 목록에 있고 graph 도메인은 없어 주장보다도 보수적. (3) 어떤 package.json에도 @azure/msal*, @microsoft/microsoft-graph-client 의존성이 없고, 애드인 프론트엔드(apps/web/s
개선안: 1) Azure AD 앱 등록 + MSAL OBO(on-behalf-of) 토큰 교환 서비스 구현, 2) OutlookGraphAttachmentTransport의 실구현(Graph /messages/{id}/attachments 스트리밍 → 기존 file_objects 저장 경로) 주입, 3) 기존 운영 게이트(EV-OUTLOOK-002/003 증거 참조)를 그대로 통과 조건으로 사용해 링 단위 활성화, 4) 웹훅 구독(delta query)으로 자동 파일링 확장. 인터페이스·게이트가 이미 완비되어 전송체 구현만으로 가동 가능.

--- (high/MISSING) 국내 법률/공시 데이터 연동(법제처·국가법령정보센터·대법원·DART·KRX) 전무
요구출처: 사양서 §12.3 외부 연동(국내 법률/공시 데이터)
현재상태: 리포지토리 전체(apps/api, packages, tools)에 법제처/law.go.kr/대법원/scourt/DART/KRX 관련 코드·설정·커넥터가 단 한 건도 없다.
증거: 전역 grep '법제처|대법원|DART|KRX|law.go.kr|scourt' 무결과 (apps/, packages/, tools/ 전체)
검증판정: CONFIRMED — 반박 시도가 실패했으며, 갭 주장은 정확하다. 검증 결과: (1) 주장 근거 재현 — apps/, packages/, tools/ 전체에 대해 '법제처|대법원|DART|KRX|law.go.kr|scourt' grep을 재실행한 결과 실질적 매치 0건 (유일한 매치는 'requiredArtifact' 식별자 내 'dArt' 부분문자열과 .next 빌드 산출물의 오탐뿐). workers/, db/migrations, infra/, deploy/, .env.example, apps/api/src/modules(integrations, dd, litigation 포함 전 모듈)에도 go.kr/opendart/fss/krx 관련 엔드포인트, API 키, 커넥터, 환경변수가 전무하다. (2) 리포지토리 자체 문서가 갭을 자인 — docs/package/codex/80_Open_Items.md:12 (OPEN-02): "국내 법률DB 연동 source(원천 Q-004) — 연동 없음. authorities 테이블(scope global|tenant)은 스키마만 존재. 사양에 없는 외부 API 호출은 절대 금지". (3) 그 authorities 테이블조차 실제로는 존재하지 않음 — docs/package/07_Data_Model_and_Schema_Draft.md:43의 설계 초안 문서에만 있고, db/migrations 전체(0000~0087)
개선안: 1) integrations 모듈에 커넥터 프레임워크(인증·레이트리밋·스키마 정규화 공통층)를 추가하고, 2) 국가법령정보 공동활용 Open API·DART OpenAPI·대법원 종합법률정보 순으로 커넥터 구현, 3) 수집 데이터를 knowledge/graph 계층(기존 graph_nodes/graph_edges)에 인용 가능 엔터티로 적재, 4) pg-boss 주기 잡으로 증분 동기화. 계약 인텔리전스·송무 모듈의 판례/법령 연결 요구의 선행 작업.

--- (high/NOT_OPERATIONAL) 성능·확장성 목표가 자가보고 ledger로만 관리 — 실부하 테스트 수단 없음, /scale 콘솔은 production hidden
요구출처: 사양서 §13(페이지 2초, 동시 100→10,000명, 인덱스 샤딩), Phase 8
현재상태: scale 모듈은 p50/p95/p99를 '입력값으로 받아' INSERT하는 성능 기록부이며 측정 자체를 수행하지 않는다. tools/bench는 로컬 AI 모델 벤치 전용. k6/Gatling류 부하 하니스, 인덱스 샤딩, 오토스케일 없음. /scale 웹 라우트는 production 'hidden'으로 봉인. 업로드는 기본 200MB(1GB는 migration 소스 경로만 허용).
증거: apps/api/src/modules/scale/scale.service.ts:130-180 (입력된 p95로 pass/fail 판정 후 INSERT); apps/web/src/lib/features.ts:149-155 ('/scale' production: 'hidden'); apps/api/src/modules/document/validators/file-size.validator.ts:3-4 (기본 200MB, 마이그레이션만 1GB); tools/bench (local-model-bench만 존재)
개선안: 1) k6 기반 부하 시나리오(검색·업로드·동시세션)를 CI 야간 잡으로 돌리고 결과를 기존 scale_performance_runs에 자동 기록(자가보고→실측 전환), 2) 1GB 업로드를 일반 경로에서도 지원하도록 tus/S3 multipart 재개 업로드 도입, 3) 읽기 부하용 Postgres 리드레플리카 + pgbouncer, 4) document_search_index 파티셔닝(tenant_id 해시) 등 §13의 샤딩 요구 반영, 5) 목표 달성 후 /scale 콘솔 노출.

--- (medium/SHALLOW) 관측성: 수제 in-memory 메트릭 레지스트리 — OTel 트레이싱·알림 없음, observations 배열 무한 증가
요구출처: 사양서 §11.1(Prometheus, Grafana, OpenTelemetry)
현재상태: /metrics 엔드포인트는 존재하고 Prometheus 텍스트 포맷을 직접 렌더링하지만, prom-client도 OTel SDK도 없는 수제 구현이다. MetricsRegistry.observe()가 모든 요청을 배열에 push하고 reset() 외 정리가 없어 장기 운영 시 메모리 누수 구조. 분산 트레이싱, 로그 상관관계, Grafana 대시보드, Alertmanager 부재. 구조화 로깅(pino)은 있음.
증거: apps/api/src/common/metrics/metrics.middleware.ts:36-44 (observations 배열 무한 push); apps/api/src/common/metrics/metrics.controller.ts:9-15; apps/api/package.json:13-26 (prom-client/@opentelemetry 부재); grep 'prometheus|opentelemetry' 전역 무결과 (라이브러리 기준)
개선안: 1) prom-client로 교체(고정 버킷 히스토그램, 카디널리티 제한)해 메모리 문제 해소, 2) @opentelemetry/sdk-node로 HTTP·pg·pg-boss 자동계측 + trace_id를 pino 로그에 주입, 3) ingestion 워커(FastAPI)에도 OTel 계측 추가해 업로드→추출→인덱싱 전 구간 트레이스 연결, 4) Grafana 대시보드(§13 목표치 기준 SLO 패널) + Alertmanager 룰을 infra에 코드로 포함.

--- (medium/MISSING) 데이터 마이그레이션: OneDrive 외 소스(파일서버/SharePoint/iManage/NetDocuments/PST) 및 '마이그레이션 AI 8종' 부재
요구출처: 사양서 §14.1(대상 소스), §14.3(마이그레이션 AI 8종)
현재상태: OneDrive 마이그레이션은 dry-run→승인→쓰기→롤백·closeout까지 갖춘 도구체인이 있고 프로덕션 wave 011까지 실제 실행된 반면, 다른 소스용 도구는 전무하다. 매핑도 결정론적 규칙 검증(매니페스트의 matter_code 형식 검사 등)이며 §14.3의 AI 추정(파일명 기반 Matter 추정, 본문 기반 고객 추정, 중복 병합 후보 등) 8종은 구현되지 않았다.
증거: tools/migration/ (onedrive-*, matter-app-* 만 존재); tools/migration/onedrive-import-target-resolution.mjs:93-102 (결정론적 blocker 규칙); grep 'pst|sharepoint|imanage|netdocuments' 무결과; git log: 'docs(release): record production import wave 011' (OneDrive는 실운영 실적 있음)
개선안: 1) OneDrive 파이프라인의 스테이지(스코프→매니페스트→dry-run→승인→쓰기→closeout)를 소스 어댑터 인터페이스로 일반화, 2) SMB/파일서버 크롤러와 PST 파서(readpst/pypff) 어댑터 추가, 3) 기존 로컬 AI 인프라(Gemma 기반 ai-prep, local-embedding)를 재사용해 §14.3의 매핑 제안(파일명→Matter, 이메일 도메인→Matter, 중복 병합 후보)을 '제안+휴먼 승인 워크북' 형태로 삽입 — 승인 게이트 체계가 이미 있으므로 AI 제안층만 얹으면 됨.

--- (medium/SHALLOW) 멀티테넌시 격리 티어 상향 경로·테넌트 라이프사이클 관리 부재 (Shared DB 티어는 견고)
요구출처: 사양서 §11.3(Shared DB → Schema per Tenant → DB per Tenant → Dedicated)
현재상태: Shared DB + RLS는 FORCE RLS·GUC 주입까지 모범적으로 구현되어 있으나, 그 위 단계가 없다: 테넌트 생성/정지/오프보딩 API 부재(tenant.controller는 settings/workspaces 조회 3개뿐), 테넌트별 커넥션 라우팅·schema/DB per tenant 옵션 없음, 테넌트별 벡터 인덱스 분리(§13) 없음. tenants 테이블에 region/data_residency 컬럼은 있으나 라우팅에 미사용.
증거: apps/api/src/modules/tenant/tenant.controller.ts:16-34 (읽기 3개 엔드포인트); apps/api/src/modules/tenant/tenant.store.ts:17-34 (RLS GUC 주입은 실구현); db/migrations/0061_enterprise_hardening.sql:157-162 (FORCE RLS); apps/api/src/modules/tenant/tenant.entity.ts (region/dataResidency 필드)
개선안: 1) 테넌트 프로비저닝/정지/오프보딩(데이터 export 포함) 관리 API + 슈퍼어드민 콘솔, 2) DATABASE_URL 단일 상수(각 서비스에 하드코딩된 기본값) 대신 테넌트→커넥션풀 라우팅 레이어를 도입해 대형 고객의 DB-per-tenant 승격 경로 확보, 3) pgvector 인덱스를 tenant 파티션으로 분리, 4) data_residency 값을 스토리지 버킷/리전 선택에 실제 반영.

--- (medium/MISSING) Cold storage/스토리지 계층화 미구현
요구출처: 사양서 §11.2(Cold Archive), §13(cold storage 자동 이동)
현재상태: 단일 S3 버킷만 사용하며 라이프사이클 정책, 스토리지 클래스 전환, 아카이브 티어 접근 흐름 관련 코드가 전혀 없다. records 모듈의 archive는 거버넌스 레코드 생성일 뿐 바이트 이동이 없음.
증거: grep 'cold|glacier|archive.tier' 전역 무결과; apps/api/src/modules/storage/s3-storage.adapter.ts:15-21 (단일 bucket 설정); apps/api/src/modules/records/records.controller.ts:80 (archives는 레코드 생성)
개선안: 1) file_objects에 storage_class·last_accessed_at 컬럼 추가, 2) pg-boss 주기 잡으로 종결 Matter/보존단계 문서를 아카이브 버킷(또는 S3 lifecycle로 GLACIER 클래스)으로 이동, 3) 아카이브 문서 다운로드 시 복원 대기 UX + 감사 이벤트, 4) records 모듈의 archive 액션과 실제 바이트 이동을 연결.

--- (medium/SHALLOW) HWP 바이너리(.hwp) 파싱 미지원 — HWPX만 텍스트 추출 가능
요구출처: 사양서 §18(한글/HWP 처리: HWP/HWPX, OCR, 문단번호 인식 별도 개발항목)
현재상태: 확장자 검증은 .hwp 업로드를 허용하지만 ingestion 워커가 'UNSUPPORTED_HWP_BINARY'로 즉시 실패 처리한다. HWPX는 ZIP/XML 섹션 텍스트 추출이 실구현되어 있으나 문단번호 인식·별지/첨부 구분·OCR 폴백은 없다.
증거: workers/ingestion/app/extract_router.py:31 (UNSUPPORTED_HWP_BINARY); workers/ingestion/app/parsers/hwpx.py:25-50 (HWPX 실구현); apps/api/src/modules/document/validators/file-extension.validator.ts (.hwp 허용)
개선안: 1) hwp5 포맷 파서(olefile 기반 pyhwp 계열 또는 hwp→hwpx 변환 서비스)를 ingestion 워커 파서로 추가, 2) 추출 실패 시 PDF 변환→OCR 폴백 체인 구성, 3) 한국 법률문서 특화 후처리(문단번호·조항 구조, 별지/첨부 분리)를 chunking 단계에 추가, 4) 기존 extraction 상태머신(failed reason code)이 잘 되어 있으므로 파서 등록만으로 통합 가능.

--- (low/QUALITY) 이벤트 비동기 처리: pg-boss 실구현이나 워커가 API 프로세스 내장 + 환경변수 기본 비활성 — 독립 워커 배포 형태 아님
요구출처: 사양서 §11.1(Queue: Kafka/RabbitMQ/SQS), §13(이벤트 기반 비동기 처리)
현재상태: 트랜잭셔널 enqueue·재시도·DLQ를 갖춘 pg-boss 큐가 추출/인덱싱/AI-prep에 실사용되는 것은 실질 구현이다. 다만 큐 워커가 EXTRACTION_QUEUE_WORKER_ENABLED 플래그(기본 off)로 API 프로세스 안에서 구동되어, 워커 스케일아웃·격리가 불가능하고 플래그 미설정 배포에서는 잡이 적체된다. 수천만 건급 이벤트 처리를 위한 파티셔닝·스트림 처리 경로 부재.
증거: apps/api/src/modules/document/extraction/extraction-queue.service.ts:17-21 (workerEnabled 기본 false); apps/api/src/modules/document/extraction/extraction-queue.service.ts:53-63 (트랜잭셔널 enqueue — 잘 구현됨); apps/api/package.json:22 (pg-boss 의존성)
개선안: 1) 큐 워커를 별도 디플로이먼트(전용 Node 프로세스)로 분리하고 API에서는 enqueue만 수행, 2) 큐 깊이·처리율·DLQ 카운트를 /metrics에 노출해 알림 연결, 3) 이메일 수천만 건 규모 도달 시 pg-boss→스트림 브로커(Kafka/Redis Streams) 전환을 위한 프로듀서 추상화를 지금 도입, 4) 워커 동시성·batchSize를 부하 테스트 결과로 튜닝.

--- (low/MISSING) API 완비성: §12.2 예시 엔드포인트 중 문서 버전 비교(POST /documents/{id}/compare) 부재
요구출처: 사양서 §12.2(POST /documents/{id}/compare, 문서 비교 100페이지 30초)
현재상태: §12.2의 다른 엔드포인트(matters, documents/versions, search, ethical-walls, audit/events, external-links, retention/disposal, ai, clauses)는 대응 라우트가 존재하나 버전 비교 엔드포인트와 비교 엔진(diff/redline)이 없다.
증거: grep 'compare' apps/api/src/modules --include=*.controller.ts 무결과; apps/api/src/modules/document/document.controller.ts:230-251 (버전 생성/목록은 존재); apps/api/src/modules/records/records.controller.ts:48-112 (retention/disposal 존재); apps/api/src/modules/ethical-wall/ethical-wall.controller.ts (윤리장벽 존재)
개선안: 1) ingestion 워커에 문서 비교 잡(docx: python-docx 기반 구조 diff, PDF: 텍스트 레이어 diff) 추가, 2) POST /documents/{id}/compare → pg-boss 비동기 잡 → 비교 아티팩트(document_preview_artifacts 패턴 재사용) 저장, 3) 웹 문서 액션 센터에 나란히 보기/redline 뷰 연결, 4) §13의 '100페이지 30초' 목표를 성능 기록에 실측 등록.

====================================================================================================
[completeness-critic]
요약: 두 컨셉 문서를 전수 대조한 결과, 10개 차원 다이제스트가 놓친 요구 영역은 대부분 리포지토리에 코드가 전혀 없음을 확인했다. 힌트로 제시된 항목 중 다수는 실제로는 이미 커버되었거나 문서에 없는 요구였다. (1) 조직 디렉토리: apps/api/src/modules/org-directory/org-directory.service.ts에 user/group 검색(권한·감사 결합)이 실구현되어 있어 놓친 갭이 아님. (2) 알림: notifications 모듈(db/migrations/0086_create_dms_notifications.sql, /notifications 가시 라우트)이 존재하고 workflows-collab 다이제스트가 이미 다룸. (3) 마이그레이션 AI 8종·Obsidian/LLM Wiki export·한국 특화(HWP·법령판례·DART)·데스크톱 Obsidian 역할은 각각 enterprise-platform, knowledge-graph, document-vault/search 다이제스트의 기존 finding으로 커버됨. (4) 데스크톱 앱/PWA는 컨셉 문서 어디에도 요구로 존재하지 않아 finding 대상이 아님(권장 스택은 React/Next.js 웹 + Word/Outlook add-in). 반면 어느 차원에서도 다뤄지지 않은 미구현 요구 7건을 확인했다: 작성·정합성 검토 워크플로(SHA 작성/의견서/회의록 QC — 관련 코드 grep 0건, meeting/transcript는 apps/api/src/modules/document/filename-metadata.parser.ts의 파일명 토큰 1건뿐), Analytics 계층(modules 목록에 analytics 부재, apps/api/src/modules/dashboard/dashboard.service.ts는 DMS work-item 카운트 전용), SaaS 상용화 계층(billing/subscription/metering/plan/residency grep 실질 0건 — db/migrations/0062_scale_learning.sql 주석과 permission.service.spec.ts 테스트 속성명뿐), Public API 표면(api_key/oauth 0건, webhook은 db/migrations/0061_enterprise_hardening.sql:213 SIEM sink_type enum 값으로만 존재), Word Add-in(apps/web/public/outlook-addin/manifest.xml이 MailApp 전용, Document host 코드 0건), 문서 태그(tags 컬럼/테이블 0건, 가상 폴더는 0083 governed saved-search만 존재), 협업·전자서명 커넥터(docusign/slack/teams/zoom/sharepoint/google-drive grep 0건). 이 미커버 요구 집합 전체로는 구현이 사실상 전무하여 NOT_IMPLEMENTED.


--- (high/MISSING) 작성(Drafting)·정합성 검토 워크플로 축 전면 부재 — SHA 작성, 법률의견서 작성, 회의록 정합성 검토(Meeting Minutes QC)가 어느 차원에서도 미커버
요구출처: concept-obsidian-llm.txt §6.2(SHA 작성 Workflow: 당사자 구조 추출~초안 생성), §6.4(회의록 정합성 검토 Workflow: 회의록 claim-녹취록-계약서 대조), §5.11(Negotiation Ledger의 Transcript/MeetingNote 노드), 제7장 v2.0 §7.2(Workflow Template 20종 중 Legal Opinion, Meeting Minutes QC, Billing Narrative); concept-spec.txt §4 Workflow 레이어('계약검토, DD, 송무, 의견서')
현재상태: workflows-collab 다이제스트는 계약검토/DD/송무/External Portal 4축만 분석했고, 컨셉 문서의 나머지 워크플로 축인 SHA 등 계약서 작성(drafting), 법률의견서 작성, 회의록-녹취록-계약서 정합성 검토는 어떤 다이제스트에도 등장하지 않는다. 리포지토리에도 관련 코드가 전무하다: transcript/meeting minutes/회의록/녹취 grep 결과 apps/api/src/modules/document/filename-metadata.parser.ts의 파일명 토큰 1건뿐이고, SHA/주주간계약/legal opinion drafting 코드는 0건이며, work_product는 db/migrations/0027의 privilege_status enum 값으로만 존재한다. AI 생성은 요약 3태스크에 한정된다(ai-governance 다이제스트 확인).
증거: grep -rniE 'transcript|meeting[_-]?(note|minute)|회의록|녹취' apps/api/src apps/web/src workers → apps/api/src/modules/document/filename-metadata.parser.ts 1건 (파일명 메타데이터 토큰); grep -rniE 'work[_-]?product|legal[_-]?opinion|drafting' apps/api/src db/migrations → db/migrations/0027_extend_documents_metadata.sql:29 privilege_status enum 값뿐; grep -rniE 'shareholders?[_-]?agreement|주주간' apps/api/src packages → 0건; workflows-collab 다이제스트 구현요약: '4개 워크플로 축' = contract-intel/dd/litigation/external — 회의록 QC·SHA·의견서 축 미언급
개선안: 갭 분석에 '산출물 작성·정합성 검토' 축을 별도 차원 또는 workflows-collab 하위 항목으로 추가하라. 구현 우선순위는 (1) 회의록 claim 추출-녹취록-계약서 대조(Conflict Detector 실사용처, 컨셉 문서의 대표 시나리오), (2) WorkProduct 초안(의견서/서면) 생성 파이프라인의 evidence-pack 재사용, (3) SHA 지배구조 설계 워크플로 순이 컨셉 문서 우선순위와 부합한다.

--- (medium/MISSING) Analytics 계층(사용량·쟁점빈도·재사용률·생산성/품질/지식자산 지표) 전무 — 아키텍처 12개 레이어 중 유일하게 갭 분석에서 누락
요구출처: concept-spec.txt §4 아키텍처 'Analytics — 운영·지식 활용 분석: 사용량, 쟁점빈도, 재사용률'; concept-obsidian-llm.txt v2.0 제13장 제품 분석 및 운영지표(생산성 지표 10·품질 지표 10·지식자산 지표 10), §9.3 AI 사용량 관리(월별/Matter별/사용자별 사용량·비용 사용자 노출)
현재상태: 10개 차원 어디에도 Analytics 계층 finding이 없다. 리포지토리에 analytics 모듈이 존재하지 않으며(apps/api/src/modules 목록에 부재), apps/api/src/modules/dashboard/dashboard.service.ts(495줄)는 DMS work-item 상태 카운트 전용, /metrics는 인프라 Prometheus 텍스트, ai-ops는 로컬 AI 헬스 카운터다. 재사용률/쟁점빈도/생산성/AI 사용량 리포트에 해당하는 테이블·서비스·UI가 전무하고, 웹 /admin 라우트도 account-ledger와 security뿐이다.
증거: ls apps/api/src/modules → analytics 부재 (ai, audit, dashboard, ... work 32개 모듈 중 없음); apps/api/src/modules/dashboard/dashboard.service.ts — pending/completed/blocked 등 work-item 카운트 쿼리만 존재; grep -rniE '재사용|reuse[_-]?rate|issue[_-]?frequency|productivity' apps/web/src → search-save-panel.tsx 1건(무관); find apps/web/src/app/'(app)'/admin → account-ledger·security 페이지뿐
개선안: 갭 분석 차원 목록에 Analytics를 추가하거나 enterprise-platform 차원에 명시적 finding으로 편입하라. 구현은 기존 append-only 감사로그(0006)와 ai_sessions(0050)를 소스로 하는 읽기 전용 집계 뷰부터 시작하면 신규 수집 인프라 없이 사용량·AI 승인/반려율·검색 사용률 지표를 도출할 수 있다.

--- (medium/MISSING) SaaS 상용화 계층(플랜·과금·사용량/AI 토큰 미터링·마켓플레이스·데이터 레지던시) 전무
요구출처: concept-obsidian-llm.txt v2.0 §2.8(SaaS Operations and Marketplace Layer: Subscription Plan/Usage Metering/AI Token Metering/Billing Integration/Marketplace 20항목), 제9장 제품 패키징 및 가격체계(Starter~Private/Sovereign 4플랜, 과금 기준 10종), §3.1.3(데이터 레지던시 7개 리전); concept-spec.txt §19 Level 5('글로벌 데이터 레지던시')
현재상태: enterprise-platform 다이제스트는 SSO/BYOK/SIEM/백업/HA/관측성/마이그레이션을 다뤘으나 상용 SaaS 운영 계층(플랜, 과금, 미터링, 마켓플레이스, 레지던시)은 언급하지 않는다. 리포지토리에 subscription/billing/metering/plan 코드가 실질 0건이고(0062 마이그레이션의 'no billing secret' 주석과 permission.service.spec.ts의 'matter.billing_rate' 테스트 속성명뿐), tenant 스키마에 plan/tier/region 컬럼이 없으며, residency 검색 결과도 0건이다. 마켓플레이스/커넥터 프레임워크 부재.
증거: grep -rniE 'billing|timesheet|invoice|subscription|usage[_-]?meter|token[_-]?meter' apps/api/src apps/web/src db/migrations → 실코드 0건 (db/migrations/0062_scale_learning.sql:300 주석, apps/api/src/modules/permission/permission.service.spec.ts:167 테스트 속성뿐); grep -iE 'plan|tier|region' db/migrations/0001_initial_schema.sql의 tenant 정의 → 0건; grep -rn 'residency' apps/ db/ → dist 산출물 외 소스 0건; grep -rniE 'marketplace' apps/api/src db/migrations → 0건
개선안: 이 계층은 v2.0 문서의 Stage 6(Knowledge and Marketplace) 및 Enterprise 플랜 요건이므로 즉시 구현 대상은 아니나, 갭 등록부에는 별도 항목으로 기록해야 한다. 특히 데이터 레지던시와 AI 사용량 미터링은 tenant 스키마·ai_sessions 스키마 설계에 소급 비용이 크므로 컬럼 예약(tenant.region, ai_sessions 토큰 카운트)만이라도 선행할 가치가 있다.

--- (medium/MISSING) Public API 플랫폼 표면 부재 — 머신 클라이언트 인증(API Key/OAuth)과 이벤트 Webhook이 전무
요구출처: concept-obsidian-llm.txt v2.0 §8.3(Public API 12종), §8.4(Webhook 이벤트 15종: Matter created, Document uploaded, Conflict detected, Ethical wall breach attempted 등); concept-spec.txt §12.1(Integration API)
현재상태: enterprise-platform 다이제스트의 API 완비성 finding은 문서 버전 비교 엔드포인트 1건만 지적했고, 외부 시스템이 기계적으로 연동할 수 있는 표면(API Key/OAuth 클라이언트 인증, 아웃바운드 이벤트 Webhook)의 전면 부재는 어느 차원도 다루지 않았다. 리포지토리의 인증은 세션 쿠키 단일 방식이며(api_key/oauth grep 0건), 'webhook'은 db/migrations/0061_enterprise_hardening.sql:213과 apps/api/src/modules/enterprise/enterprise.service.ts:119의 SIEM sink_type enum 값으로만 존재하고 실제 디스패치 로직이 없다(SIEM 자체도 enterprise 다이제스트가 'recorded 단일값' 스텁으로 확인).
증거: grep -rniE 'api[_-]?key|personal[_-]?access[_-]?token|oauth' apps/api/src db/migrations → 0건; grep -rni 'webhook' apps/api/src db/migrations → enterprise.service.ts:119, 0061_enterprise_hardening.sql:213 (SIEM sink_type enum 값 2건뿐); apps/api/src/modules/auth — 세션 기반 인증만 존재
개선안: 테넌트 스코프 API Key(해시 저장·권한 서브셋·감사 결합)와 pg-boss 기반 아웃바운드 webhook 디스패처(재시도·서명 헤더)를 갭 등록부에 추가하라. 기존 감사이벤트 allowlist가 이미 이벤트 카탈로그 역할을 하므로 webhook 이벤트 매핑 비용은 낮다.

--- (medium/MISSING) Word Add-in(작성 환경 내 조항은행 검색·삽입, citation 삽입, Style Bible, AI 통제) 요구가 미커버 — document-editing 갭과 별개의 제품 표면
요구출처: concept-obsidian-llm.txt v2.0 §4.3(Word Add-in 20개 기능: Clause Library 검색, 유사 조항 삽입, citation 삽입, 정의어/조항번호/cross-reference 검토, Firm Style Bible 비교, track changes 삽입, 선택 범위만 AI 처리), §15.5 Stage 4, 제17장 '여섯째, Word Add-in과 Outlook Add-in은 필수'
현재상태: document-editing 다이제스트는 'Word/Office 네이티브 편집·co-authoring 부재'(파일 편집 경로)를 critical로 다뤘으나, Word 안에서 Vault 지식(조항은행·유사조항·citation·스타일)을 소비·삽입하는 add-in 제품 표면은 어느 차원에도 없다. 리포지토리의 Office add-in은 apps/web/public/outlook-addin/manifest.xml의 xsi:type=MailApp(Outlook 메일함 호스트) 단일이며, Word(Document host) 매니페스트·태스크페인·API가 전혀 없다. 조항은행 백엔드(listClauseBank)는 존재하나 소비처가 봉인된 /contracts 웹 라우트뿐이다.
증거: apps/web/public/outlook-addin/manifest.xml:7 xsi:type="MailApp", :21 <Host Name="Mailbox"/> — Word Document host 부재; grep -rniE 'word[_-]?add[_-]?in|Office\.HostType' apps/web/src → Word 호스트 코드 0건 (매치는 무관한 테스트 파일); knowledge-graph 다이제스트: 조항은행이 'matter 범위 해시/오프셋 목록' — Word 삽입 가능한 형태 아님
개선안: 갭 등록부에 Word Add-in을 Outlook add-in과 대칭되는 별도 항목으로 추가하라. 컨셉 문서는 이를 '최상위 SaaS 필수'로 명시하므로, 기존 Outlook add-in의 세션교환·운영게이트·매니페스트 렌더링 인프라(tools/release/render-outlook-manifest.mjs)를 재사용하는 Word 태스크페인 스켈레톤이 저비용 착수점이다.

--- (low/MISSING) 협업·전자서명 커넥터 생태계(Teams/Slack/Zoom/DocuSign/Google Workspace) 전무 — 통합 연동 요구 중 미커버 잔여분
요구출처: concept-spec.txt §12.3 외부 연동(Microsoft 365의 Teams, Google Workspace, 내부 시스템 CRM/ERP/Billing/Time Tracking/HR/Messenger); concept-obsidian-llm.txt v2.0 §8.1 필수 연동 26종(Teams, Slack, Zoom, DocuSign, Adobe Acrobat, 전자서명 시스템 등)
현재상태: enterprise-platform 다이제스트는 M365 Graph 스텁, 국내 법률데이터 부재, OneDrive 외 마이그레이션 소스 부재를 커버했으나, 협업 메신저(Teams/Slack/Zoom)·전자서명(DocuSign)·Google Workspace 실시간 커넥터는 어느 차원도 언급하지 않았다. 리포지토리에 해당 코드가 0건이다. email-vault 다이제스트의 'Gmail add-on 부재'가 Google 축의 일부만 커버한다.
증거: grep -rniE 'docusign|slack|teams|zoom|sharepoint|google[_ -]?(drive|workspace)' apps/api/src apps/web/src tools → 0건; apps/api/src/modules/integrations → matter-app 단일 (env 플래그 기반 로컬 프로젝션, matter-core 다이제스트가 커버)
개선안: 장기 로드맵 항목으로 갭 등록부에 기록하되 우선순위는 낮게 두라. 컨셉 문서 자체가 Word/Outlook을 1순위 연동으로 지정하므로, 전자서명(체결본 워크플로와 직결)만 계약검토 15단계 finding에 연계해 앞당길 가치가 있다.

--- (low/SHALLOW) 문서 조직화의 '폴더/태그 혼합 구조'(MVP 필수) 중 태그가 전무하고 폴더는 가상 검색폴더로만 대체됨
요구출처: concept-spec.txt §16.1 MVP 필수 기능('폴더/태그 혼합 구조'); concept-obsidian-llm.txt v2.0 §4.2.1(문서 태그, 폴더 및 Matter 기반 문서 분류), §7.1 Matter 필수 속성(tags)
현재상태: document-vault 다이제스트는 '폴더 업로드(구조 보존 업로드)' 부재만 지적했고, 브라우징·조직화 구조로서의 폴더/태그는 어느 차원도 다루지 않았다. 리포지토리에 태그 스키마가 전혀 없고(migrations 전체에서 tags 컬럼/테이블 0건), 문서의 물리 폴더 트리도 없다. 부분 대체물로 governed 가상 폴더(db/migrations/0083_extend_saved_searches_for_governed_folders.sql, /search/folders 라우트)와 Outlook 폴더 매핑(0074)만 존재한다.
증거: grep -rniE '\btags\b' db/migrations → 0건; db/migrations/0083_extend_saved_searches_for_governed_folders.sql — 저장검색 기반 가상 폴더 (물리 트리 아님); db/migrations/0074_create_outlook_folder_mappings.sql — Outlook 메일 폴더 매핑 전용; matters.tags / documents.tags 컬럼 부재 (0013, 0027 마이그레이션 확인)
개선안: governed 검색폴더가 폴더 요구의 상당 부분을 의도적으로 대체한 설계인지 ADR로 명문화하고, 태그(문서·Matter 공용 라벨 테이블 + RLS)는 별도 소규모 갭으로 등록하라. MVP 필수 명시 항목이므로 미구현 사유가 기록되지 않으면 수용성 검증에서 재발견된다.
