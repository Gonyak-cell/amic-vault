# DLP Fixtures

These fixtures are synthetic. They use impossible or reserved-looking values for
pattern validation and must never be replaced with client data, real documents,
real identifiers, or failed redaction drafts.
